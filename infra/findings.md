# Hallazgos y preguntas abiertas

**Alcance: solo infraestructura on-premise.** La infraestructura en proveedores cloud (Azure/AWS) se estacionó fuera de este relevamiento — ver [`../cloud-infra/findings-cloud.md`](../cloud-infra/findings-cloud.md) para lo que ya se había encontrado ahí (incluye un hallazgo importante, una VM de Azure sin inventariar, por si se retoma).

Fuentes: ExportList.csv (export de vCenter, verdad de base para "qué VM existe" — desde el 18 ago 2026 incluye también "Tiempo de actividad"/uptime y una columna "Notas" con anotaciones manuales por VM, cargadas en `vms[].uptime_days` / `vms[].notes` en el inventario), CSV de Relevamiento (manual, 13 clientes, solo WL+DB), y — agregado después — dos emails internos más su adjunto RAR (`source-files/extracted/`), que contiene una matriz de servicios por cliente más completa y un relevamiento de infraestructura Docker. **Todo el material derivado de Relevamiento y de los emails es documentación interna de trabajo, no verdad verificada — tratar cada hallazgo de abajo con las mismas pinzas que aplican los propios documentos fuente** (la hoja "Discrepancias" de la matriz existe justamente porque el propio equipo de Open encontró que sus dos fuentes de datos no coincidían).

## Sobreasignación de RAM en el cluster ESXi principal — confirmada con datos exactos (pedido de Alexis Lombardi, 12/13 sep 2026)

Alexis pidió, por host ESXi: cantidad de VMs, recursos asignados y recursos reales — sospecha que hay más RAM asignada a las VMs de la que un host físico individual realmente tiene, y lo marca como problema latente a informar.

**Versión final — un solo snapshot, capacidad física exacta.** Las dos primeras vueltas de este análisis tuvieron que aproximar la capacidad física (por `Consumed Memory %`) o mezclar fechas distintas (config de agosto + uso de septiembre) porque ningún export traía todo junto. El 12 sep 2026 se regeneraron dos exports con **todas las columnas**, que sí lo traen:

- **`ExportList20260912.csv`** (regenerado, formato completo) — por VM: host ESXi, RAM/vCPU *asignada* (`Memory Size`/`CPUs`, configuración), estado ON/OFF, uso real (`Host CPU`/`Host Mem`). Todo en el mismo snapshot.
- **`ExportList-hosts_and_clusters20260912.csv`** (regenerado, formato completo) — por host: **`Memory Size (MB)`, la RAM física real instalada**, ya no una inferencia. También `CPUs` (sockets físicos).
- **`ExportListPiedras-host_and_clusters20260912.csv`** (15 sep 2026) — mismo tipo de export que el anterior, pero para el host del sitio Piedras (`192.168.100.4`), que hasta ahora quedaba afuera del análisis por no tener este export propio.

Con esto la comparación es directa: RAM asignada a las VMs encendidas vs. RAM física real del host, sin aproximar nada.

| Host ESXi | VMs (total/ON) | vCPU asignada (total/ON) | RAM asignada GB (total/ON) | RAM real usada GB (ON) | **RAM física exacta (GB)** | Excedente/margen GB (%) | ¿Sobreasignado? |
|---|---|---|---|---|---|---|---|
| 192.1.1.214 | 5/4 | 30/22 | 86/62 | 49.7 | 120.0 | −58.0 (−48%) | no |
| 192.1.1.215 | 8/8 | 46/46 | 126/126 | 64.2 | 140.0 | −14.0 (−10%) | no |
| **192.1.1.216** | 11/10 | 33/29 | 129/105 | 51.5 | 63.9 | **+41.1 (+64%)** | **sí** |
| **192.1.1.217** | 7/7 | 56/56 | 110/110 | 71.3 | 95.9 | +14.1 (+15%) | **sí** |
| **192.1.1.218** | 7/5 | 36/30 | 100/66 | 51.6 | 64.0 | +2.0 (+3%) | **sí** (al límite) |
| 192.1.1.219 | 7/6 | 32/24 | 58/52 | 45.9 | 72.0 | −20.0 (−28%) | no |
| 192.1.1.220 | 9/6 | 39/32 | 106/70 | 44.1 | 128.0 | −58.0 (−45%) | no |
| **192.1.1.221** | 22/5 | 60/20 | 128/46 | 32.7 | 40.0 | +6.0 (+15%) | **sí** |
| 192.1.1.222 | 5/3 | 23/13 | 92/36 | 23.4 | 128.0 | −92.0 (−72%) | no (`HA State: Uninitialization Error` — anomalía aparte) |
| **192.1.1.223** | 17/15 | 136/118 | **326/286** | **231.8** | 255.9 | +30.1 (+12%) | **sí** — único `Status: Warning` en vCenter |
| **192.1.1.224** | 16/14 | 115/107 | **324/292** | **238.1** | 255.9 | +36.1 (+14%) | **sí** |
| 192.1.3.252 | 18/11 | 108/60 | 186/110 | 105.7 | 127.3 | −17.3 (−14%) | no |
| 192.168.100.4 (Piedras) | 15/2 | 58/6 | 177/16 | 16.1 | 32.0 | −16.0 (−50%) | no — hoy (ver nota) |

*"RAM física exacta" = `Memory Size (MB)` del host, dato directo. "Excedente/margen" = RAM asignada (ON) − RAM física: positivo es cuánto se pasó de asignar; negativo es margen disponible.*

**Piedras: no sobreasignado hoy, pero con sobreasignación latente severa.** Solo 2 de sus 15 VMs están encendidas (16 GB asignados de los 32 GB físicos). Pero las 13 VMs apagadas suman **177 GB asignados en total** contra apenas 32 GB físicos — si se reactivaran todas a la vez, el host no tiene forma de soportarlo (+145 GB, +453% sobre su capacidad real). RAM/vCPU asignada de Piedras sigue viniendo del export del 18 ago (`ExportList-Piedras-Full.csv`, sin versión más nueva); la capacidad física exacta sí es del 15 sep.

**6 de 12 hosts sobreasignados: `.216`, `.217`, `.218`, `.221`, `.223`, `.224`.** Cambio respecto al corte anterior: **`192.1.1.215` sale de la lista** — con datos exactos tiene 140 GB físicos contra 126 GB asignados (14 GB de margen), no estaba realmente al límite como sugería la aproximación por porcentaje. El resto de la lista se mantiene casi sin cambios en magnitud, lo que valida que el método por porcentaje (usado en las dos vueltas anteriores) era razonablemente confiable como primera aproximación. En GB absolutos `.223`/`.224` siguen siendo los más críticos (+30/+36 GB); en % relativo **`.216` sigue siendo el peor (+64%)**. `.218` queda al límite justo (+3%, +2 GB) — el más frágil de los seis.

- **Tres blind spots resueltos de paso.** Las 3 VMs nuevas sin host/IP conocidos (ver más abajo, "GIAR — nuevo export...") ya tienen dueño en este export completo: `OL7SIGOPROD` → `192.1.1.215` (`192.1.1.66`), `OL8CASLAWL01` → `192.1.3.252` (`192.1.1.64`), `OL8HEINPROD` → `192.1.3.252` (`192.1.1.63`) — las tres Oracle Linux 7, con 10/18/22 días de uptime respectivamente (consistente con ser altas recientes). Cargado en `inventory.json` → `vms[]`.
- **Hallazgo colateral: dos VMs de `192.1.1.223` (el host con más `Status: Warning`) cambiaron de configuración entre el 18 ago y el 12 sep.** `OPENDBPR001`: 32→20 GB RAM, 16→12 vCPU (bajó). `DMWL01`: 16→24 GB RAM, 4→10 vCPU (subió). No hay forma de saber desde acá si fue una mitigación deliberada del problema de sobreasignación o un cambio no relacionado — vale la pena preguntar.
- **CPU sigue sin poder confirmarse, pero ahora se conocen los sockets físicos por host** (columna `CPUs` del export de hosts): 1 socket en `.216`/`.219`/`192.1.3.252`, 2 en el resto. Sin cores ni GHz no alcanza para replicar el mismo análisis de sobreasignación en CPU — requeriría TeamViewer (Summary de cada host) o un export adicional.
- Cargado en `inventory.json` → `esxi_capacity` (`physical_capacity_known: true`, ya no `"approximate"`), incluyendo ahora Piedras.

## DVAL — destino de migración de la matriz (`10.77.7.14`/`PRODDVAL`) descartado, no existe en la red (15 sep 2026)

La matriz declara para DVAL un destino de migración nunca antes revisado: `sid_nuevo: PRODDVAL`, `servidor_db_destino: 10.77.7.14` — mismo patrón que destrabó a EBY/GIAR/ROMAN/Rex (stack legado vs. consolidado nuevo). Se probó el mismo camino barato que cerró esos cuatro clientes: `sudo grep -iE 'dval|proddval'` sobre el `tnsnames.ora` y el `formsweb.cfg` de `OPENWLPROD01` (donde ya hay `sudo` real, sin necesitar credencial nueva) — **cero coincidencias en ambos archivos**. A diferencia de GIAR/ROMAN, acá no hay ni siquiera un alias de config — DVAL no está deployado en este stack consolidado.

Se probó además si `10.77.7.14` responde en absoluto, desde el mismo host (`OPENWLPROD01`, mismo segmento `10.77.x`): el puerto Oracle (`1521`) dio `CLOSED` y el `ping` devolvió **`Destination Host Unreachable`** — no es un firewall filtrando el puerto, es una IP sin nada detrás en ese segmento. **La migración a `PRODDVAL`/`10.77.7.14` nunca se completó** (o la nota de la matriz está mal, o la VM nunca se provisionó) — descarta esta vía como atajo para cerrar la capa 6 de DVAL sin la credencial bloqueada.

**Conclusión: el único camino que queda para DVAL sigue siendo `DBClientes.238` (`192.1.1.238`, DB legada), con la credencial rechazada dos veces (`root` y `soportesmart`, ver `QUESTIONS.md`).** No hay atajo de config como el que funcionó para los otros cuatro clientes con patrón legado/consolidado.

## Nuevo: infraestructura on-premise real que no está en nuestro inventario

Buscamos todas las IPs mencionadas en el material fuente y las cruzamos contra `inventory.json`. Del lado on-premise, encontramos una sola cosa sin identificar:

- **Servidor(es) NFS.** `OPENDOCKER.57` y `VM-DOCKER-Clientes` montan recursos NFS externos (uno con "montaje con error" y otro "con alta ocupación", según el relevamiento de Docker) pero el host NFS nunca se nombra. Fácil de resolver nosotros mismos: entrar por TeamViewer a esos dos hosts y correr `mount`/revisar `/etc/fstab`.

(El mismo cruce encontró dos cosas del lado cloud — una VM de Azure sin inventariar y una mención sin detalle de AWS — documentadas en `cloud-infra/findings-cloud.md` en vez de acá.)

## Nuevo: ¿cuál fue el cliente/servidor incorporado o migrado más recientemente? (18 ago 2026)

vCenter no expone fecha de creación de VM en el export que tenemos, y no hay acceso programático a los hosts — así que no hay una respuesta exacta. Pero el export enriquecido con "Tiempo de actividad" (uptime desde el último reinicio) y "Notas" manuales da la mejor señal disponible hasta ahora:

- **Señal fuerte, explícita: `DASADBPROD01` (base de datos de producción de Maipú).** La nota dice literalmente "DASA db PRODUCCION NUEVO" — no es una inferencia por uptime, es una anotación directa de que es una base nueva. Maipú figura como `"Solo infraestructura"` en `inventory.json` (todavía sin WebLogic/Condor terminado de configurar sobre esa base), consistente con una incorporación en curso.
- **Señal débil, por uptime bajo: `OPENDBPROD006` (base de datos de Entidad Binacional Yacyretá, 28 días de actividad)** — el uptime más bajo entre todos los servidores de cliente encendidos. EBY figura como `"Migrado / actualización pendiente"`.
- **Señal débil, por uptime bajo: `OL8LABWL01` (WebLogic de Heinlein, 55 días de actividad)** — segundo uptime más bajo. Heinlein también figura como `"Solo infraestructura"`.
- Los tres clientes con las señales más fuertes (Maipú, EBY, Heinlein) coinciden en tener un `status` de migración/infraestructura incompleta en el inventario — consistente con la idea de que son las incorporaciones más recientes y todavía en curso.

**Caveat importante:** el uptime mide el último reinicio de la VM, no su fecha de creación — un reinicio por mantenimiento o parche produce la misma señal que una VM recién provisionada. Solo `DASADBPROD01` tiene evidencia directa (la nota "NUEVO"); el resto es inferencia. Para una respuesta definitiva, preguntar directamente al equipo saliente en la reunión pendiente, o (si hay una sesión de TeamViewer a vCenter) revisar la propiedad "Created"/`Config.CreateDate` de cada VM candidata — no está en el export CSV que tenemos.

## Resueltos / confirmados

**DCVIAJES — capa 6 cerrada del todo, evidencia transaccional directa en vez de una sesión capturada (15 sep 2026).** `root@192.1.1.190` — `ORACLE_HOME`/`ORACLE_SID` no vienen en el `PATH` por defecto (mismo patrón ya visto en GIAR), seteados a mano (`/app/oracle/product/11.2.0/dbhome_1`, `ORACLE_SID=DCVIAJES`). `v$session` en el momento solo mostró `SYS` (la propia consulta) y `DBSNMP`/`emagent` (agente OEM) — sin sesión de aplicación viva en ese instante. Pero `v$sqlarea` filtrado por `parsing_schema_name = 'CONDOR'` reveló el patrón clásico de Forms para numerar documentos — `SELECT NVL(MAX(RENGLON),0)+1 FROM AUDIT_TNUMDOC`, `INSERT INTO AUDIT_TNUMDOC (...)`, `SELECT UID FROM SYS.DUAL`, 43 ejecuciones cada una, **13-sep-26**. `AUDIT_TNUMDOC` es una tabla de auditoría real con columnas `USERID`/`NOMUSR`/`TERMINAL` — leída directamente: usuario `343`/`CONTABLE`, terminal `WEB`, 43 documentos `NDPROV` (Nota de Débito a Proveedor) numerados el 13-sep para dos empresas del mismo tenant (`EMP` 21 y 90). Es evidencia transaccional real con usuario nominal — más fuerte que una sesión de red capturada — cierra capa 6 a 1.0 sin necesitar agarrar una sesión en vivo. Bono indirecto: confirma que el módulo de aplicación funciona de punta a punta (capa 4) — un documento real llegó a la DB — aunque el proceso Forms específico de DCVIAJES en `WebLogic.191` no se haya observado directamente (sigue sin `sudo` en ese host, mismo permission wall de siempre). Cabo suelto menor: `TERMINAL = 'WEB'` sugiere que al menos parte del flujo entra por una interfaz web (¿ORDS?), no solo Forms clásico — la matriz no declara ningún producto además de `Enterprise: Sí`, vale la pena anotarlo como blind spot chico, no bloqueante.

**Nuevo export de vCenter (`ExportList20260912.csv`, 12 sep 2026) — todo el stack legado de GIAR confirmado apagado; 3 VMs nuevas descubiertas.** Formato más liviano que `ExportList.csv` (solo `Name`/`State`/`Status`/`Provisioned`/`Used`/`Host CPU`/`Host Mem`, sin IP/ESXi host/guest OS), pero sirve para detectar cambios de estado desde el 18 ago. Cruce completo contra `inventory.json` (`vms[]`, 144 registros) — metodología según `CLAUDE.md` §3 (re-correr el cruce, no mezclar a mano):

- **`WL-GIAR` y `DB-GIAR` (todo el stack legado de GIAR) pasaron de `Encendido` a `Apagado`.** Descubierto durante el trazado en vivo de GIAR (`plan_relevamiento_alta_giar.md`): al ir a comprobar `WL-GIAR` por SSH, la VM resultó estar apagada en vCenter mismo (no solo "sin sesiones", como pasó con ABB — apagada de verdad). El nuevo export confirma que `DB-GIAR` corrió la misma suerte. Es evidencia más fuerte que la de ABB para la pregunta de baja de `QUESTIONS.md`: ahí la VM seguía encendida sin tráfico; acá el stack completo está apagado. Cierra capa 4 y 6 del legado de GIAR por ausencia directa — no hace falta SSH/`sqlplus`, no puede haber proceso vivo en una VM apagada. La pregunta que queda abierta es si GIAR sigue vivo por el stack "nuevo" (`OPENWLPROD01`/`PRODGIAR`, alias TNS ya conocido pero sin tráfico confirmado) o si está de baja del todo — ver `plan_relevamiento_alta_giar.md` para el próximo paso.
- **3 VMs completamente nuevas, ninguna vista en ningún export anterior:** `OL7SIGOPROD`, `OL8CASLAWL01`, `OL8HEINPROD` (las tres `Encendido`). El export liviano no trae IP/ESXi host/guest OS — pendiente TeamViewer para esos datos.
  - **`OL7SIGOPROD`** — coincide en tiempo con el cierre de `Database .90`/`SIGO` de hoy mismo (ver la entrada de EBY más abajo). Podría ser un app server dedicado nuevo para SIGO, separado del `WebLogic.191` compartido.
  - **`OL8HEINPROD`** — refuerza el hallazgo ya anotado del 6 sep (PDB `HEINLEIN_PROD` en `CDBOPEN03`, deliberadamente sin investigar por estar Heinlein fuera de foco): Heinlein tendría producción real, no solo el ambiente de test ya confirmado. El `status: "Solo infraestructura"` de `inventory.json` quedaría desactualizado si esto se confirma.
  - **`OL8CASLAWL01`** — **blind spot nuevo, sin ningún rastro previo en el proyecto.** El nombre no coincide con ninguno de los 15 clientes conocidos. "Casla" es el apodo popular de Club Atlético San Lorenzo de Almagro — mismo patrón de nombre que "BOCA" (Boca Juniors, cliente confirmado). El sufijo "WL01" sigue la convención de nombre de `OL8LABWL01` (WebLogic de Heinlein). Candidato fuerte a cliente nuevo sin ningún rastro previo — requiere TeamViewer para IP/rol.
- **Ningún VM desaparecido:** las 144 VMs previas (129 del sitio principal + 15 de Piedras) siguen todas presentes por nombre en el export nuevo.
- Otros 3 cambios de estado sin cliente asignado (`OPENDB19DEV01`, `OPENDBPROD011`, `OPENOEM24ai`, todas `Encendido`→`Apagado`) — infraestructura interna/dev, baja prioridad.

**GIAR — el stack "nuevo" es real y activo, no solo un alias muerto (12/13 sep 2026).** Con el legado descartado (arriba), se entró a `PRODGIAR` (PDB en `OPENDBPROD001`/`CDBOPEN03`, vía `su - oracle` + `ORACLE_HOME`/`PATH` manuales — `sqlplus` no está en el `PATH` por defecto ni para `root` ni para `oracle` en este host). `dba_segments` (top por tamaño) reveló, además de `CONDOR` (7.5 GB)/`CONDORBKP` (4.5 GB)/`CONDOR_ORIG` (4.2 GB) — patrón de copia viva/backup/original —, un schema **`MOTOROLA`** (464 MB, 70 segmentos) nunca visto antes. Hipótesis a confirmar: GIAR = "Arris de Argentina S.A.", y Arris adquirió el negocio de set-top-boxes de Motorola Home — `MOTOROLA` podría ser el nombre legado real de los datos de GIAR, previo al rebrand. `dba_tab_modifications` dio 0 filas para los 6 schemas candidatos. `v$session` filtrado por `con_id` (corrigiendo el intento anterior, que sin el filtro mezclaba sesiones de toda la CDB — aparecían usuarios de Rex Argentina) dio **cero sesiones de aplicación** — pero la consulta fue en sábado, mismo caveat que ya jugó en contra con ROMAN/DCVIAJES.

- **`dba_objects.last_ddl_time` da una señal a favor de actividad real, no en contra.** Procedimientos de negocio de `CONDOR` (`EJECUTA_NOTIF`, `JENVIOALERTAS_VENCIMP`, `BORRA_DEBUG_APP`) tocados **12/13-sep-26** (hoy/ayer) — mientras que el procedimiento propio de `MOTOROLA` (`GENERA_ARCHIVOS`) no se toca desde **29-may-26**. `last_ddl_time` mide cambios de código/recompilación, no ejecución — no es prueba directa de uso, pero un procedimiento de negocio recompilado tan reciente en un cliente "en disputa" no se puede ignorar.
- **Confirmación decisiva: `formsweb.cfg` real de `OPENWLPROD01` tiene secciones dedicadas a GIAR.** `sudo grep -iE giar` sobre el `formsweb.cfg` de `WLS_FORMS` (ruta real: `.../config/fmwconfig/servers/WLS_FORMS/applications/formsapp_12.2.1/config/formsweb.cfg` — el primer intento con un glob más corto no encontró el archivo, hubo que ubicarlo con `sudo find`) encontró **`[giargprod]`** (`pageTitle=Giarg PRODUCCION`, `userid=@PRODGIAR`, `form=/u01/cliente/giarg/cdr2/menues/cdr2.fmx`), `[activaciongiarg]`, `[giargprodFSAL]`. "Giarg" coincide letra por letra con `sid_actual: GIARG` de la matriz. **Es deployment real y deliberado, mismo patrón que EBY/ROMAN — GIAR sí migró al stack nuevo, no es solo un alias de DB de paso** (descarta la hipótesis que se manejaba horas antes, cuando solo se sabía del alias TNS).
- **Conclusión revisada:** el legado (`WL-GIAR`+`DB-GIAR`) está muerto, confirmado sin dudas. Pero el stack nuevo (`OPENWLPROD01`/`giargprod` → `PRODGIAR`/`CONDOR` en `OPENDBPROD001`) tiene app desplegada real y señales de actividad reciente en el código — **no se puede concluir "GIAR de baja" con esto**. Falta solo confirmar sesión de usuario real en día hábil (la consulta de hoy fue en sábado) para pasar de "config confirmada" a "en vivo confirmado".
- **Actualización (14 sep 2026, lunes — día hábil): cuatro fuentes independientes, todas en cero.** Se repitió `v$session` filtrado por `con_id` en día hábil: de nuevo **cero sesiones de aplicación** en `PRODGIAR`, ya sin el caveat de "es fin de semana". Sumado a eso, en `OPENWLPROD01`: `sudo netstat -tn | grep 10.77.7.11` **sin una sola conexión** hacia la DB de GIAR, y `grep -iE 'giarg|activaciongiarg'` sobre el `access.log` de `WLS_FORMS` — **el actual más 7 rotados, cubriendo del jueves 10-sep al lunes 14-sep (3 días hábiles completos)** — con **cero coincidencias**. Es la misma prueba, con el mismo peso, que cerró ROMAN (dos fuentes en cero) — acá son **cuatro**: `v$session` (dos veces), `netstat`, y `access.log` de varios días. **GIAR queda con el mismo perfil que ROMAN: deployment de producción real y deliberado (`formsweb.cfg`, alias `PRODGIAR`), pero sin un solo usuario activo observable en ningún punto de la ruta.** No es prueba de una decisión formal de baja (eso sigue siendo pregunta de negocio, `QUESTIONS.md`), pero la evidencia técnica ahora respalda la nota informal de la matriz ("de baja, se mantienen las bases") mucho más que el campo formal ("Mantenimiento solamente") o la `observación` ("ya se está usando como producción"). Cabo suelto sin resolver, no bloqueante: el schema `MOTOROLA` en la misma PDB, con última recompilación de mayo — no aporta a la pregunta de tráfico actual. **GIAR queda con las 7 capas trazadas** (1/2 sin dominio conocido para el stack nuevo — nadie encontró por dónde entrarían usuarios reales si los hubiera —, 3/4/6 cerradas, 5/7 sin trazar por baja prioridad dado el resultado).

**Argocean — capa 6 cerrada por ausencia directa, sin necesidad de sesión (12 sep 2026).** Octavo trazado punta a punta ([`plan_relevamiento_alta_argocean.md`](../plan_relevamiento_alta_argocean.md)), el cliente con menos datos de los 15 — descubierto solo por una nota suelta en la hoja Discrepancias (`DB 172.18.5.60` / SID `MBA`, "no figura en tabla funcional"), sin `code` ni `matrix_detail`. A diferencia de GIAR/ABB, acá no hizo falta inferir inactividad por ausencia de sesiones/DML: `DB-ARGOCEAN` figura directamente `Apagado` en `ExportList.csv` (uptime_days: 0, sin IP asignada) — el dato más duro posible, gratis, sin TeamViewer.

- **Capa 6 — cerrada.** `DB-ARGOCEAN` apagada, confirmado por la fuente más confiable del proyecto (vCenter). No puede haber instancia Oracle corriendo en una VM apagada. El SID reclamado `MBA` no se parece a ningún alias visto en otros `tnsnames.ora` del proyecto — sin poder confirmarse a menos que la VM se prenda.
- **Capa 3 — cerrada, con un hallazgo de exposición real (13 sep 2026).** En el dashboard de `FW` (`192.1.3.1`, capturas de pantalla), las 5 reglas de Argocean/ROMAN de interés están **todas habilitadas** (check verde + texto normal; la única regla deshabilitada del listado completo es `FW Https`, gris/atenuada — confirma que la columna de estado sí es legible visualmente aunque la transcripción a texto de `pfsense-192.1.3.1-nat-rules.txt` no la haya capturado). Pero **la restricción por alias `Argocean_IP_Publicas` es letra muerta en la práctica**: hay una tercera regla, `Publicacion de Weblogic 11gR2` (origen `*`, sin restricción), habilitada y listada *antes* que las reglas con alias (`ROMAN_IP_PUBLICAS` y `Argocean_IP_Publicas`) para el mismo destino exacto (`WAN:8080` → `172.18.5.40:80`). pfSense evalúa NAT top-to-bottom, gana la primera que matchea — cualquier IP de Internet ya entra por la regla genérica antes de necesitar pertenecer a ningún alias. El alias documenta intención de control de acceso, no control de acceso real vigente. Contenido de `Argocean_IP_Publicas` (parcial, la UI lo trunca): `186.22.238.146, 190.229.251.241, 200.68.123.109, 190.210.104.50, 200.55.243.90, 186.22.245.44, 186.128.254.54, 190.194.229.35, 186.189.239.45…` — IPs públicas argentinas (ISPs residenciales/corporativos por el rango), consistentes con clientes/usuarios reales, no infraestructura interna.
- **Capa 4 — host vivo y con actividad reciente confirmada; atribución Argocean vs. ROMAN sin resolver por muro de permisos (13-14 sep 2026).** `WL-CLIENTES` (`172.18.5.40`) no está aislado de red ni dormido: primero confirmado desde el firewall mismo (`ping`/SSH vía `Diagnostics > Command Prompt` de pfSense, ver más abajo), y finalmente **accedido de verdad por consola remota de vCenter** (`soportesmart`, sin necesidad de red — evita el problema de rutas externas/SSH del todo). Adentro:
  - **`ps -ef`:** dominio **`ClassicDomain`** (`/app/oracle/mid/user_projects/domains/ClassicDomain`), Oracle Forms & Reports **11.1.1** genuino (`Oracle_FRHome1`, JDK 1.7.0_55) — primer host del proyecto donde el dato "11gR2" de la descripción NAT resulta literal, no el patrón "funcional decía 11, real era 12" visto en GIAR/JOBS/BOCA/ROMAN. `WLS_FORMS` corriendo sin cortes desde **2024** (coincide con los 823 días de uptime de la VM); `WLS_REPORTS` **reiniciado el 11 de septiembre** (2-3 días antes de esta sesión); un worker de motor de reportes (`rwEng-0`) lanzado **el mismo día de la consulta a las 13:29** — actividad real y reciente, no un proceso zombie.
  - **`netstat -tlnp`:** confirma que el puerto `80` (el que espera la regla NAT sin restricción de alias que ya habíamos encontrado, ver capa 3 más arriba) tiene un Apache/OHS real escuchando en `172.18.5.40` — la exposición encontrada en el firewall no apunta al vacío. También `:9001`/`:9002` (managed servers) y `:7001` (consola admin) bindeados a esa misma IP.
  - **Sin poder atribuir a Argocean específicamente.** El host es compartido con ROMAN, y sin `sudo` (usuario `soportesmart`, UID 509, sin privilegios) no se pudo leer `formsweb.cfg` (`Permission denied`, mismo muro `oracle:oinstall` ya visto en `WL12C-Desarrollo`/`WebLogic.191`) ni ningún log de acceso (`find / -iname "*access*.log"`/`"*.log" -newer ...` — ambos vacíos, sin resultados ni siquiera parciales). Un intento de distinguir por HTTP (`curl` a `frmservlet?config=csm` vs. `config=argocean` vs. un nombre inventado) no sirvió — Forms devuelve la misma plantilla genérica `base.jpi.htm` sin validar el parámetro en ese paso.
  - **Nota aparte, sin explicar:** la consola de vCenter aceptó `soportesmart` sin problema, pero ese mismo usuario/password fue rechazado por SSH (`Permission denied`) antes de lograr el acceso por consola — podría ser una restricción de PAM/sshd específica para acceso remoto, o que el intento de SSH usó una contraseña vieja/incorrecta que se corrigió después. Sin explicación confirmada.
  - **Rutas externas siguen sin funcionar** (SSH directo, SSH vía NAT pública `200.55.243.116:215`, SSH administrativo al propio `FW`) — no se pudo confirmar con certeza desde qué estación de trabajo se corrieron esos intentos, así que no se atribuye a Piedras con seguridad (corrección de una entrada anterior de esta misma sesión).
  - **Conclusión:** capa 4 queda con el host confirmado activo (contradice parcialmente la hipótesis de "nunca llegó a producción"), pero sin poder aislar si la actividad es de ROMAN o de Argocean — la ruta de dominio de Argocean en el NPM sigue deshabilitada, lo único que sigue apuntando en esa dirección.
- **Ruta de dominio deshabilitada.** `argocean.condorenterprise.com.ar` → `172.18.5.40:80` en el NPM de `VM-DOCKER-Clientes`, `enabled=0` (visto 19 ago 2026) — coherente con un cliente sin uso real hoy.
- **Nota de negocio distinta a ABB/GIAR:** `status: "Inventario técnico incompleto"` y la ausencia total de este cliente en la tabla principal de la matriz sugieren que Argocean nunca llegó a producción, no que se dio de baja después de haber tenido uso — matiz para `QUESTIONS.md`.

**EBY — capa 6 cerrada del todo, 7/7; `Database .90` descartada como su DB, blind spot de SIGO resuelto de paso (12 sep 2026).** Único cabo suelto que le quedaba a EBY desde el 2 sep ([`plan_relevamiento_alta_eby.md`](../plan_relevamiento_alta_eby.md)): a qué cliente correspondía `Database .90`/`CDRADM`, la DB compartida detrás de la ruta paralela legada `yacyreta.condorwork.com.ar` → `WebLogic.191`. `ssh root@192.1.1.90` (misma credencial que ya abrió `192.1.1.32`/`CLIENTES-DB` para BOCA) → `su - oracle` → `sqlplus / as sysdba`. Oracle 11.2.0.4.0, non-CDB (`v$database` no tiene columna `cdb` — `ORA-00904` al intentarlo, confirma que no es 12c+), única instancia levantada `CDRADM` (`OPEN` declarada en `/etc/oratab` pero apagada).

- **De los 8 inquilinos de `WebLogic.191`, solo `SIGO` existe como schema real en esta DB.** `dba_segments` (top 15 por tamaño) + `SELECT username FROM dba_users WHERE username IN ('EBY','YACYRETA','YACYRET','CEFAS','DCVIAJES','DVAL','UIA','MAFISA','TASSAROLI','SIGO')` devolvió una sola fila: `SIGO`. 2.6 GB, 211 segmentos, **último DML 12/09/2026 11:00 — uso productivo el mismo día de la consulta**, más una sesión `SIGOWEB` vía JDBC Thin activa desde un contenedor Docker. Ninguna de las otras 7 (incluidas `EBY`/`YACYRETA`) tiene schema propio acá.
- **Cierra EBY por descarte, no por hallazgo positivo.** La ruta productiva real (`eby-prod` → `OPENWLPROD01` → `OPENDBPROD005`, cerrada por `tnsnames.ora` oficial desde el 2 sep) sigue siendo la única con DB propia confirmada. La ruta paralela `yacyreta`/`WebLogic.191` no tiene una DB de EBY identificable — lo que hay detrás de esa VM compartida es la DB de SIGO. **EBY pasa de 6/7 a 7/7 capas cerradas.**
- **Blind spot resuelto: `SIGO` (uno de los 4 clientes de `WebLogic.191` sin VM mapeada — junto a `UIA`, `Mafisa`, `Tassaroli`) ahora tiene DB confirmada en vivo: `Database .90` (`192.1.1.90`, SID `CDRADM`).** Sigue sin VM propia de WebLogic (usa el compartido `.191`) y sin credencial/entry point propio investigado — pero ya no es un nombre suelto sin infraestructura detrás. `UIA`, `Mafisa` y `Tassaroli` siguen sin resolver.
- **Cabo suelto menor, no bloqueante:** la sesión `AGARCIA` vía `frmweb@NAI` (`NAI` = hostname real de `WebLogic.191`, Forms real) es una cuenta nominal de usuario, no un schema de cliente — no se pudo atribuir a ningún tenant específico con el método usado (podría ser un usuario de SIGO con login personal, o de otro inquilino sin schema propio). Sin seguir esa punta — excede el alcance de "a quién pertenece `.90`", que ya quedó resuelto para lo que hacía falta (EBY, descartado; SIGO, confirmado).

Detalle completo cargado en `inventory.json` → `vms['Database .90'].db_detail` y `clients[EBY].database.resolved`.

**GIAR — capa 3 cerrada (config), backend legado confirmado por NAT explícito; nuevo blind spot (segundo stack CEFAS + cliente "SYT") (12 sep 2026).** Transcripción completa de Firewall > NAT > Port Forward de `FW` (`192.1.3.1`, pfSense confirmado del segmento aislado `192.1.3.252` — nunca antes accedido), 53 reglas, cargadas en `inventory.json` → `vms[FW].nat_rules`.

- **`giarprod.condorenterprise.com.ar` → `200.55.243.117:80` (NPM `DOCKER-DEB`) tiene backend real confirmado: `WL-GIAR` (`10.10.1.50`).** Dos reglas explícitas, `source: *` (sin restricción de IP): `200.55.243.117:80` → `10.10.1.50:80` ("GIAR WL http") y `:443` → `:443` ("GIAR WL"). Cierra capa 3 de punta a punta — el tráfico entra por `DOCKER-DEB`, sale de nuevo por la IP pública de `FW`, y termina en el WL legado de GIAR. SSH también mapeado (`200.55.243.117:215` → `10.10.1.50:22`). Falta capa 4: confirmar que hay un proceso vivo escuchando ahí (nadie entró a `WL-GIAR` todavía).
- **`DB-GIAR` (`10.10.1.9`, legado) tiene ruta de acceso conocida** — SSH vía `200.55.243.117:212`, listener Oracle vía `200.55.243.117:1522` ("mitiga GIAR DB 1521"). Sin acceder todavía.
- **Candidato de DB nuevo, sin confirmar: `10.1.1.10`, `200.55.243.117:51521` → `10.1.1.10:1521`, descripción literal "mitiga GIAR DB 1521 NUEVO".** Subred `10.1.1.x` no coincide con ningún otro segmento del proyecto — podría ser un error de tipeo por `10.10.1.10`, o una red genuinamente nueva. El nombre "NUEVO" sugiere que sería el reemplazo del legado `10.10.1.9` — candidato a verificar antes que el legado, sin VM identificada en `ExportList.csv`.
- **Cierra el ítem 1 de "Todavía abierto" (instancias extra `WL-ROMAN`/`DB-ROMAN-HISTORICO`).** Las reglas `SSH WLromanHist` (`172.18.5.39:22`) y `Publicacion de Weblogic` (`172.18.5.39:8888`) confirman `WL-ROMAN` como el WL histórico/legado de ROMAN (distinto de `WL-CLIENTES`, la ruta legada actual). `SSH DBromanHist` (`172.18.5.38:22`) + `Publicacion de Base Datos` (alias `ROMAN_IP_PUBLICAS`) confirman `DB-ROMAN-HISTORICO` de la misma forma. Ambos cargados en `inventory.json` con `used_by_clients` → ROMAN.
- **Resuelve un blind spot viejo:** `ords-romans.open.com.ar` → `200.55.243.116:2235` (visto el 1 sep, IP sin destino conocido) resulta ser la misma `FW` — la regla `wl12-cl-syt-ORDS-ROMANS` (`WAN:2235` → `172.18.5.111:8001`) lo confirma. `200.55.243.116` es la IP WAN primaria de `FW` (la que ya se conocía); `200.55.243.117` es una IP WAN secundaria explícita, usada para GIAR/CEFAS/NGINX.
- **Confirma a `WL-CLIENTES` (`172.18.5.40`) como compartido por tres clientes a la vez:** reglas separadas por alias de origen — `ROMAN_IP_PUBLICAS` y `Argocean_IP_Publicas`, ambas apuntando al mismo `172.18.5.40:80` ("Publicacion de Weblogic 11gR2 (ROMAN)" / "...argocean") — confirma en la propia config del firewall lo que ya se sabía por el NPM (WL compartido ROMAN/Argocean).
- **Blind spot nuevo, no menor: un segundo stack de CEFAS y un cliente "SYT" sin ningún rastro previo**, todos en este mismo segmento aislado — `CEFAS WL` (`10.10.1.100`), `mitiga CEFAS DB 1521` (`10.10.1.8`), `Cefas CondorLink`/`CEFAS CONDORLINK` (`172.18.5.243` **y** `10.10.1.43`/`VM-DOCKER-Clientes (1)` — dos IPs con el mismo rol), y `WL12-CLI-SYT`/`DB-CLI-SYT` (`172.18.5.111`/`.112`, con consola WebLogic propia en `:7001`). Ninguna de estas 5 IPs nuevas aparece en `ExportList.csv`. "SYT" no coincide con ningún cliente conocido ni con los nombres sueltos ya vistos (Mafisa/UIPBA/COBRA/Allianz/Monroe/Rovella/SMQ/NABSA) — ver `blind_spots` en `inventory.json` para el detalle y las rutas SSH ya mapeadas para investigar cada IP.
- **`VM-DOCKER-Clientes (1)` (`10.10.1.43`/`172.18.5.123`/`192.1.3.4`) confirmada como el 4º host Docker/NPM que quedaba sin transcribir** (ver findings.md 1 sep) — sirve NGINX genérico (puertos 81/80/443) y, sorprendentemente, también contenedores CEFAS CONDORLINK (compartiendo rol con `172.18.5.243`).
- **Detalle metodológico:** a diferencia de las reglas de `FWOPEN` (todas `source: *`), varias de `FW` usan alias de origen dedicados (`ROMAN_IP_PUBLICAS`, `Argocean_IP_Publicas`, `OPEN_REDES_PUBLICAS_2024`) — restringir por alias en vez de abrir a todo Internet. **Revisado para Argocean (13 sep 2026, ver entrada más abajo): la restricción es en la práctica letra muerta** — una regla previa sin restricción, para el mismo destino, ya deja pasar cualquier IP. No se revisó todavía si `ROMAN_IP_PUBLICAS` tiene el mismo problema estructural (misma regla genérica lo cubre) ni el contenido de `OPEN_REDES_PUBLICAS_2024`.

**BOCA — capa 6 cerrada, trazado completo 7/7, uso productivo activo confirmado en vivo (12 sep 2026).** Único paso que quedaba pendiente desde la sesión del 6 sep ([`plan_relevamiento_alta_boca.md`](../plan_relevamiento_alta_boca.md)): `sqlplus` a `CLIENTES-DB` (`192.1.1.32`, `root`, ya accesible desde el trazado de CEFAS/ABB). `ORACLE_SID=BOCA` → `v$database` mostró `CDB=YES`: la instancia `BOCA` es una CDB multitenant, prácticamente vacía a nivel raíz (solo schemas de infraestructura Oracle — `SYS`, `APEX_180200`, `OJVMSYS`, etc., nada de negocio). Los datos reales viven en la PDB **`BOCAPDB`** (`READ WRITE`): schema `CONDOR` con **~22.5 GB** (2571 segmentos, coincide con los `18G` declarados en la matriz), más `CONDORBI` (105 MB) y un schema chico `BOCA` (11 MB, legado/menor). Charset `WE8MSWIN1252` confirmado dentro de la PDB (= matriz, el único cliente del parque con este charset). **Último DML de `CONDOR`: 12/09/2026 21:37 — el mismo día de la consulta, uso productivo en vivo sin ambigüedad**, mejor evidencia posible de capa 6 lograda hasta ahora en el proyecto. Resuelve la fila "BOCA / SID" de Discrepancias: **`BOCA` (CDB) y `BOCAPDB` (PDB) eran las dos correctas a la vez**, no una disputa real — mismo patrón multitenant que Rex/GIAR (`CDBOPEN03`).

**ABB — trazado completo (7/7 capas), DB productiva confirmada, y evidencia de que está apagado de hecho desde el 1‑jul‑2026 (6 sep 2026).** Quinto trazado punta a punta ([`plan_relevamiento_alta_abb.md`](../plan_relevamiento_alta_abb.md)), elegido para cerrar el último cabo de Tier 1 que dependía solo de nosotros. El plan de BOCA esperaba leer el datasource de ABB de paso en `192.1.2.54`, pero ese box no tiene consola ni `sudo` — se cerró por `sqlplus` directo.

- **Capa 6 — DB productiva = `192.1.1.31` (`DBClientes-12C.31`), instancia `ABB`, schema de app `CONDOR` (~6.9 GB).** `root@192.1.1.31` (misma credencial que sirvió para `192.1.1.90` en el trazado EBY). Instancia `ABB` = non‑CDB, `READ WRITE`, `NOARCHIVELOG`, levantada desde el 31‑ene‑2026; charset `WE8ISO8859P1` (= matriz). El schema `ABB` del mismo box es legado (último DML abr‑2021, 612 MB) — los datos vivos están en `CONDOR`. **`192.1.1.190` descartada:** su `/etc/oratab` (Oracle 11.2) no tiene instancia `ABB`, solo `abbhist`/`abbtubio` (sub‑bases históricas, apagadas); lo único levantado ahí es `ora_pmon_DCVIAJES`.
- **Capa 1 — ruta viva pero sin uso real desde el 1‑jul‑2026.** `abb.condorwork.com.ar` (NPM `DOCKER-DEB`, `proxy_host` id 28) → `192.1.2.54:9001`. Access log (`proxy_host-28.log`, 198k líneas, oct‑2022 → hoy): última **sesión Forms real** (`POST .../forms/lservlet` 200, applet Java) el **01/07/2026 20:24 UTC**; después solo 3 `GET .../forms/frmservlet?config=ABB` sueltos (21‑jul, 18‑ago, UA de navegador, sin login) — pinta de chequeo "¿sigue arriba?". Agosto y septiembre: cero. La población de usuarios de 2026 estaba concentrada en un solo bloque `98.98.27.0/24` (ARIN/AT&T — probable egreso corporativo de ABB), no una base repartida como BOCA.
- **El corte calza exacto entre dos fuentes independientes:** último DML del schema `CONDOR` en `192.1.1.31` = **01/07/2026 17:24** (`dba_tab_modifications`) = misma fecha/hora que la última sesión Forms del access log (17:24 ART = 20:24 UTC). El `last_login` viejo de `CONDOR` (05/05/2026) no contradice — es una sesión *pooled* que se logueó una vez en mayo y sirvió hasta el apagado (`last_login` guarda el último login, no la última actividad; AWR retention de `.31` = 8 días, no llega a junio).
- **Capas 2‑5 y 7:** NPM = `DOCKER-DEB`. Firewall = NAT genérico, sin regla dedicada; comparte con BOCA la exposición `FWOPEN` regla `test` → WAN `9998` → `192.1.2.54:9001`. Motor clásico = `WLS_FORMS` en `192.1.2.54` (`base_domain`, F&R `12.2.1.4.0`, config `ABB` responde 200), sin managed server dedicado — heredado de la sesión BOCA. Capa 5 = no aplica (sin contenedor `abb`/`jasper` en `OPENDOCKER01`; matriz con Jasper en blanco). Capa 7 = no aplica (sin `/clientes/abb` en `WebLogic.191`, sin Condor Link).
- **Implicancia para la pregunta de baja (`QUESTIONS.md`):** no es prueba de la baja formal, pero es un dato duro de apoyo a la nota informal de la matriz ("ABB y GIAR de baja, se mantienen las bases"): ABB no tiene una sola sesión ni un solo DML desde el 1‑jul, con la infra igual encendida. Sigue siendo decisión de negocio del equipo saliente.
- **Bonos (co‑inquilinos que nunca habían tenido sesión propia):**
  - **ESYOP — capa 6 verificada en vivo.** Corre en el mismo box `192.1.1.31` (`ora_pmon_esyop`). DB `ESYOP`, `READ WRITE`, charset `WE8ISO8859P1` (= matriz), schema de app `CONDOR` con último DML **04/09/2026** → en uso activo (contraste total con ABB). Sin sesión desde `WebLogic.19` al mirar (sábado); la recencia del DML cubre la capa. Antes estaba resuelto solo por IP (`match: false`).
  - **DCVIAJES — capa 6 verificada parcial.** `192.1.1.190` (Oracle 11.2). DB `DCVIAJES`, `READ WRITE`, `NOARCHIVELOG`, charset `WE8ISO8859P1` (= matriz), schema `CONDOR` con último DML **04/09/2026** → en uso. Sin sesión en vivo capturada, AWR vacío — confirmación parcial (DB viva y en uso, sin sesión desde `WebLogic.191` al mirar).
- **Cabo suelto nuevo (bajo):** `192.1.1.190` tiene en `/etc/oratab` varias instancias sin mapear a clientes conocidos — `GRIMALDI`/`GRIMA2015`/`GRIMATEMP`, `SECLA`/`SECLATEST`, `REXTEST`, `TOLEDO` (solo `DCVIAJES` estaba levantada). `REXTEST` podría ser el test de Rex Argentina. Candidatos a clasificar en la pasada de VMs `database` sin mapear.

**ROMAN (CSM) — trazado formal iniciado; capas 4/6/7 cerradas a nivel config, "en uso" sin confirmar (6 sep 2026).** SSH + `sudo (ALL) ALL` a `OPENWLPROD01` (reusando el acceso del 2 sep de EBY). Ver [`plan_relevamiento_alta_roman.md`](../plan_relevamiento_alta_roman.md).

- **Capa 4 (config).** ROMAN está configurado en `WLS_FORMS` del dominio `base_domain` (compartido con EBY/GIAR, **sin managed server propio**). `formsweb.cfg` con secciones reales `[csm]`/`[csmerp]`/`[csmFSAL]`/`[csmERPFSAL]` → `pageTitle=CSM PRODUCCION`, `userid=@PRODCSM`, `form=/u02/clientes/csm/cdr2/menues/{cdr2w,cdr2,finit}.fmx`; QA: `[qacsmFSAL]`/`[qacsmerpFSAL]` → `userid=@QACSM`. `csm.env` solo setea `FORMS_PATH` + `NLS_LANG=AMERICAN_AMERICA.WE8ISO8859P1`.
- **Versión WL = `12.2.1.4.0`** (mismo box/molde que EBY/GIAR) — **cierra la fila "ROMAN (CSM) / Versión de WebLogic" de la tabla de Discrepancias** (infra decía `10`, funcional `WL 11`; real es 12, cuarto caso del patrón tras GIAR/JOBS/BOCA).
- **Capa 6 (config, 0.9).** Alias `PRODCSM`/`QACSM` → `10.77.7.30:1525` (`OPENDBPROD03`), `SERVICE_NAME=PRODCSM`/`QACSM`, leídos en vivo del `tnsnames.ora` del dominio. Charset `WE8ISO8859P1` confirmado. **`romanstest` y `OPENDBDES011` (`10.77.7.151`, la VM con nota de vCenter "Roman test") quedan descartados de la ruta productiva** — no aparecen en el `formsweb.cfg` productivo. Falta `sqlplus`/`v$session` directo (credencial de DB no conseguida para `OPENDBPROD03`, misma situación que `10.77.7.15`/`.22`).
- **Capa 7 — no aplica.** `/u02/clientes/csm` y `/u02/clientes/qacsm` (2.5G c/u, `oracle:oinstall`, creados 3-mar-2026) en el filesystem **local** de `OPENWLPROD01`; el único volumen dedicado bajo `/u02/clientes/` es `ebyprod`.
- **"En uso" SIN confirmar.** 5 días de `access.log` de `WLS_FORMS` (Sep 2–6, con hábiles) muestran **solo `config=eby*` — cero `config=csm`/`qacsm`**. `netstat` de `OPENWLPROD01` (sábado, box ocioso): 0 conexiones Oracle a `10.77.7.30`. **ROMAN está configurado como producción pero puede estar dormido** — coincide con el `status: "Entorno actual / posible test"` del inventario. Pendiente: access log del NPM de `DOCKER-DEB` para `romanprod.condor.solutions` (proxy host id 93), y `v$session` en `OPENDBPROD03` cuando haya credencial.
- **Actualización (12 sep 2026) — credencial `root` probada y rechazada.** `ssh root@10.77.7.30` (`OPENDBPROD03`) falló, igual que `ssh root@10.77.7.151` (`OPENDBDES011`, "Roman test") y `ssh root@10.77.7.15` (`OPENDBPROD005`, DB de EBY) — el mismo `root` que sí funcionó en `192.1.1.32` (`CLIENTES-DB`, cierre de capa 6 de BOCA el mismo día) no sirve en el segmento `10.77.7.x`. Capa 6 de ROMAN sigue en 0.9 (config), no 1.0 (en vivo). Ver `QUESTIONS.md` — pasa a pregunta de credencial pendiente, sin reunión adicional confirmada con el equipo saliente.
- **Capa 1 CERRADA — sin tráfico real, confirmado (12/13 sep 2026).** Acceso a `DOCKER-DEB` (`192.1.1.37`) vía `soportesmart` (falló una vez por contraseña, entró en el reintento — sin causa identificada, no reproducido en otro host). NPM corre en el contenedor `ssl-app-1` (`jc21/nginx-proxy-manager`). `proxy-host-93_access.log` (`romanprod.condor.solutions`) y `proxy-host-94_access.log` (`romanqa.condor.solutions`): **0 requests `lservlet` en 5.097 requests totales** (log activo desde antes del 17-ago, escribiendo hasta hoy). El 100% del tráfico logueado es escaneo de vulnerabilidades genérico (`/wp-includes/wlwmanifest.xml`, `/.env`, `/.git/config`, `/xmlrpc.php?rsd`), todo `404`, desde IPs típicas de bots (`146.70.194.254`, `94.154.43.135`, `35.255.25.42`). **Dos fuentes independientes coinciden: cero uso real de ROMAN** — ni en el `access.log` de `WLS_FORMS` (6-sep, cero `config=csm`) ni en el del NPM de entrada (12-sep, cero `lservlet`). ROMAN está configurado como producción de punta a punta pero no tiene un solo usuario activo por esta ruta.
- **`ords-roman2.open.com.ar` (proxy host id 85, → `10.77.7.12:8040`) también revisado — mismo resultado, y el backend parece caído.** `proxy-host-85_access.log`, 322 requests totales: **305 `499`** (nginx sin respuesta del backend) + **9 `504`** (gateway timeout) — **95% son fallas de backend, no respuestas reales**. Los otros 8: 5 `200` (renovación automática de certificado Let's Encrypt vía `/.well-known/acme-challenge/`, no es tráfico de aplicación) y 3 `400`. **Cero requests de aplicación real.** Refuerza que `10.77.7.12` (blind spot sin VM en el inventario) probablemente ni está levantado, o el ORDS que debería correr ahí no responde. Tercera ruta de ROMAN confirmada sin uso real.
- **Observación de seguridad menor.** Las únicas líneas "roman" en el `access.log` de `WLS_FORMS` son un bot escaneando archivos de backup expuestos (`/romanprod.condor.solutions.zip`, `.sql`, `_db.sql`, `.tar.gz`, ídem `romanqa`; todo `404`, ráfaga de ~5 s el 4-sep desde un solo session id). Los dominios `*.condor.solutions` son enumerables y hay escaneo automatizado contra ellos.

**Rex Argentina — WL+DB confirmados en vivo por primera vez; `PRODGIAR`/`QAREX01` existen como PDB pero sin tráfico visto (6 sep 2026).** Acceso a `OPENDBPROD001` (`CDBOPEN03`, Oracle 19c Container Database) vía `su - oracle` (el `root` de este host no tiene `sqlplus` en el PATH — `/etc/oratab` da `CDBOPEN03:/u01/app/oracle/product/19.0.0/dbhome_1:N`). `v$pdbs` lista 6 PDBs: `PDB$SEED`, `QAREX01`, `HEINLEIN_PROD`, `PRODGIAR`, `HEINLEIN_TEST`, `PRODREX01` — dos nombres nuevos nunca vistos (`HEINLEIN_PROD`, `QAREX01`; Heinlein fuera de foco por ahora, no se sigue esa punta). Notablemente, **`PRODCEFAS` y `QAGIAR` — ambos esperados por el `tnsnames.ora` de `OPENWLPROD01` (2 sep 2026) — NO existen como PDB provisionada acá**: refuerza que la migración de CEFAS nunca cortó tráfico real, y revela que la QA de GIAR probablemente nunca se armó. `ALTER SESSION SET CONTAINER = <pdb>; SELECT username, machine, program, count(*) FROM v$session ...` corrido sobre las tres PDB de interés (Heinlein deliberadamente fuera de alcance por indicación del usuario):

- **`PRODREX01` — tráfico real confirmado.** Sesiones con usuarios nominales: `MAIETA` (×4), `CDELISE` (×8), `MCORREA` (×1), todas `frmweb@ol8labwl01.localdomain`, más una `JBRICEÑO` vía `java@ol8labwl01.localdomain`. Cierra de una el candidato ambiguo de Rex Argentina: es `OPENDBPROD001`/`PRODREX01` el que está en uso, no `PDBREXPROD`/`192.1.3.34` (que sigue sin VM identificada, ahora como candidato secundario, no el principal). De paso confirma `OL8LABWL01` como el motor clásico real de Rex (la ruta `serzarex.condor.solutions` del NPM, hasta ahora sin confirmar). **Rex Argentina pasa a WL+DB verificados en vivo** — primera vez que este cliente tiene algo más que candidatos por nombre/config.
- **`PRODGIAR` y `QAREX01` — solo sesiones `SYS` internas** (`oracle@opendbprod001.open (OFSD)`, `sqlplus@opendbprod001.open (TNS V1-V3)`) en el momento de la consulta, sin usuarios de aplicación. No es evidencia de que estén inactivas — es una foto de un instante — pero tampoco suma confirmación nueva más allá de que la PDB existe y está `READ WRITE`. GIAR sigue en el mismo estado que antes (candidato sin verificar en vivo); `QAREX01` es un hallazgo nuevo (segundo ambiente de Rex) en el mismo estado.
- **Nota de alcance:** `HEINLEIN_PROD` (PDB nueva, contradice el `status: "Solo infraestructura"` de Heinlein en el inventario) quedó deliberadamente sin chequear — fuera de foco por ahora a pedido explícito, no una limitación técnica. Retomar si Heinlein vuelve a ser prioridad.

**Rex Argentina — trazado punta a punta cerrado, 7/7 (14 sep 2026).** SSH a `OPENDOCKER.57` (`192.1.1.57`, hostname real `dbdocker` — no coincide con el nombre de vCenter). `docker ps -a` revela los nombres reales de los contenedores del módulo Self Service (el nombre `SelfRex` del relevamiento Docker original no existe como tal): `rex_frontend` (nginx, puerto host `80`), `rex_backend` (Jetty/Dropwizard, puerto host `4433`→contenedor `9001`), `rex_postgres` (Postgres 9.5) — los tres `Up` desde hace **2 años**, tráfico/uptime real, no solo desplegados.

- **Capa 3 cerrada a 1.0.** Los puertos reales (`80`, `4433`) coinciden exacto con las reglas NAT de `FWOPEN` ya conocidas desde el 19 ago (`SS Rex Front`→`:80`, `SS Rex Back`→`:4433`) — cierra el lazo entre la regla de firewall y el servicio real corriendo del otro lado.
- **Capa 5 cerrada a 1.0.** Contenedores identificados por nombre real y confirmados activos (2 años de uptime), no solo "servicio listado en el relevamiento Docker".
- **Capa 6 — segunda DB de Rex, un módulo separado.** `rex_postgres` es una base propia del módulo Self Service, distinta de la Oracle `PRODREX01` que ya sirve al motor clásico (confirmada el 6 sep). Mismo cliente, dos módulos con dos DBs — no una ambigüedad a resolver, un patrón nuevo (más simple que el "legado vs. nuevo" de GIAR/ROMAN: acá los dos stacks conviven a propósito, sirviendo cosas distintas).
- **Capa 7 cerrada a 1.0 — y confirmada rota.** `docker inspect rex_backend`/`rex_frontend` muestra que ambos montan el mismo bind `/data/selfrex/archivos` (destino `.../recibos` en el backend, `/opt/archivos` en el frontend) — es el storage real donde se guardan y sirven los recibos, no un mount huérfano. `df -h` en el host da **`Stale file handle`** sobre ese mismo path — el NFS del lado servidor cambió o reinició y el cliente quedó con un handle inválido. Es el mismo mount que `inventory.json` (`OPENDOCKER.57.docker_detail`) ya marcaba genéricamente como "uno de los NFS da error", ahora identificado como el de Rex específicamente. **Hallazgo operativo activo, no solo de inventario** — la app sigue arriba (nginx/Jetty responden), pero la carga/descarga de recibos probablemente falla mientras el mount esté en este estado.
- **Rex Argentina queda con las 7 capas cubiertas** — ver `plan_relevamiento_alta_rex.md`. Cierra el ítem 2 de Tier 1 de `PLAN.md`.

**`WL12C-Desarrollo` (`192.1.2.54`) accedido por primera vez — corre el Forms productivo de BOCA (pese al nombre "Desarrollo"), pero sin consola ni sudo no se lee su DB (6 sep 2026).** SSH con `soportesmart` vía ProxyJump por `DOCKER-DEB` (`ssh -J soportesmart@192.1.1.37 ... soportesmart@192.1.2.54` — el puerto 22 de `.2.54` no responde directo desde la estación de TeamViewer). Contexto: se eligió BOCA como próximo trazado (`plan_relevamiento_alta_boca.md`) justamente para forzar la entrada a este box, el último WebLogic multi-inquilino sin acceder. Hallazgos:

- **Versión `12.2.1.4.0`** (pantalla de login, sin credencial) — confirma `wl_version: 12.2` de BOCA/ABB; tercer caso del patrón "inventario técnico decía 11, real es 12" (después de GIAR y JOBS).
- **Dominio `base_domain`, Oracle Forms & Reports 12.2.1** (`/u01/app/oracle/product/12.2.1/user_projects/domains/base_domain`) — mismo molde que `WL12C-PROD` (JOBS) y `OPENWLPROD01`. Managed servers: `AdminServer`, `WLS_FORMS`, `WLS_REPORTS`, y 7 ORDS: `Ords-Bocat`, `ORDS-CEFAST`, `ORDS-EBY`, `ORDS-JOBS`, `ORDS-T2022DEV01`, `Server-Ords`, `Server-ordsERP19`. El nombre "Desarrollo" de la VM **engaña** — sirve Forms productivo real (ver abajo). Mismo caso que `WL12C-PROD`, que tampoco es "solo prod JavaEE".
- **`cabj.condorwork.com.ar` → `WLS_FORMS` (`:9001`) es producción viva de BOCA.** Access log de `DOCKER-DEB` (`proxy_host-18.log`): **3.84M `POST /forms/lservlet` con `200`**, últimos **hoy**, desde IPs residenciales argentinas (`190.136.163.251`, `190.188.62.19`, `181.92.70.163`), UA `Java/1.8.0_421`. Volumen bajo de fin de semana (~1.5k req en sáb+dom) pero real. Los alias `boca.condorwork.com.ar` (id 19) e id 17 están muertos desde 2022 — solo `cabj` (id 18) está vivo.
- **DB local `fmwdb`** (`ora_pmon_fmwdb`, `/etc/oratab`) — repositorio RCU del propio dominio FMW, no una base de cliente. Explica el tráfico pesado a `127.0.0.1:1521`.
- **`netstat -tn` (sin sudo — `soportesmart` solo en su grupo, sin `oinstall`/`dba`/sudoers, sin PID map):** destinos Oracle remotos — `192.1.1.44` (`Database .44 - Clientes TEST`, dominante), `192.1.2.240` (`OPENDB19DEV01`, Oracle 19c dev — **IP nueva, identificada en esta sesión**), `192.1.1.25` (`Database.25` = CONDORERP/T2022, ERP interno), `192.1.1.32:1525` (`CLIENTES-DB` prod — pocas conexiones, varias CLOSE_WAIT), `192.1.1.22` (`OPENDBPROD006` — solo CLOSE_WAIT/SYN_SENT, algo intenta y no engancha). El box mezcla ORDS de test con Forms de prod, así que el `netstat` no aísla a qué DB pega `WLS_FORMS` (BOCA) — para eso hace falta el PID map (sin sudo) o mirar `v$session` desde `CLIENTES-DB`.
- **Muro de permisos, igual que `WebLogic.191`:** sin sudo, sin credencial de consola WebLogic, árbol `oracle:oinstall` sin lectura para otros → no se pudo leer `tnsnames.ora` ni datasources. El rédito planeado "una pantalla de consola cierra ABB+BOCA+EBY en un dato" **no aplica** — no hay consola accesible en este box; ABB hay que cerrarlo por otra vía.
- **Capa 5 de BOCA — sin capa Docker propia (6 sep 2026).** `docker ps -a` en `OPENDOCKER01` (`192.1.1.110`): no hay contenedor `cabj`/`boca`, y nada escucha en `:8090` — el proxy host `cabjjasper.condorwork.com.ar` → `192.1.1.110:8090` es ruta muerta. Mismo patrón que JOBS (motor clásico + reportería, sin frontend/backend containerizado). Reportería real de BOCA probablemente `WLS_REPORTS` en el propio `192.1.2.54` + Discoverer, sin verificar (no crítico).
- **Pendiente de BOCA:** capa 6 — `sqlplus` a `CLIENTES-DB` (`192.1.1.32`, ya accesible desde el trazado CEFAS; `ora_pmon_BOCA` visto ahí el 25 ago) con `ORACLE_SID=BOCA` → `SELECT name FROM v$database` cierra `BOCA` vs `BOCAPDB`, `v$session WHERE machine='192.1.2.54'` confirma el vínculo. Toca prod → correr lunes a sábado. **Capa 7 cerrada (6 sep 2026): no aplica** — `ls /clientes/` en `WebLogic.191` solo tiene `cefas` y `lost+found`, no hay `/clientes/boca` (mención previa a esa carpeta era error). Capa 5 también cerrada (sin capa Docker — ver arriba).
- **Capa 3 / exposición:** `FWOPEN` tiene una regla `test` → WAN TCP `9998` → `192.1.2.54:9001`, `source: *` — expone el puerto Forms de este box (que sirve BOCA en prod) a Internet sin pasar por NPM. Mismo tenor que `vcenter`/`firewall` en WAN (ver más abajo).

**`tnsnames.ora` real de `OPENWLPROD01` — cierra EBY capa 4/6 y destraba GIAR, ROMAN y (por primera vez) Rex Argentina (2 sep 2026).** Con credencial nueva (root, conseguida para el equipo saliente — ver `QUESTIONS.md`) se pudo entrar a `192.1.1.90` y confirmar `sqlplus`; y con `sudo` real en `OPENWLPROD01` se leyeron `formsweb.cfg` y `tnsnames.ora` completos. Un solo archivo TNS trae alias de al menos 6 clientes:

- **EBY — capa 6 cerrada para la ruta `eby-prod`.** Alias `EBYPROD`/`EBYQA`/`EBYAUDIT`, los tres `HOST=10.77.7.15` (`OPENDBPROD005`) `PORT=1521`, `SERVICE_NAME=EBYPROD`/`EBYQA`/`EBYAUDIT` — coincide letra por letra con `sid_nuevo` de la matriz. Es configuración oficial del dominio (no solo tráfico de red como se tenía hasta ahora) — cierra la disputa sin necesitar `sqlplus` directo a esa VM (que sigue bloqueado por credencial). `formsweb.cfg` confirma además los tres ambientes reales: `[ebyprod]`→`/u02/clientes/ebyprod/cdr2/menues/cdr2w.fmx`, `[ebyqa]`, `[ebyaudit]` (mismo patrón, sin `userid` preconfigurado — el login lo tipea el usuario, coincide con la sesión real de `AGARCIA` vista en `Database .90`). **EBY queda en 6 de 7 capas cerradas** — solo falta a qué DB corresponde la ruta paralela `yacyreta`/`WebLogic.191` (ver hallazgo de `Database .90` más abajo), no bloqueante.
- **`192.1.1.90` = `CDRADM`, confirmado por `sqlplus` — DB compartida, no exclusiva de EBY.** Oracle 11g 11.2.0.4.0, charset `WE8ISO8859P1`. Sesiones activas al momento de la consulta: `AGARCIA` vía `frmweb@NAI` (`NAI` es el hostname real de `WebLogic.191` — confirma que el `netstat` de la sesión anterior no era ruido, hay Forms real llegando desde ahí), `SIGOWEB` vía JDBC Thin desde un contenedor Docker, y una conexión de BI/Power Query (`Microsoft.Mashup.Container`). Ninguna evidencia ata esas sesiones específicamente a EBY — más probable que `CDRADM` sea una base administrativa compartida entre varios de los 8 tenants de `WebLogic.191`. **Actualización (12 sep 2026): resuelto — ver entrada "EBY, capa 6 cerrada del todo" más abajo.**
- **GIAR — segunda DB candidata.** Alias `PRODGIAR`/`QAGIAR` → `10.77.7.11` (`OPENDBPROD001`, el mismo host consolidado que ya sirve a Heinlein y al destino de CEFAS) — junto al candidato original `DB-GIAR`/`10.10.1.9`. Dos candidatos, mismo patrón legado-vs-consolidado que EBY/CEFAS, sin verificar en vivo.
- **ROMAN — primera DB real encontrada.** Alias `PRODCSM`/`QACSM` (CSM = nombre oficial de ROMAN, "CSM Ciencia al Servicio del Movimiento") → `OPENDBPROD03` (`10.77.7.30`, puerto **1525** no 1521 — mismo patrón de puerto no estándar que `CLIENTES-DB`), VM con uptime bajo (61 días). Coincide con el WL nuevo (`OPENWLPROD01`) hallado el 1 sep — candidato fuerte a stack completo migrado, sin verificar en vivo todavía.
- **Rex Argentina — primera DB jamás mapeada (Tier 1 #2 de `PLAN.md`, avanza de cero).** Dos alias distintos: `PROD_REX`→`10.77.7.11`/`OPENDBPROD001` (`SERVICE_NAME=PRODREX01`) y `PDBREXPROD`→`192.1.3.34` (`SERVICE_NAME=pdbrexprod`, **VM sin ningún rastro en `ExportList.csv`** — nuevo blind spot, segmento aislado `192.1.3.x`). Sin resolver cuál está realmente en uso. Sumado a esto, `serzarex.condor.solutions` (NPM, 1 sep) → `OL8LABWL01` (WL de Heinlein) es candidato sin confirmar a ser el motor clásico de Rex.
- **Heinlein — confirmado el ambiente de test.** Alias `HEINLEIN_TEST` → `OPENDBPROD001`, coincide con el dominio `heinleintest.condor.solutions` ya visto en el NPM.
- **`VINST`/`VINST2025`/`T2022` — no son clientes, son sistemas internos.** `T2022` → `Database.25.`, cuya nota de vCenter dice literalmente "BD 19: CONDORERP(T2022)" — un ERP interno de Condor, no un cliente. `VINST` → `192.1.1.24` (`Database-24`/`opendb`, ya conocido). `VINST2025` → `10.77.7.151` (`OPENDBDES011`, notado en vCenter como "Roman test" — posible superposición de nombres, sin aclarar). Confirma la sospecha de "Todavía abierto" #3: son nombres de esquema/sistema legado, no clientes nuevos.

`OPENDBPROD001` (`10.77.7.11`) queda confirmado como un segundo host de DB fuertemente consolidado (después de `CLIENTES-DB`/`CLIENTES-DB2`): sirve a Heinlein, CEFAS (destino), GIAR y Rex Argentina (estos dos últimos sin verificar en vivo) — los cuatro a través del mismo `tnsnames.ora` de `OPENWLPROD01`.

**EBY — `OPENWLPROD01` es un segundo stack completo (WL+DB), no un destino de migración a medio confirmar (2 sep 2026, en curso).** Acceso SSH real a `10.77.7.201` (`OPENWLPROD01`) con `soportesmart`, capa 4/6 de `plan_relevamiento_alta_eby.md`. A diferencia de `WebLogic.191` (sin sudo), acá `soportesmart` tiene **`sudo (ALL) ALL`** — permite lo que en `.191` quedó bloqueado por permisos. Hallazgos:

- **Dominio identificado: `base_domain`, Oracle Forms & Reports 12.2.1**, en `/u01/app/oracle/product/12.2.1/user_projects/domains/base_domain` — mismo estilo de instalación que `WL12C-PROD` (JOBS), distinto del `ClassicDomain`/11g de `WebLogic.191`. Managed servers `AdminServer`, `WLS_FORMS`, `WLS_REPORTS` (sin el sufijo numérico `1` que sí tiene JOBS).
- **`netstat -tn | grep -E ':1521|:1525'` da una lectura limpia y sin ambigüedad: el 100% de las conexiones Oracle reales (3/3) van a `10.77.7.15` (`OPENDBPROD005`)** — cero a `192.1.1.22` (`OPENDBPROD006`, la DB "reclamada") ni a `192.1.1.90` (`Database .90`, la dominante en `WebLogic.191`). Cada motor de EBY parece conectar a una DB distinta.
- **Tres archivos de ambiente dedicados a EBY**, encontrados con `sudo grep -ril eby/yacyret` sobre todo el árbol de config del dominio: `ebyprod.env`, `ebyqa.env`, `ebyaudit.env`, bajo `.../formsapp_12.2.1/config/`. Confirma un deployment real y deliberado (prod/qa/audit) en este host — no una mención de paso. También aparecen `formsweb.cfg` (+ `.ori`) y `tnsnames.ora` con referencias a eby/yacyret — lectura del `ORACLE_SID`/`TWO_TASK` de los `.env` en curso.
- **Hipótesis reforzada: EBY corre en dos stacks completos y paralelos, no un motor con DB ambigua.** `yacyreta.condorwork.com.ar` → `WebLogic.191` → `Database .90` (sin cliente asignado en el inventario, candidata sin confirmar) vs. `eby-prod.condorwork.com.ar` → `OPENWLPROD01` → `OPENDBPROD005` (`10.77.7.15`, nota vCenter literal `"ebyprod"`, `sid_nuevo` de la matriz). Coincide con la propia observación de la matriz: *"BD y WL migrados; falta actualizar la base mediante import..."* — la migración a este segundo stack parece ya hecha y con tráfico real (1.45M requests, conexiones DB activas), no "pendiente" como sugería el `status` general de EBY en el inventario.
- **Capa 6 sigue sin `sqlplus` directo** — `soportesmart` fue rechazada por SSH en los tres hosts de DB probados (`192.1.1.90`, `192.1.1.22`, `10.77.7.15`), aunque la misma cuenta sí entra a los hosts de app/middleware (contraste anotado en `QUESTIONS.md`, pendiente del equipo saliente). La evidencia de red (netstat) y los archivos `.env` son la mejor confirmación disponible sin esa credencial.

**Re-dump completo del NPM de `DOCKER-DEB` — 101 proxy hosts (1 sep 2026).** Consulta directa a `proxy_host` / `redirection_host` de la MariaDB interna (`ssl-db-1`) + configs vivas de nginx (`ssl-app-1`), vía la sesión SSH a `DOCKER-DEB`. Archivos crudos en `DOCKER-DEB-NginxProxyManager/dockerdeb_*_2026-09-01.*`. La transcripción vieja (`proxy_hosts.csv`, 79 filas) quedó corta — hay 22 entradas nuevas, casi todas de 2025-08 en adelante. Hallazgos:

- **EBY — 10 proxy hosts, capa 1 cerrable.** Dos rutas Forms productivas confirmadas por access log (`yacyreta.condorwork.com.ar` → `192.1.1.191:80` / `WebLogic.191`; `eby-prod.condorwork.com.ar` → `10.77.7.201:9001` / `OPENWLPROD01`, ambas con ~1.5M req 200 el mismo día). ORDS: `ords-eby.open.com.ar` → `192.1.2.54:7010`, y dos nuevos (`ords-yacy` → `10.77.7.15:8080`, `ords-ebyqa` → `10.77.7.15:8040`). Detalle en `plan_relevamiento_alta_eby.md` § *Capa 1 — rutas de entrada*.
- **`10.77.7.15` = `OPENDBPROD005`** — VM **encendida, ya en el inventario**, nota literal `"ebyprod"`. Es la DB destino de migración de EBY (`EBYPROD` de la matriz), con dos ORDS apuntándole. Cierra el "esa IP no resuelve a ninguna VM" de `plan_relevamiento_alta_eby.md` capa 6.
- **`10.77.8.201` = `OPENWLCLI01`** (el clon "dejar apagada" de `OPENWLPROD01`, `state: Apagado`). Por eso `ebyprod.open.com.ar` → `10.77.8.201` está muerto (8.6k×502): apunta a una VM deliberadamente apagada. Los dos blind spots de la ronda anterior quedan cerrados.
- **ROMAN migró al WL compartido `OPENWLPROD01`.** `romanprod.condor.solutions` y `romanqa.condor.solutions` → `10.77.7.201:9001`, ambos **habilitados**; el viejo `roman.condorwork.com.ar` → `172.18.5.40` sigue deshabilitado (NPM de `VM-DOCKER-Clientes`). `OPENWLPROD01` pasa a servir **GIAR + EBY + ROMAN** — segundo box más compartido después de `WebLogic.191`. Falta confirmar en vivo, pero el patrón habilitado/deshabilitado es inequívoco. También `ords-roman2.open.com.ar` → `10.77.7.12:8040` (`.12` sin VM conocida).
- **Rebrand a `condor.solutions`.** `open.com.ar`, `www.open.com.ar`, `condorsolutions.com.py` → `301` a `condor.solutions`. Los entrypoints de cliente de generación **actual** son `<cliente>.condor.solutions` (`romanprod`, `romanqa`, `heinleintest`, `serzarex`, `qadmportal`, `dmportal`); los `*.condorwork.com.ar` / `*.open.com.ar` son la generación anterior. Explica por qué varios dominios "conocidos" se veían viejos.
- **`serzarex.condor.solutions` → `192.1.2.195:9001` (`OL8LABWL01`, el WL de Heinlein), habilitado.** Nombre de cliente/app sin identificar — posible **Rex Argentina** (Rex ya tiene Self Service en `192.1.1.57`; esto sería un motor clásico en el box de Heinlein), o algo aparte. `serzarex-test` deshabilitado. `heinleintest.condor.solutions` va al mismo box. Sumar `serzarex` a los nombres a trackear.
- **MAIPU:** `portalproveedores.diagnosticomaipu.com.ar` → `301` a `dmportal.condor.solutions` (no está en este NPM — buscar en `OPENDOCKER04`). `qadmportal.condor.solutions` → `10.77.10.201:8040` (`DASADBPROD01`, la DB nueva de Maipú), ORDS deshabilitado.
- **GIAR:** `giarprod.condorenterprise.com.ar` → `200.55.243.117:80` (IP pública, no interna), habilitado — otra señal de que GIAR no está tan "de baja".
- **`10.77.x` no es una red oculta:** son las IPs "prod/migración" de VMs ya inventariadas, dual-homed con `192.1.x` — `OPENWLPROD01`=`10.77.7.201`, `OPENWLCLI01`=`10.77.8.201`, `OPENDBPROD005`=`10.77.7.15`, `OPENWLDES010`=`10.77.7.51`, `OPENDBPROD001`=`10.77.7.11`, `DASADBPROD01`=`10.77.10.201`. Siguen sin VM: `10.77.7.13` (destino JOBS de la matriz) y `10.77.7.12` (`ords-roman2`).
- **Pendiente:** los NPM de `OPENDOCKER04` (`10.77.7.5:81`) y `VM-DOCKER-Clientes (1)` (`192.1.3.4:81`) — ambos responden desde `DOCKER-DEB`, sin transcribir. Reconciliar `proxy_hosts.csv` (79) a las 101 y cargar en `inventory.json`.

**46 reglas NAT de `FWOPEN` transcriptas (19 ago 2026).** `FWOPEN-nat-rules/nat_rules.csv`, cargado en `inventory.json` → `vms[FWOPEN].nat_rules`. Confirma por qué no hay una regla NAT específica para cada backend interno (`192.1.1.1:9001` de JOBS, por ejemplo): pfSense solo expone los dos NPM (`192.1.1.37` con IP pública dedicada `200.55.243.94`, y `192.1.1.38` — el de los contenedores CEFAS — con la WAN1 genérica, ambos en 80/443); el resto del ruteo por dominio lo hace NPM puertas adentro, en la misma LAN, sin que pfSense tenga que intervenir. Además:

- **`WebLogic.191` confirmado como el host más compartido del relevamiento.** Entre NAT directo y dominios de NPM, sirve a **DVAL** (ya conocido), **UIA**, **Mafisa**, **SIGO**, **EBY/Yacyretá**, **DCViajes**, **Tassaroli** — y, según se confirmó después vía el NPM de `VM-DOCKER-Clientes` (ver más abajo), también **CEFAS** (`cefas.condorwork.com.ar`). Ocho clientes distintos en la misma VM.
- **"Weblogic TARANTO" y "esyop acceso" son la misma regla exacta** (mismo puerto destino `8906`, mismo NAT `192.1.1.19:8888` — que es literalmente `WebLogic.19`, el WL ya resuelto de ESYOP). "Taranto" es probablemente un alias/nombre interno viejo de ESYOP, no un cliente nuevo.
- **`SS Rex Front`/`SS Rex Back` → `192.1.1.57`** (`OPENDOCKER.57`, host Docker ya conocido) — candidato fuerte para la infraestructura de **Rex Argentina**, que hasta ahora tenía cero recursos mapeados (Tier 1, ítem 2 de `PLAN.md`).
- **Acceso directo a Oracle sin pasar por NPM, para varios clientes:** `mitiga Cefas DB 1525` (WAN1:`11517` → `192.1.1.32:1525`, la DB actual de CEFAS), `acceso jobs` x2 (→ `192.1.1.44:1521` y `192.1.1.51:1521`, dev y prod de JOBS), `acceso YACYRETA` (→ `192.1.1.22:1521`), `UIA DB 1521` (→ `192.1.1.17:1521`, restringido a un alias `ip_uia`). Para CEFAS en particular, esto abre una vía a la capa 5 de `plan_relevamiento_alta_cefas.md` sin necesitar SSH — solo credencial Oracle.
- **`vcenter` expuesto por WAN** (`667` → `192.1.1.29:443`, coincide exacto con la VM `vCenter` del inventario) y **Exchange también** (`80`/`443`/`25`/`587`/`993` → `192.1.1.10`, IP que no matchea ninguna VM del export — posible appliance físico o VM apagada sin IP reportada). Contradice, al menos en apariencia, el hallazgo anterior de "solo OpenVPN expuesto" del relevamiento manual de pfSense — sin confirmar todavía si esas reglas están habilitadas o si ese hallazgo anterior hablaba de otro firewall. Ver "Todavía abierto" más abajo.
- **Nueve IPs de destino NAT sin VM conocida:** `192.1.1.10` (Exchange), `.12` (NAT Remote App), `.133` (Firma Digital), `.17` (Cliente UIPBA/UIA DB), `.2` (SIGOWEB Backend), `.26` (TETST), `.39` (REMOTO_SALUD), `.70` (SELFSERVICE), `.97` (Jira) — ninguna aparece en las 129 VMs de `ExportList.csv`. Mismo patrón que otros blind spots del proyecto: pueden ser VMs apagadas sin IP reportada, o equipos fuera de vSphere.
- **Nombres de cliente nuevos, sin documentar:** `Mafisa` (`Mafisa Publicacion`) y `UIPBA` (`Cliente UIPBA`) — se suman a `COBRA`, `SIGO`, `Pietroboni`, `UIA` y `Tassaroli` como candidatos a clientes sin registrar en los 15 conocidos.

**Mapeo completo dominio → NPM → servidor interno de `DOCKER-DEB` (19 ago 2026).** 79 proxy hosts reales, transcriptos de 8 capturas de pantalla del panel de `DOCKER-DEB` (`192.1.1.37:81`) — ver `DOCKER-DEB-NginxProxyManager/proxy_hosts.csv` y `inventory.json` → `vms[DOCKER-DEB].docker_detail.proxy_hosts`. Varios hallazgos de fondo, no solo el mapeo en sí:

- **`WebLogic.191` (`192.1.1.191`) resulta compartido — y CEFAS no aparece ahí.** Puerto `80` sirve a `sigo.open.com.ar`, `uia.condorenterprise.com.ar`, `yacyreta.condorwork.com.ar` (**EBY**) y `dcviajes.opensolutions.com.ar`/`tassaroli.condorwork.com.ar` (puerto `8888`) — cinco dominios de clientes distintos en la misma VM, ninguno con "cefas" en el nombre. Esto es coherente con el `connection refused` que ya habíamos visto en el puerto `7001` (`plan_relevamiento_alta_cefas.md`): la VM está viva y sirve a otros clientes, pero puede que el deployment de CEFAS específicamente ya se haya migrado fuera de ahí. El dominio principal de CEFAS sigue sin aparecer — no está en `DOCKER-DEB`, habría que buscarlo en el NPM de `VM-DOCKER-Clientes` (`192.1.1.38:81`), que es donde viven los contenedores "CEFAS frontend/backend".
- **EBY tiene tres endpoints reales, no dos.** `eby-prod.condorwork.com.ar` y `eby-qa.condorwork.com.ar` → `10.77.7.201:9001` (`OPENWLPROD01`); `ebyprod.open.com.ar` → `10.77.8.201:9001` (la IP que habíamos marcado sospechosa de nota contaminada — resulta ser real después de todo); `yacyreta.condorwork.com.ar` → `192.1.1.191:80` (`WebLogic.191`). Ninguno de los dos candidatos originales de la hoja Discrepancias (`10.77.7.201` vs `192.1.2.54`) resulta ser el único real — hace falta revisar cuál de los tres está efectivamente en uso antes de cerrar el ítem 3 de Tier 1 en `PLAN.md`.
- **Tres nombres nuevos, confirmados como dominios reales de clientes:** `sigo.open.com.ar`, `uia.condorenterprise.com.ar` ("UIA", posible Unión Industrial Argentina), `pietroboni.condorwork.com.ar` y `tassaroli.condorwork.com.ar`. `SIGO` y `Pietroboni` ya estaban anotados como señal débil (nombre de esquema Oracle) en "Todavía abierto" #3 — esto los sube a señal fuerte, son dominios públicos reales, no solo nombres de tablespace.
- **Confirma en vivo el WebLogic compartido `WL12C-Desarrollo.2.54`:** `abb.condorwork.com.ar`, `cabj.condorwork.com.ar` (Boca) y `condor.open.com.ar` van los tres a `192.1.2.54:9001`.
- **`cloud.opensolutions.com.ar` → `192.1.1.33:80`** — resuelve por qué Nextcloud en `NUBE-OPEN-DOCKER` rechazaba el acceso por IP directa.
- **Dos IPs públicas fuera del rango interno:** `condor.com.ar` → `34.198.2.32` y `condorsolutions.com.py`/`www.condorsolutions.com.py` → `34.202.29.65` (ambas con pinta de AWS), y `proviadev01.condor.com.ar` → `172.171.154.190` (rango de Azure) — coincide con Condor ProvIA, ya documentado en `cloud-infra/`.
- **Rango nuevo sin identificar: `192.168.222.x`** (`ords-balanz.open.com.ar` → `.20`, `ords-balanztest.open.com.ar` → `.19`) — no aparece en ningún otro documento. Podría ser otro sitio o red aislada, como pasó con Piedras.
- **Una IP nueva dentro de la subred de Piedras:** `it.open.com.ar` → `192.168.100.8` (deshabilitado) — no estaba en las 15 VMs de `ExportList-Piedras-Full.csv`, sugiere que ese export no es exhaustivo de todo lo que responde en esa subred.

**Dominio principal de CEFAS encontrado — y las dos tecnologías conviven de verdad (19 ago 2026).** Acceso SSH directo a `VM-DOCKER-Clientes` (`192.1.1.38`) y consulta a la tabla `proxy_host` de su NPM interno (9 filas, ver `inventory.json` → `vms[VM-DOCKER-Clientes].docker_detail.proxy_hosts`) — corrige y completa el hallazgo anterior de `DOCKER-DEB`:

- **`cefas.condorwork.com.ar` → `192.1.1.191:80` (`WebLogic.191`), habilitado.** Contradice lo que se había concluido con los datos de `DOCKER-DEB` sola ("CEFAS no aparece en `WebLogic.191`") — sí aparece, solo que en el NPM de otra VM. `WebLogic.191` sigue siendo compartido con otros clientes (ver hallazgo de `DOCKER-DEB` arriba), pero CEFAS es uno de ellos después de todo.
- **`cefas.condorlink.com.ar` → `192.1.1.38:8881` (`ss_front_cefas`) y `cefasbk.condorlink.com.ar` → `192.1.1.38:9001` (`ss_back_cefas`), ambos habilitados.** Es la app "Self Service" (`matrix_detail.productos.Self Service: "Sí"` para CEFAS) — confirma la hipótesis de `plan_relevamiento_alta_cefas.md`: la capa Docker no es un reemplazo del motor clásico, es un **producto aparte que convive con él** (acá, específicamente Self Service, no "Work" en general).
- **Nombres reales de los 5 contenedores** (antes solo "CEFAS frontend/backend" genérico): `ss_front_cefas` (imagen `self-service-angular:cefas-sfd`), `ss_back_cefas` (`self-service-backend:cefas-sfd`), `ss_pg_cefas` (`self-service-database:yacyreta-sfd` — **el tag de imagen dice "yacyreta", el nombre del contenedor dice "cefas"**, posible copia del compose de EBY/Yacyretá reusada para CEFAS sin actualizar el tag — mismo patrón de contaminación por copy-paste que ya se había visto en la columna `Notas` de `ExportList.csv`), más `nginx_app_1`/`nginx_db_1` (el propio NPM y su MariaDB interna).
- **Argocean — WebLogic identificado por primera vez.** `argocean.condorenterprise.com.ar` → `172.18.5.40` (`WL-CLIENTES`, el mismo WL compartido de ROMAN) — deshabilitado (`enabled=0`), sin confirmar si sigue en uso. Cierra parcialmente el vacío que quedaba abierto desde que se descubrió `DB-ARGOCEAN` (ver más abajo): ya tiene DB y WL candidatos, aunque el WL aparece apagado en la config.
- **`roman.condorwork.com.ar`/`roman.condorenterprise.com.ar` → `172.18.5.40`, también deshabilitados** — mismo patrón que Argocean, a confirmar si es config vieja o si ROMAN realmente entra por otra ruta hoy.

**CEFAS — Self Service confirmado autónomo del motor clásico (25 ago 2026).** Acceso SSH+root real a `VM-DOCKER-Clientes`, `config.yml` de `ss_back_cefas` (`/usr/src/backend/config.yml`, patrón Dropwizard) leído directo del contenedor vía `docker exec`. El único datasource configurado es `jdbc:postgresql://postgres:5432/selfservice` — el `ss_pg_cefas` interno. Cierra capa 4b de `plan_relevamiento_alta_cefas.md`: las dos tecnologías conviven (ver hallazgo de arriba) pero **no comparten datos** — Self Service no toca el Oracle del motor clásico. El `docker-compose.yml`/`config.yml` completo tiene contraseñas en texto plano (Postgres y SMTP) — no versionar ese archivo tal cual, sacarlas primero si hace falta guardarlo en el repo.

**`WebLogic.191` es en realidad Oracle Forms & Reports 11g, no WebLogic JavaEE genérico (25 ago 2026).** Acceso SSH real (requiere forzar `-oHostKeyAlgorithms=+ssh-rsa`, el host solo ofrece host keys viejos que los clientes OpenSSH modernos rechazan por default). `ps aux` muestra el dominio `ClassicDomain` (`/app/oracle/mid/user_projects/domains/ClassicDomain`) con managed servers `WLS_FORMS`/`WLS_REPORTS` — Oracle Forms & Reports Middle Tier, no una app JavaEE con datasources de WebLogic. El banner de login del host lista "CLIENTES: OPEN, UIA, DCVIAJES" — desactualizado, no menciona a ninguno de los ~8 clientes reales que sirve (mismo patrón de documentación que no sigue el ritmo de la realidad ya visto en la columna `Notas` de `ExportList.csv`). Sin `sudo` funcional en este host no se pudo leer `tnsnames.ora`/`formsweb.cfg`, pero **`netstat -tn` sin privilegios especiales mostró las conexiones Oracle activas en vivo**: 2 `ESTABLISHED` a `192.1.1.32:1525` (`CLIENTES-DB`, la DB actual de CEFAS) y cero tráfico hacia `10.77.7.11` (`OPENDBPROD001`, destino de migración) — confirma que la migración de CEFAS todavía no cortó tráfico productivo. Cierra capa 4a/5 de `plan_relevamiento_alta_cefas.md`. Otras conexiones activas del mismo host, sin identificar todavía: `192.1.1.90` (la más frecuente), `192.1.1.238`.

**CEFAS — servidor NFS identificado, y es el mismo `WebLogic.191` (25 ago 2026).** `mount`/`/etc/fstab` en `VM-DOCKER-Clientes` (`192.1.1.38`) confirma un mount NFSv4: `192.1.1.191:/clientes/cefas/cdr2/condorlink` → `/home/open/cefaslink/archivos` — justo el `uploadPath` que usa `ss_back_cefas` (ver hallazgo de 4b, arriba). Cierra el blind spot de storage abierto desde el primer relevamiento de Docker: no hay un servidor NFS separado, `WebLogic.191` cumple doble función (Forms & Reports + storage NFS para Self Service). Coincide con el volumen `/dev/mapper/cdr2-cdr2_data1` ya visto en el banner de uso de disco de esa VM.

**CEFAS — SID confirmado, disputa cerrada (25 ago 2026).** Acceso logrado a `CLIENTES-DB` (`192.1.1.32`) — el bloqueo de credencial que había quedado abierto se resolvió. `ps -ef | grep pmon` reveló tres instancias Oracle corriendo en ese mismo host: `ora_pmon_BOCA`, `ora_pmon_CEFAS`, `ora_pmon_wl12prod`. Con `ORACLE_SID=CEFAS`, `sqlplus / as sysdba` → `SELECT name FROM v$database;` devolvió **`CEFAS`** — cierra la disputa `CEFAS` vs `CEFASPDB` (la matriz decía `CEFASPDB`, el nombre real es `CEFAS`). Cierra capa 5 de `plan_relevamiento_alta_cefas.md` — CEFAS queda con las 6 capas del trazado completas. **Bonus para JOBS:** `ora_pmon_wl12prod` en el mismo host es candidato fuerte a la DB de `WL12C-PROD` (capa 4/6 de `plan_relevamiento_alta_jobs.md`, todavía abierta) — mismo servidor que `CLIENTES-DB`, no una VM aparte.

**CEFAS — destino de migración de la DB identificado (18 ago 2026).** `matrix_detail.ip_db_destino` de CEFAS (`10.77.7.11`) coincide exacto con la VM `OPENDBPROD001`, ya presente en el inventario como base de datos de **Heinlein**. Sería una instancia Oracle compartida entre dos clientes, mismo patrón que otras DBs multi-cliente del proyecto (ej. `DBClientes.190` para ABB/DCVIAJES). Falta confirmar el SID (`prodcefas` según la matriz) y si ya es la productiva — ver `plan_relevamiento_alta_cefas.md` capa 5.

**Credencial de pfSense — confirmada, no bloqueada (18 ago 2026).** Acceso real logrado al dashboard de `FWOPEN` (`192.1.1.11`) durante el trazado punta a punta de CEFAS. A diferencia de SSH/Portainer/NPM (siguen sin credencial), pfSense ya no es un bloqueador — ver `plan_relevamiento_alta_cefas.md`.

**`WebLogic.191` — connection refused en el puerto de consola (18 ago 2026).** A diferencia de `OPENWLPROD01`/GIAR (mismo truco de mirar la pantalla de login sin credencial, que sí respondió), `http://192.1.1.191:7001/console` no respondió nada — ni la pantalla de login. Sin diagnóstico todavía: puede ser el firewall (falta revisar la regla NAT en `FWOPEN`, ahora accesible), el proceso de WebLogic caído, o que se haya movido de IP durante la migración que la matriz menciona ("BD y WL migrados", sin decir a qué IP). No asumir que el servidor está muerto sin revisar la regla NAT primero.

**JOBS — versión de WebLogic confirmada, y `WL12C-PROD` es Forms & Reports, no JavaEE puro (1 sep 2026).** Acceso SSH real a `WL12C-PROD` (`192.1.1.1`). `http://192.1.1.1:7001/console/login/LoginForm.jsp` muestra "Versión de WebLogic Server: **12.2.1.4.0**" — cierra la fila JOBS de la tabla de Discrepancias: la fuente funcional (WL 12) tenía razón, el inventario técnico (11) estaba desactualizado, mismo patrón que ya se vio con GIAR. `sudo ss -tlnp` mapeó 4 puertos ya conocidos por NPM más dos sin identificar (`8003`, `9002`), y procesos `frmweb`/`rwserver` — misma firma que `WebLogic.191` (CEFAS). `ps -fp <pids>` + `ps -ef | grep java | grep -o -- '-Dweblogic.Name=[^ ]*'` cerró la duda: dominio **`base_domain`** (distinto nombre del `ClassicDomain` de CEFAS, pero mismo tipo de producto — Oracle Forms & Reports 12c, confirmado también por los paths `config/fmwconfig/`, `reports_12.2.1/`, `jps-config.xml` propios de un dominio FMW/JRF). Managed servers identificados uno por uno:

| Puerto | Managed server | Rol |
|---|---|---|
| `7001` | `AdminServer` | Consola de administración |
| `9001` | `WLS_FORMS1` | Motor Forms — sirve `enerflex.condorwork.com.ar` y `jobsprod.condorwork.com.ar` |
| `9002` | `WLS_REPORTS1` | Motor Reports (antes sin identificar) |
| `8003` | `ORDS-Enerflex` | ORDS dedicado a Enerflex (antes sin identificar) |
| `8002` | `ORDS-Jobs` | ORDS dedicado a JOBS — ya conocido (`ords-jobs.open.com.ar`) |
| `8001` | `ORDS-Open` | ORDS — ya conocido (`ords-open.open.com.ar`), sin cliente específico |
| `7574` | (Node Manager) | Proceso liviano (`-Xmx200m`, `coherence.home`), sin `-Dweblogic.Name` propio |

Cierra los dos puertos que habían quedado sin mapear. `netstat -tn | grep -E ':1521|:1525'` mostró conexiones `ESTABLISHED` **simultáneas a tres DBs**: mayoría a `192.1.1.51:1521` (`CLIENTES-DB2`, lo que dice la matriz), varias a `192.1.1.32:1521`/`1525` (coincide con la nota cruda "db de WL en 1.32" y con `ora_pmon_wl12prod` ya visto ahí), y algunas a `192.1.1.24:1521`/`1525` (`Database-24`, hostname `opendb`, con un schema `OPEN:Y` activo en sus notas de vCenter — encaja con que sea el destino de `ORDS-Open`). No es "una DB u otra": el dominio entero usa las tres, aparentemente una por rol. `sudo netstat -tnp | grep -E ':1521|:1525'` (1 sep 2026) cerró la asignación exacta por PID:

| Managed server | PID | DB(s) |
|---|---|---|
| `AdminServer` | 2034 | `.51` y `.32` |
| `WLS_FORMS1` | 2331 | `.51` y `.32` |
| `WLS_REPORTS1` | 2344 | `.51` y `.32` |
| `ORDS-Jobs` | 2209 | solo `.51` |
| `ORDS-Enerflex` | 2146 | `.51`, `.32` y `.24` |
| `ORDS-Open` | 2398 | `.51` y `.24` |
| `frmweb`/`rwserver` (workers de sesión Forms/Reports) | varios | mayoría `.51`, algunos `.32`; `rwserver` → `.24` |

Confirma el patrón: **`192.1.1.51` (`CLIENTES-DB2`) es la DB de datos de negocio real** de Enerflex/JOBS — la usan todos los procesos, `ORDS-Jobs` en exclusiva. Coincide con lo que decía la matriz. **`192.1.1.32` (`CLIENTES-DB`) es el schema propio del dominio** (`AdminServer` + los dos motores clásicos) — coincide con la nota cruda "db de WL en 1.32" y con `ora_pmon_wl12prod`. **`192.1.1.24` (schema `OPEN`)** la tocan `ORDS-Open` y `ORDS-Enerflex` — consistente con ser infraestructura compartida, no datos de un cliente puntual. Cierra capa 4 y 6 de `plan_relevamiento_alta_jobs.md` — sin pasos pendientes en esa VM.

**JOBS — las dos rutas de entrada están vivas a la vez, ninguna es legacy (1 sep 2026).** `jobsprod.condorwork.com.ar` (dedicado, `192.1.1.1:9001`) y `jobs.condorwork.com.ar` (compartido con ABB/Boca, `192.1.2.54:9001`) tenían sesión Forms activa **en simultáneo** al momento de revisar, cada una con un cliente/IP distinto (`jobsprod`: `181.94.246.158`; `jobs`: `181.91.85.168`) — no es dedicado-real/compartido-legacy como se sospechaba. Confirmado por los access logs de NPM en `DOCKER-DEB` (contenedor `ssl-app-1`). **Gotcha de esta VM:** usa MariaDB como backend (contenedor `ssl-db-1`, credenciales `npm`/`npm`, hay que forzar `-h127.0.0.1` porque por socket/`localhost` da `Access denied`) en vez del `sqlite` que usan otras instancias de NPM del proyecto — y el nombre de archivo `proxy_host-<N>.log` **no coincide** con el `id` de la tabla `proxy_host` (probablemente por hosts borrados/recreados en algún momento). Para ubicar el log real de un dominio hay que buscarlo por contenido, no asumir el número: `find /data/logs -name "proxy_host-*.log" -exec grep -l "<dominio>" {} \;`. Abre una pregunta nueva, sin resolver: **quiénes son los dos usuarios/clientes reales detrás de cada ruta** — no necesariamente los mismos.

Evidencia concreta (verificada, 1 sep 2026, ~17:28 UTC):

```
# jobsprod.condorwork.com.ar -> 192.1.1.1:9001 (proxy_host-51.log)
[01/Sep/2026:17:28:08 +0000] - 200 200 - POST http jobsprod.condorwork.com.ar "/forms/lservlet;jsessionid=Mj9dFwOFJbSQ8chhU0u2OG6DXXZA1RMDgvbgz2CVqtqOor7hWLbI!-1231080922" [Client 181.94.246.158] [Sent-to 192.1.1.1] "Mozilla/4.0 (Windows 11 10.0)/1.8.0_471"

# jobs.condorwork.com.ar -> 192.1.2.54:9001 (proxy_host-42.log)
[01/Sep/2026:17:24:12 +0000] - 200 200 - POST http jobs.condorwork.com.ar "/forms/lservlet;jsessionid=RQ1FynM9jQBpyjHH7VyJsg1pr_nnaUe1a1JvsdFOOYnNBBXSS8-r!2090724003" [Client 181.91.85.168] [Sent-to 192.1.2.54] "Java/1.8.0_121"
```

Dos IPs cliente distintas (`181.94.246.158` vs `181.91.85.168`), dos `jsessionid` distintos, dos backends distintos, con actividad a minutos de diferencia entre sí en la misma ventana de revisión — descarta que sea el mismo usuario probando las dos rutas.

**JOBS — capa Docker/reportes confirmada, patrón distinto de CEFAS (1 sep 2026).** `sudo docker ps` en `OPENDOCKER01` (`192.1.1.110`): JOBS solo tiene `jasper-jobs-jasperreports-1` (`:8098`, coincide con `jobsjasper.condorwork.com.ar`) más su propia `jasper-jobs-mariadb-1` — sin contenedores frontend/backend dedicados. Confirma la hipótesis de `plan_relevamiento_alta_jobs.md`: el patrón de JOBS es "motor clásico (Forms) + reportes", sin capa web/Docker propia, a diferencia de CEFAS (que sí tiene `ss_front_cefas`/`ss_back_cefas` para Self Service). El resto de los 7 contenedores del host es infraestructura genérica de "Open" (`jasper-open-release-*`, `cont_condor_new`, `cont_open_com_ar2`), sin cliente específico identificado todavía. Cierra capa 5 — JOBS queda con las 7 capas del trazado completas, igual que CEFAS.

**JOBS/Enerflex — SIDs reales confirmados vía `tnsnames.ora` (1 sep 2026).** Como Forms no usa datasources de WebLogic (ver hallazgo anterior), el SID real vive en `/u01/app/oracle/product/12.2.1/network/admin/tnsnames.ora` de `WL12C-PROD`, no en `config/jdbc/`. Cuatro entradas TNS: `JOBS` → `192.1.1.51:1521`, `SERVICE_NAME=JOBS` (coincide exacto con `matrix_detail.sid_actual` de JOBS); `ENERFLEX` → `192.1.1.51:1521`, `SERVICE_NAME=enerflex` (coincide con `matrix_detail.sid_actual="ENERFLEX"` de Enerflex, salvo mayúscula/minúscula — Oracle no distingue ahí, no es una discrepancia real); `OPEN` → `192.1.1.24:1521`, `SERVICE_NAME=OPEN` (cierra la identidad de `Database-24`/`opendb`); y **`ENERFLEX_TEST`** → mismo host `.51` — un ambiente de test de Enerflex sin documentar hasta ahora en ningún material fuente. Cierra por completo el trazado punta a punta de JOBS (las 7 capas) y deja a Enerflex con su DB también confirmada de punta a punta.

**Otra confirmación de que la columna `Notas` mezcla contenido entre VMs (18 ago 2026).** `OPENWLPROD01` tiene, entre sus notas, el mismo fragmento "CEFAS... test weblogix simple licence. 12.2.1.19 y db 23ia" que ya habíamos marcado como sospechoso en `OPENWLCLI01` (ítem 4 de "Todavía abierto", más abajo) y en `PiedrasWL01` — ahora aparece en un **tercer** VM no relacionado. Refuerza que estos tres fragmentos de nota son contaminación entre celdas, no datos propios de cada VM — no leer "CEFAS" en la nota de `OPENWLPROD01` como evidencia de que el WL de CEFAS vive ahí.

**"Piedras" — confirmado como sitio real (18 ago 2026).** Dos vías independientes el mismo día: una sesión de TeamViewer activa dentro de un host Windows en `192.168.100.165/24` (gateway `192.168.100.254`) — exactamente el subnet del dashboard pfSense "Open - Piedras" (`192.168.100.1`) que había quedado como "no responde" en el relevamiento manual de firewalls — y un export de vCenter propio del sitio, `ExportList-Piedras-Full.csv`, que confirma un tercer host ESXi (`192.168.100.4`, distinto tanto del cluster principal `192.1.1.x` como del segundo host `192.1.3.252`) con 15 VMs propias. El host Windows resultó ser una de esas 15 VMs (`Win10-Piedras`, IP y hostname `DESKTOP-RK7PGB6` coinciden exacto con el export). Detalle completo en `infra/inventory.json` → `meta.piedras_site` / `vms[].site == "Piedras"`, y tabla resumen en `infra/topology.md` §3.

Hallazgos derivados de las 15 VMs de Piedras, todas apagadas salvo `Win10-Piedras` y `DC2`:

- **`DC2` — segundo controlador de dominio.** El sitio principal ya tenía `DC1`; que Piedras tenga su propio `DC2` sugiere que es un sitio de Active Directory replicado, no un entorno aislado — dato a favor de tratarlo como parte del mismo dominio administrativo, no como infraestructura completamente separada.
- **`OpenPiedrasFw01` — candidato fuerte a ser el pfSense que "no responde".** FreeBSD, 2 NICs, nombre explícito. Está *apagada* en el export — probable explicación de por qué el dashboard `192.168.100.1` nunca respondió durante el relevamiento de firewalls. Sigue sin confirmarse por acceso directo (para eso habría que encenderla).
- **`OPENDB_31` — Piedras respalda hacia el datacenter principal, no localmente.** Su nota de backup Veeam apunta al servidor `openbk` / repositorio `openbk.open.solutions` — que es literalmente la VM `OPENBK` del sitio principal (`192.1.1.14` / `10.77.254.114`), no un Veeam local de Piedras. Confirma conectividad activa entre ambos sitios vía `10.77.254.114`.
- **Ruteo Piedras → cluster principal, confirmado en vivo (18 ago 2026).** Desde dentro de la sesión de TeamViewer en `Win10-Piedras` (`192.168.100.165`), `http://192.1.1.38:81/` (el panel de Nginx Proxy Manager de `VM-DOCKER-Clientes`, en el cluster principal `192.1.1.x`) respondió directamente y mostró la pantalla de login de NPM. A diferencia del hallazgo de `OPENDB_31` (inferido de una nota de backup), esto es una prueba de conectividad real, hecha a mano, y confirma que Piedras alcanza por red al segmento `192.1.1.x` — no solo al rango de backups `10.77.254.x`. Implica que el ítem 1 de Tier 1 (mapear las 4 instancias de NPM — ver `plan_relevamiento_alta_cefas.md`) puede arrancarse directamente desde una sesión de TeamViewer a Piedras, sin necesitar un salto intermedio a una VM del cluster principal. Login a NPM todavía pendiente — falta la credencial.
- **Ampliación (13-14 sep 2026, durante el trazado de Argocean): Piedras llega al dashboard HTTPS de `FW` pero no más allá.** Los intentos de acceso a `WL-CLIENTES` (`172.18.5.40`) de estos días se hicieron, sin saberlo en el momento, desde `Win10-Piedras` (`192.168.100.165`, confirmado por `ipconfig` — gateway `192.168.100.254`). Resultado: `https://192.1.3.1` (dashboard pfSense) responde con normalidad desde Piedras, pero **ni el SSH del propio `FW` (puerto 22) ni una ruta directa a la red interna que protege (`172.18.5.x`) son alcanzables** desde esa misma estación — coherente con que ese segmento sigue genuinamente aislado, ahora confirmado también desde Piedras específicamente. El único camino que sí funcionó fue ejecutar comandos *dentro* del propio firewall vía `Diagnostics > Command Prompt` de la UI web (que sí llega, por HTTPS) — desde ahí el `ping`/`ssh` a `172.18.5.40` sí conectó (el firewall tiene IP propia en esa red, `172.18.5.2`). Ver la entrada de Argocean capa 4, más abajo, para el detalle completo del intento.
- **`PiedrasDB01` — nota cruzada sin explicar.** Su campo `Notas` contiene solo una IP, `192.1.3.121`, que pertenece al rango del *segundo* host del sitio principal (`192.1.3.252`), no al rango de Piedras (`192.168.100.x`). Podría ser una referencia a una migración o replicación entre sitios — sin confirmar.
- **`COBRA` — posible cliente sin documentar.** Windows Server 2012, DNS `COBRA.open.solutions`, no coincide con ninguno de los 15 clientes conocidos. Mismo patrón por el que se encontró Argocean — no asumir que es un cliente nuevo sin verificar.
- **`PiedrasWL01` es explícitamente de prueba** — su propia nota dice "test weblogix simple licence. 12.2.1.19 y db 23ia", así que no cuenta como WebLogic productivo del sitio.

**`ExportList-Datacenter-Full.csv` — mismo snapshot, sin novedad.** Llegó junto con el export de Piedras como un re-export "más completo" del sitio principal, pero al comparar registro por registro contra `ExportList.csv` (129 VMs) el estado, host e IP de cada VM son idénticos — es el mismo snapshot con columnas extra (Clúster, UUID, DNS, estado de VMware Tools). El campo `Clúster` viene vacío para las 129 VMs, así que no resuelve la pregunta abierta sobre si `192.1.3.252` es un cluster/sitio separado. No aportó VMs ni hallazgos nuevos — se documenta acá para que nadie vuelva a re-parsearlo esperando encontrar algo distinto.

**`OPENPORTAL01` = "portalDM" — resuelto.** La nota de la VM en el export enriquecido dice "portal prod DM" con IP `10.77.10.5 (ex 192.1.3.22)` — coincide exactamente con la entrada de Relevamiento para `portalDM` que quedaba sin confirmar (ver "Todavía abierto" más abajo, antes ítem 2). Es el portal de producción de **Maipú** (mismo prefijo "DM" que `DMWL01`/`DMFW01`, y su job de backup Veeam es `2026_DiagMaipu`).

**`OPENDOCKER03` — resuelto (ya estaba explicado, ahora sin ítem abierto pendiente).** La IP que Relevamiento le atribuye (`192.1.1.113`) no aparecía en el ExportList.csv original porque la VM estaba apagada al momento del export y vCenter solo reporta IP de invitado con VMware Tools corriendo. El relevamiento de Docker la confirma de forma independiente en esa misma IP (Ubuntu 22.04, Portainer). La nota del export enriquecido confirma la IP una tercera vez.

**`WEBSERVER` — rol confirmado.** La nota dice: corre Docker, con contenedores para `help.open.com.ar` (probablemente un wiki/portal de ayuda — el nombre del contenedor es `documize`), `nagios` (monitoreo) y `ppc-prod`. No es un frontend nginx (esa conjetura ya se había descartado) ni un servidor web genérico — es un host de utilidades internas.

**`OPENWLCLI01` — aclarado.** La nota dice "(se clono de la 10.77.7.201) dejar apagada" — es un clon intencionalmente apagado de `OPENWLPROD01` (WebLogic de producción de EBY/GIAR), no un servidor productivo separado. Coherente con que figura "Apagado" y 0 días de actividad en el export.

**`OpenRepo` — aclarado.** La nota dice "en teoria no se usa mas x migracion a git" — repositorio de código legado, reemplazado por un sistema de control de versiones git. VM apagada, sin uso activo esperado.

**nginx — resuelto.** `Relevamiento_general_infraestructura_Docker_actualizado.docx` confirma que Nginx Proxy Manager corre como contenedor en 4 de los 9 hosts docker: `DOCKER-DEB`, `OPENDOCKER04`, `VM-DOCKER-Clientes`, y `VM-DOCKER-Clientes (1)`. No es una categoría de host dedicado — es un servicio containerizado. La conjetura anterior `web_frontend_nginx_candidate` (`OPENPORTAL01`, `OPENPORTALCLI02`, `WEBSERVER`) estaba equivocada; el rol real de esas VMs sigue sin confirmar.

**pfsense — confirmado para 7 de 9 hosts.** `Analisis Azure.docx` (ver `cloud-infra/source/`) ya había confirmado `OPENVPNFW01` (VPN Site-to-Site de Azure, peer "pfSense" en `200.55.243.92`). `source-files/Relevamiento (sin claves) - Pfsense.csv` — un relevamiento manual con acceso real al dashboard de cada firewall — confirma 6 más: `CliProFw01`, `DMFW01`, `FW`, `FWOPEN`, `OPENFWCLI001`, `OPENFWCLI10`. Cada uno tiene su IP de dashboard, credencial de acceso (usuario `smartsouth`, sin contraseña en el archivo) y una etiqueta de destino — por ejemplo `DMFW01` aparece etiquetado "Open - Maipu", consistente con ser el firewall dedicado de ese cliente. Solo quedan sin confirmar `OPENFWCLI02` y `VM_FW`. Detalle completo en `inventory.json` → `vms[].confirmation`.

**Corregido: la exposición a Internet NO es mínima — el "hallazgo positivo" anterior era incorrecto (19 ago 2026).** El relevamiento manual de firewalls había anotado "No hay puertos TCP expuestos... el único servicio escuchando desde Internet es OpenVPN en el puerto 2190 (UDP)". La captura real de la tabla Firewall → NAT → Port Forward de `FWOPEN` (`192.1.1.11`) contradice esto directamente: **al menos 19 reglas de NAT activas en WAN1**, la mayoría TCP, ver detalle completo en `inventory.json` → `vms[FWOPEN].nat_rules`. Entre lo más sensible:

- **`vcenter` — puerto WAN `667` → `192.1.1.29:443`.** La interfaz de administración de vCenter (la herramienta que gestiona los 12 hosts ESXi del cluster principal) está expuesta a Internet. Es la exposición más seria de la lista.
- **`firewall` — puerto WAN `666` → `192.1.1.11:443`.** El propio `FWOPEN` expone su panel de administración HTTPS hacia afuera (aparece duplicada en la captura).
- **Múltiples apps de cliente con NAT directo, sin pasar por NPM:** `192.1.1.191` (`WebLogic.191`) recibe tres puertos WAN distintos hacia el mismo destino interno `:80` — `8089` ("Mafisa Publicacion"), `8088` ("NAT DVAL"), `8191` ("UIA Weblogic") — cada cliente con su propio puerto externo en vez de un dominio.
- **Correo (Exchange):** `192.1.1.10:25/587/993` — un servidor de mail que no está en ningún export de vCenter (ver blind spot nuevo, abajo).
- **Confirma las dos NAT reales de los NPM que ya veníamos mapeando:** `200.55.243.94:80/443` → `192.1.1.37` (`DOCKER-DEB`) y `WAN1:80/443` → `192.1.1.38` (`VM-DOCKER-Clientes`) — **esta última es la ruta real de entrada de CEFAS**, cierra la capa 3 de `plan_relevamiento_alta_cefas.md`.

No se revisó ningún otro firewall todavía — no se puede asumir que esto es exclusivo de `FWOPEN` ni que los demás (`FW`, `CliProFw01`, etc.) están mejor. Vale la pena repetir este chequeo en los otros pfSense confirmados antes de dar por cerrado el tema.

**Rex Argentina — primera infraestructura identificada (19 ago 2026).** La misma tabla de NAT de `FWOPEN` tiene dos reglas "SS Rex Front" (puerto WAN `4431` → `192.1.1.57:80`) y "SS Rex Back" (`4433` → `192.1.1.57:4433`). `192.1.1.57` es `OPENDOCKER.57`, que el relevamiento de Docker ya listaba con un servicio `SelfRex` sin más contexto — ahora se confirma qué es: el mismo patrón "Self Service" que CEFAS (`ss_front_cefas`/`ss_back_cefas`), pero para Rex. Es la primera infraestructura real que se le puede asociar a Rex Argentina desde que se lo descubrió — avanza el ítem 2 de Tier 1 en `PLAN.md`, aunque todavía sin identificar la DB.

**Dos clientes/apps nuevos, sin ningún rastro en ningún otro documento: "Mafisa" y "UIPBA".** `Mafisa Publicacion` (NAT directo a `WebLogic.191:80`) y `Cliente UIPBA` (`192.1.1.17:9001`, más `UIA DB 1521` en la misma IP puerto `1521` — un listener Oracle). **Ninguna de las dos IPs de UIPBA/UIA DB (`192.1.1.17`) ni la de mail (`192.1.1.10`) aparecen en ExportList.csv** — agregado como blind spot nuevo en `inventory.json`. Mismo patrón de "cliente sin documentar" que Argocean, pero esta vez ni siquiera vCenter los ve — podrían ser hosts físicos o de un cluster/vCenter distinto al relevado.

**`DB-ARGOCEAN` — resuelto.** Es la base de datos de un cliente llamado **Argocean**, según la hoja Discrepancias de la matriz ("DB 172.18.5.60 / SID MBA — No figura [en tabla funcional]"). Argocean no está en la lista original de 13 clientes en absoluto — apareció solo en las notas de discrepancias. La VM está actualmente apagada en ExportList.csv. WebLogic identificado el 19 ago 2026 — ver hallazgo de `VM-DOCKER-Clientes` más arriba.

**GIAR — versión de WebLogic resuelta (16 ago 2026).** Verificado por TeamViewer entrando a `http://10.77.7.201:7001/console/login/LoginForm.jsp` (`OPENWLPROD01`): la página de login muestra "Versión de WebLogic Server: **12.2.1.4.0**". Confirma el valor del inventario técnico (`12.2`); el valor funcional ("WL 11") era el desactualizado. No se pudo entrar a la consola en sí — la cuenta compartida (`soportesmart`) fue rechazada, así que solo se confirmó la versión desde la pantalla de login, no el resto del dominio. **Gotcha a tener en cuenta:** `10.77.7.201` (GIAR) y `10.77.10.101` (Maipú) se confunden fácil al leerlas rápido — casi se registra este dato contra el cliente equivocado.

## Nuevo: los "13 clientes" son más bien 15, con bajas

La matriz (`Matriz_servicios_por_cliente_Hosting_V2.xlsx`) agrega dos clientes que no estaban en el CSV de Relevamiento:

- **Rex Argentina** (código `279`) — "incorporado recientemente al hosting", entorno PROD, usa Condor Work, DB referenciada solo como "REX Producción" (todavía sin resolver a una VM específica — necesita una búsqueda por TeamViewer, ~15 usuarios / 4.600 legajos según las notas de la matriz).
- **Argocean** — ver arriba, por ahora solo con DB, sin WebLogic identificado.

Más importante todavía, aunque con una fuente más floja de lo que parece a primera vista: **una nota suelta en la matriz dice que ABB S.A. y Arris de Argentina S.A. (GIAR) ya son clientes dados de baja**, con sus bases de datos retenidas solo temporalmente. Esa nota está pegada debajo de la tabla principal del Excel (no es un campo formal), y dice textual "Estos dos clientes estan de baja pero por el momento se mantiene sus bases". El problema: el campo formal `Estado / migración` de esos mismos dos clientes, en la misma planilla, dice **"Mantenimiento solamente"** — no "de baja". No son necesariamente contradictorios (podría estar en mantenimiento mientras se lo da de baja), pero es una nota informal contradiciendo, o al menos matizando, un campo estructurado — no algo para dar por hecho. Confirmar con el equipo saliente antes de tratar a cualquiera de los dos como cliente de baja prioridad. (Ambos siguen apareciendo en `used_by_clients` en inventory.json de todas formas, ya que sus VMs todavía existen y están encendidas.)

## Nuevo: la matriz documenta sus propias discrepancias sin resolver

La hoja "Discrepancias" de la matriz es el propio equipo de Open detectando conflictos entre una fuente "funcional" (de una persona llamada Fran) y el inventario "técnico" — vale la pena llevarla adelante tal cual en vez de re-derivarla:

| Cliente | Tema | Dice la fuente funcional | Dice el inventario técnico |
|---|---|---|---|
| ROMAN (CSM) | Versión de WebLogic | WL 11 | 10 |
| ~~ABB~~ | ~~DB actual~~ | ~~`192.1.1.31` / SID ABB~~ | ~~`192.1.1.31` y `192.1.1.190` (dos IPs)~~ → **resuelto: `192.1.1.31` / SID `ABB` / schema `CONDOR`** (6 sep 2026) |
| CEFAS | SID | CEFASPDB | CEFAS |
| ~~BOCA~~ | ~~SID~~ | ~~BOCAPDB~~ | ~~BOCA~~ → **resuelto: las dos correctas — `BOCA` es la CDB, `BOCAPDB` la PDB con los datos (schema `CONDOR`)** (12 sep 2026) |
| EBY | Servidor WebLogic | destino `10.77.7.201` | actual, compartido: `192.1.2.54` |
| Enerflex | Charset | `WE8ISO8859P15` | sin verificar — marcado como posible error de tipeo |

El documento explícitamente **no** eligió un valor por sobre el otro cuando las fuentes no coincidían — la misma política que debería seguir este proyecto. Ver `clients[].matrix_detail` en inventory.json para el detalle completo por cliente (SID, versión/edición/tamaño/charset de DB, destino de migración, qué productos Condor usa cada cliente).

**ABB resuelto (6 sep 2026):** la fuente funcional tenía razón — la DB actual productiva es **`192.1.1.31`** (instancia `ABB`, schema de app `CONDOR`), confirmada por `sqlplus`/`dba_tab_modifications`. La segunda IP del inventario técnico (`192.1.1.190`) **no es DB de ABB**: ese box no tiene instancia `ABB`, solo las sub‑bases históricas `abbhist`/`abbtubio` (apagadas). Ver "Resueltos / confirmados" arriba.

**BOCA resuelto (12 sep 2026):** ninguna de las dos fuentes estaba equivocada — **`BOCA` y `BOCAPDB` son ambas correctas, a distinto nivel.** `sqlplus`/`v$database` en `CLIENTES-DB` (`192.1.1.32`, `ORACLE_SID=BOCA`) mostró `CDB=YES`: la instancia `BOCA` es una CDB multitenant casi vacía (solo schemas de infraestructura Oracle), y los datos reales de la app viven en la PDB `BOCAPDB` (`ALTER SESSION SET CONTAINER=BOCAPDB`), schema `CONDOR` (~22.5 GB, coincide con los 18G declarados en la matriz). Charset `WE8MSWIN1252` confirmado dentro de la PDB. Último DML de `CONDOR`: **12/09/2026 21:37, el mismo día de la consulta** — uso productivo activo confirmado en vivo, sin ambigüedad. **BOCA queda en 7/7 capas**, trazado formal completo (ver `plan_relevamiento_alta_boca.md`). Mismo patrón multitenant ya visto en Rex/GIAR (`CDBOPEN03`).

**ROMAN resuelto parcialmente (6 sep 2026):** la versión real de WebLogic es **`12.2.1.4.0`** (`OPENWLPROD01`, mismo box/molde que EBY/GIAR) — la fuente funcional (WL 11) estaba más cerca; ambas fuentes subestimaban. Queda abierta la segunda mitad de la fila ("¿la base/servicio es TEST o producción?"): la config de `OPENWLPROD01` dice producción (`[csm]*` → `pageTitle=CSM PRODUCCION`, `@PRODCSM` → `OPENDBPROD03`), pero sin tráfico Forms en vivo observado. Ver "Resueltos / confirmados" arriba.

**CEFAS resuelto (25 ago 2026):** el SID real es **`CEFAS`**, no `CEFASPDB` — confirmado por `sqlplus`/`v$database` en `CLIENTES-DB`. Ver "Resueltos / confirmados" arriba.

**JOBS resuelto (1 sep 2026):** la versión real es **12.2.1.4.0**, confirmada en la pantalla de login de la consola WebLogic — la fuente funcional (WL 12) tenía razón, mismo patrón que GIAR. Ver "Resueltos / confirmados" arriba.

**EBY resuelto (2 sep 2026):** ninguna de las dos fuentes tenía la foto completa — **EBY corre en los dos motores a la vez**, no en uno solo. `WebLogic.191` (ruta `yacyreta.condorwork.com.ar`, DB compartida `Database .90`/`CDRADM`) y `OPENWLPROD01`/`10.77.7.201` (ruta `eby-prod.condorwork.com.ar`, DB `OPENDBPROD005` confirmada por `tnsnames.ora` real — alias `EBYPROD`/`EBYQA`/`EBYAUDIT`) están ambos en producción viva y concurrente. El "destino" que la fuente funcional señalaba (`10.77.7.201`) sí es real, pero no reemplazó al compartido — conviven. Ver "Resueltos / confirmados" arriba.

## Todavía abierto

### 1. El alcance de Relevamiento (solo WL+DB) implica que ausencia ≠ cliente nuevo

(Ver análisis previo, sigue vigente.) Solo 2 de los 13 clientes originales muestran una coincidencia de nombre para infraestructura adicional sin documentar: **GIAR** tiene una instancia extra `WL-GIAR` además de su `OPENWLPROD01`/`DB-GIAR` mapeados, y **ROMAN** tiene un `WL-ROMAN` y un `DB-ROMAN-HISTORICO` extra además de su `WL-CLIENTES`/`DB-ROMAN` mapeados. Otras ~31 VMs con patrón WL/DB no llevan ningún código de cliente en el nombre y todavía necesitan una pasada por TeamViewer para clasificarlas como: otro componente de un cliente conocido, una copia de dev/QA/test, o un cliente genuinamente separado (como resultó ser Argocean).

### 2. Segundo host ESXi (`192.1.3.252`) — probablemente un sitio separado

Todavía sin confirmar si es una segunda ubicación física o una máquina standalone; aloja la infraestructura de GIAR y ROMAN que no está en el rango `192.1.1.x`.

### 3. Nombres de posibles clientes sin trackear, vistos en las Notas del export enriquecido (18 ago 2026)

Ninguno de estos está en la lista de 15 clientes conocidos (13 de Relevamiento + Rex Argentina + Argocean). Ninguno está confirmado — son nombres sueltos en anotaciones manuales o en listas de esquemas/tablespaces de Oracle, que pueden ser clientes reales, entornos legados, o nombres internos sin relación con un cliente. Priorizar por fuerza de la señal:

- **Señal más fuerte — lista explícita de clientes.** La nota de `VPNCLIENTESRDP` (VM de acceso RDP, IP `192.1.1.136`) dice: "RDP de acceso a clientes no compatibles localmente — Allianz, Monroe, Rovella, SMQ, NABSA". Es una anotación humana enumerando clientes por nombre, no un nombre de esquema — la fuente más confiable de esta pasada. Ninguno de los cinco tiene VM mapeada todavía.
- **Señal más débil — nombres de esquema/tablespace de Oracle**, que podrían ser clientes o podrían ser artefactos internos/legados: `Electroban` y `Pietroboni` (notas de `CLIENTES-DB`/`CLIENTES-DB2`), `PVINST`/`VINST` (aparece igual en `CLIENTES-DB` y en `Database-24`, lo que sube algo la confianza de que sea el mismo nombre real), `CITEDEF` (nota de `Database .23` — coincide con el nombre de un organismo real, Centro de Investigaciones Científicas y Técnicas para las Fuerzas Armadas), `aqualum`/`abbhist`/`abbtubio` (nota de `DBClientes-12C.31` — los dos últimos parecen sub-esquemas de ABB, no clientes nuevos), y `SIGO`/`NQN`/`DBRHGOB` (notas de `OPENDBPR001` y `Database12c .18`).

Antes de tratar cualquiera de estos como cliente nuevo, confirmar por TeamViewer o preguntándole al equipo saliente — mezclar nombre de esquema con nombre de cliente fue justo el tipo de error que este proyecto viene evitando a propósito.

### 4. Posible tercera IP para EBY, pero la nota que la menciona es sospechosa de estar contaminada

El cruce de IPs (`cross_reference_ips.py`) contra los nuevos exports encontró `10.77.8.201` dentro de la nota de `OPENWLCLI01` (el clon apagado de `OPENWLPROD01`/`10.77.7.201`): "EBY PRODUCCION / 10.77.8.201 / (se clono de la 10.77.7.201) dejar apagada / EBYPROD / CEFAS / GIARG / test weblogix simple licence. / 12.2.1.19 y db 23ia". A primera vista es una tercera IP candidata para la ruta de EBY (Tier 1, ítem 3 de `PLAN.md`), pero la misma nota mezcla textualmente varios clientes sueltos (EBY, CEFAS, GIARG) y termina con **la frase idéntica, palabra por palabra**, a la nota propia de `PiedrasWL01` ("test weblogix simple licence. 12.2.1.19 y db 23ia") — una VM completamente distinta, en el sitio Piedras. Esa coincidencia exacta de texto entre dos VMs no relacionadas es la señal más fuerte hasta ahora de que la columna `Notas` del export tiene contenido pegado/desplazado entre celdas, no una anotación fiable celda-por-celda. Tratar `10.77.8.201` como un dato a verificar, no a asumir, al resolver el ítem 3 de Tier 1 — y tener la misma cautela con cualquier otra nota de `OPENWLCLI01` de acá en más.

También apareció `10.10.10.1` en la nota de `FWOPEN` (junto a sus otras IPs conocidas) — sin indicio de qué es; baja prioridad.

## Próxima pasada sugerida por TeamViewer (en orden de prioridad)

1. ~~Confirmar el estado real de ABB y GIAR~~ **RESUELTO** — ABB apagado de hecho desde el 1-jul-2026 (ver `plan_relevamiento_alta_abb.md`); GIAR probablemente migró de verdad, legado muerto en vCenter (ver `plan_relevamiento_alta_giar.md`).
2. ~~Resolver la identidad de la VM de base de datos de Rex Argentina~~ **RESUELTO (14 sep 2026)** — trazado completo 7/7, ver `plan_relevamiento_alta_rex.md` y la entrada "Rex Argentina — trazado punta a punta cerrado" más arriba.
3. Confirmar o descartar pfsense en las 2 VMs `firewall_candidate_pfsense` que quedan (`OPENFWCLI02`, `VM_FW`) — las otras 7 ya están confirmadas.
4. Recorrer las 12 VMs `infra_generic_unclear` (`OPENINFRxx`) — sin ninguna señal en el nombre.
5. Para las ~31 VMs con patrón WL/DB todavía sin mapear, clasificar cada una como producción-de-un-cliente-sin-documentar vs. dev/QA/test/histórico vs. componente de un cliente conocido.
6. Confirmar el rol real de `OPENPORTAL01`, `OPENPORTALCLI02`, `WEBSERVER` ahora que se sabe que la conjetura de nginx para ellos era incorrecta.
7. Confirmar si Allianz, Monroe, Rovella, SMQ y NABSA (nombrados en la nota de `VPNCLIENTESRDP`) son clientes activos sin VM mapeada, o clientes ya dados de baja que solo quedaron en una nota vieja.
8. Si hay oportunidad de entrar a vCenter directamente (no solo al export CSV), anotar la fecha de creación ("Created"/`Config.CreateDate`) de `DASADBPROD01`, `OPENDBPROD006` y `OL8LABWL01` — son los tres candidatos a incorporación/migración más reciente (ver hallazgo arriba), y una fecha real cerraría la pregunta en vez de inferirla por uptime.
9. **Sitio Piedras, ahora que está confirmado:** identificar `COBRA` (¿cliente sin documentar o nombre interno?), entender la nota cruzada `192.1.3.121` en `PiedrasDB01`, y — si hace falta confirmar `OpenPiedrasFw01` como el pfSense de Piedras — encenderla y entrar al dashboard.
