# Hallazgos y preguntas abiertas

**Alcance: solo infraestructura on-premise.** La infraestructura en proveedores cloud (Azure/AWS) se estacionó fuera de este relevamiento — ver [`../cloud-infra/findings-cloud.md`](../cloud-infra/findings-cloud.md) para lo que ya se había encontrado ahí (incluye un hallazgo importante, una VM de Azure sin inventariar, por si se retoma).

Fuentes: ExportList.csv (export de vCenter, verdad de base para "qué VM existe" — desde el 18 ago 2026 incluye también "Tiempo de actividad"/uptime y una columna "Notas" con anotaciones manuales por VM, cargadas en `vms[].uptime_days` / `vms[].notes` en el inventario), CSV de Relevamiento (manual, 13 clientes, solo WL+DB), y — agregado después — dos emails internos más su adjunto RAR (`source-files/extracted/`), que contiene una matriz de servicios por cliente más completa y un relevamiento de infraestructura Docker. **Todo el material derivado de Relevamiento y de los emails es documentación interna de trabajo, no verdad verificada — tratar cada hallazgo de abajo con las mismas pinzas que aplican los propios documentos fuente** (la hoja "Discrepancias" de la matriz existe justamente porque el propio equipo de Open encontró que sus dos fuentes de datos no coincidían).

## Nuevo: infraestructura on-premise real que no está en nuestro inventario

Buscamos todas las IPs mencionadas en el material fuente y las cruzamos contra `inventory.json`. Del lado on-premise, encontramos una sola cosa sin identificar:

- **Servidor(es) NFS.** `OPENDOCKER.57` y `VM-DOCKER-Clientes` montan recursos NFS externos (uno con "montaje con error" y otro "con alta ocupación", según el relevamiento de Docker) pero el host NFS nunca se nombra. Fácil de resolver nosotros mismos: entrar por TeamViewer a esos dos hosts y correr `mount`/revisar `/etc/fstab`.

(El mismo cruce encontró dos cosas del lado cloud — una VM de Azure sin inventariar y una mención sin detalle de AWS — documentadas en `cloud-infra/findings-cloud.md` en vez de acá.)

## Nuevo: ¿cuál fue el cliente/servidor incorporado o migrado más recientemente? (18 ago 2026)

vCenter no expone fecha de creación de VM en el export que tenemos, y no hay acceso programático a los hosts — así que no hay una respuesta exacta. Pero el export enriquecido con "Tiempo de actividad" (uptime desde el último reinicio) y "Notas" manuales da la mejor señal disponible hasta ahora:

- **Señal fuerte, explícita: `DASADBPROD01` (base de datos de producción de Maipú).** La nota dice literalmente "DASA db PRODUCCION NUEVO" — no es una inferencia por uptime, es una anotación directa de que es una base nueva. Maipú figura como `"Solo infraestructura"` en `inventory.json` (todavía sin WebLogic/Condor terminado de configurar sobre esa base), consistente con una incorporación en curso.
- **Señal débil, por uptime bajo: `OPENDBPROD006` (base de datos de Entidad Binacional Yacyretá, 28 días de actividad)** — el uptime más bajo entre todos los servidores de cliente encendidos. EBY figura como `"Migrado / actualización pendiente"`.
- **Señal débil, por uptime bajo: `OL8LABWL01` (WebLogic de Heinlein, 55 días de actividad)** — segundo uptime más bajo. Heinlein también figura como `"Solo infraestructura"`.
- Los tres clientes con las señales más fuertes (Maipú, EBY, Heinlein) coinciden en tener un `status` de migración/infraestructura incompleta en el inventario — consistente con la idea de que son las incorporaciones más recientes y todavía en curso.

**Caveat importante:** el uptime mide el último reinicio de la VM, no su fecha de creación — un reinicio por mantenimiento o parche produce la misma señal que una VM recién provisionada. Solo `DASADBPROD01` tiene evidencia directa (la nota "NUEVO"); el resto es inferencia. Para una respuesta definitiva, preguntar directamente al equipo saliente en la reunión pendiente, o (si hay una sesión de TeamViewer a vCenter) revisar la propiedad "Created"/`Config.CreateDate` de cada VM candidata — no está en el export CSV que tenemos.

## Resueltos / confirmados

**46 reglas NAT de `FWOPEN` transcriptas (19 ago 2026).** `FWOPEN-nat-rules/nat_rules.csv`, cargado en `inventory.json` → `vms[FWOPEN].nat_rules`. Confirma por qué no hay una regla NAT específica para cada backend interno (`192.1.1.1:9001` de JOBS, por ejemplo): pfSense solo expone los dos NPM (`192.1.1.37` con IP pública dedicada `200.55.243.94`, y `192.1.1.38` — el de los contenedores CEFAS — con la WAN1 genérica, ambos en 80/443); el resto del ruteo por dominio lo hace NPM puertas adentro, en la misma LAN, sin que pfSense tenga que intervenir. Además:

- **`WebLogic.191` confirmado como el host más compartido del relevamiento.** Entre NAT directo y dominios de NPM, sirve a **DVAL** (ya conocido), **UIA**, **Mafisa**, **SIGO**, **EBY/Yacyretá**, **DCViajes**, **Tassaroli** — y, según se confirmó después vía el NPM de `VM-DOCKER-Clientes` (ver más abajo), también **CEFAS** (`cefas.condorwork.com.ar`). Ocho clientes distintos en la misma VM.
- **"Weblogic TARANTO" y "esyop acceso" son la misma regla exacta** (mismo puerto destino `8906`, mismo NAT `192.1.1.19:8888` — que es literalmente `WebLogic.19`, el WL ya resuelto de ESYOP). "Taranto" es probablemente un alias/nombre interno viejo de ESYOP, no un cliente nuevo.
- **`SS Rex Front`/`SS Rex Back` → `192.1.1.57`** (`OPENDOCKER.57`, host Docker ya conocido) — candidato fuerte para la infraestructura de **Rex Argentina**, que hasta ahora tenía cero recursos mapeados (Tier 1, ítem 2 de `PLAN.md`).
- **Acceso directo a Oracle sin pasar por NPM, para varios clientes:** `mitiga Cefas DB 1525` (WAN1:`11517` → `192.1.1.32:1525`, la DB actual de CEFAS), `acceso jobs` x2 (→ `192.1.1.44:1521` y `192.1.1.51:1521`, dev y prod de JOBS), `acceso YACYRETA` (→ `192.1.1.22:1521`), `UIA DB 1521` (→ `192.1.1.17:1521`, restringido a un alias `ip_uia`). Para CEFAS en particular, esto abre una vía a la capa 5 de `plan_relevamiento_alta_cefas.md` sin necesitar SSH — solo credencial Oracle.
- **`vcenter` expuesto por WAN** (`667` → `192.1.1.29:443`, coincide exacto con la VM `vCenter` del inventario) y **Exchange también** (`80`/`443`/`25`/`587`/`993` → `192.1.1.10`, IP que no matchea ninguna VM del export — posible appliance físico o VM apagada sin IP reportada). Contradice, al menos en apariencia, el hallazgo anterior de "solo OpenVPN expuesto" del relevamiento manual de pfSense — sin confirmar todavía si esas reglas están habilitadas o si ese hallazgo anterior hablaba de otro firewall. Ver "Todavía abierto" más abajo.
- **Nueve IPs de destino NAT sin VM conocida:** `192.1.1.10` (Exchange), `.12` (NAT Remote App), `.133` (Firma Digital), `.17` (Cliente UIPBA/UIA DB), `.2` (SIGOWEB Backend), `.26` (TETST), `.39` (REMOTO_SALUD), `.70` (SELFSERVICE), `.97` (Jira) — ninguna aparece en las 129 VMs de `ExportList.csv`. Mismo patrón que otros blind spots del proyecto: pueden ser VMs apagadas sin IP reportada, o equipos fuera de vSphere.
- **Nombres de cliente nuevos, sin documentar:** `Mafisa` (`Mafisa Publicacion`) y `UIPBA` (`Cliente UIPBA`) — se suman a `COBRA`, `SIGO`, `Pietroboni`, `UIA` y `Tassaroli` como candidatos a clientes sin registrar en los 15 conocidos.

**Mapeo completo dominio → NPM → servidor interno de `DOCKER-DEB` (19 ago 2026).** 79 proxy hosts reales, transcriptos de 8 capturas de pantalla del panel de `DOCKER-DEB` (`192.1.1.37:81`) — ver `DOCKER-DEB-NginxProxyManager/proxy_hosts.csv` y `inventory.json` → `vms[DOCKER-DEB].docker_detail.proxy_hosts`. Varios hallazgos de fondo, no solo el mapeo en sí:

- **`WebLogic.191` (`192.1.1.191`) resulta compartido — y CEFAS no aparece ahí.** Puerto `80` sirve a `sigo.open.com.ar`, `uia.condorenterprise.com.ar`, `yacyreta.condorwork.com.ar` (**EBY**) y `dcviajes.opensolutions.com.ar`/`tassaroli.condorwork.com.ar` (puerto `8888`) — cinco dominios de clientes distintos en la misma VM, ninguno con "cefas" en el nombre. Esto es coherente con el `connection refused` que ya habíamos visto en el puerto `7001` (`plan_relevamiento_alta_cefas.md`): la VM está viva y sirve a otros clientes, pero puede que el deployment de CEFAS específicamente ya se haya migrado fuera de ahí. El dominio principal de CEFAS sigue sin aparecer — no está en `DOCKER-DEB`, habría que buscarlo en el NPM de `VM-DOCKER-Clientes` (`192.1.1.38:81`), que es donde viven los contenedores "CEFAS frontend/backend".
- **EBY tiene tres endpoints reales, no dos.** `eby-prod.condorwork.com.ar` y `eby-qa.condorwork.com.ar` → `10.77.7.201:9001` (`OPENWLPROD01`); `ebyprod.open.com.ar` → `10.77.8.201:9001` (la IP que habíamos marcado sospechosa de nota contaminada — resulta ser real después de todo); `yacyreta.condorwork.com.ar` → `192.1.1.191:80` (`WebLogic.191`). Ninguno de los dos candidatos originales de la hoja Discrepancias (`10.77.7.201` vs `192.1.2.54`) resulta ser el único real — hace falta revisar cuál de los tres está efectivamente en uso antes de cerrar el ítem 3 de Tier 1 en `PLAN.md`.
- **Tres nombres nuevos, confirmados como dominios reales de clientes:** `sigo.open.com.ar`, `uia.condorenterprise.com.ar` ("UIA", posible Unión Industrial Argentina), `pietroboni.condorwork.com.ar` y `tassaroli.condorwork.com.ar`. `SIGO` y `Pietroboni` ya estaban anotados como señal débil (nombre de esquema Oracle) en "Todavía abierto" #3 — esto los sube a señal fuerte, son dominios públicos reales, no solo nombres de tablespace.
- **Confirma en vivo el WebLogic compartido `WL12C-Desarrollo.2.54`:** `abb.condorwork.com.ar`, `cabj.condorwork.com.ar` (Boca) y `condor.open.com.ar` van los tres a `192.1.2.54:9001`.
- **`cloud.opensolutions.com.ar` → `192.1.1.33:80`** — resuelve por qué Nextcloud en `NUBE-OPEN-DOCKER` rechazaba el acceso por IP directa.
- **Dos IPs públicas fuera del rango interno:** `condor.com.ar` → `34.198.2.32` y `condorsolutions.com.py`/`www.condorsolutions.com.py` → `34.202.29.65` (ambas con pinta de AWS), y `proviadev01.condor.com.ar` → `172.171.154.190` (rango de Azure) — coincide con Condor ProvIA, ya documentado en `cloud-infra/`.
- **Rango nuevo sin identificar: `192.168.222.x`** (`ords-balanz.open.com.ar` → `.20`, `ords-balanztest.open.com.ar` → `.19`) — no aparece en ningún otro documento. Podría ser otro sitio o red aislada, como pasó con Piedras.
- **Una IP nueva dentro de la subred de Piedras:** `it.open.com.ar` → `192.168.100.8` (deshabilitado) — no estaba en las 15 VMs de `ExportList-Piedras-Full.csv`, sugiere que ese export no es exhaustivo de todo lo que responde en esa subred.

**Dominio principal de CEFAS encontrado — y las dos tecnologías conviven de verdad (19 ago 2026).** Acceso SSH directo a `VM-DOCKER-Clientes` (`192.1.1.38`) y consulta a la tabla `proxy_host` de su NPM interno (9 filas, ver `inventory.json` → `vms[VM-DOCKER-Clientes].docker_detail.proxy_hosts`) — corrige y completa el hallazgo anterior de `DOCKER-DEB`:

- **`cefas.condorwork.com.ar` → `192.1.1.191:80` (`WebLogic.191`), habilitado.** Contradice lo que se había concluido con los datos de `DOCKER-DEB` sola ("CEFAS no aparece en `WebLogic.191`") — sí aparece, solo que en el NPM de otra VM. `WebLogic.191` sigue siendo compartido con otros clientes (ver hallazgo de `DOCKER-DEB` arriba), pero CEFAS es uno de ellos después de todo.
- **`cefas.condorlink.com.ar` → `192.1.1.38:8881` (`ss_front_cefas`) y `cefasbk.condorlink.com.ar` → `192.1.1.38:9001` (`ss_back_cefas`), ambos habilitados.** Es la app "Self Service" (`matrix_detail.productos.Self Service: "Sí"` para CEFAS) — confirma la hipótesis de `plan_relevamiento_alta_cefas.md`: la capa Docker no es un reemplazo del motor clásico, es un **producto aparte que convive con él** (acá, específicamente Self Service, no "Work" en general).
- **Nombres reales de los 5 contenedores** (antes solo "CEFAS frontend/backend" genérico): `ss_front_cefas` (imagen `self-service-angular:cefas-sfd`), `ss_back_cefas` (`self-service-backend:cefas-sfd`), `ss_pg_cefas` (`self-service-database:yacyreta-sfd` — **el tag de imagen dice "yacyreta", el nombre del contenedor dice "cefas"**, posible copia del compose de EBY/Yacyretá reusada para CEFAS sin actualizar el tag — mismo patrón de contaminación por copy-paste que ya se había visto en la columna `Notas` de `ExportList.csv`), más `nginx_app_1`/`nginx_db_1` (el propio NPM y su MariaDB interna).
- **Argocean — WebLogic identificado por primera vez.** `argocean.condorenterprise.com.ar` → `172.18.5.40` (`WL-CLIENTES`, el mismo WL compartido de ROMAN) — deshabilitado (`enabled=0`), sin confirmar si sigue en uso. Cierra parcialmente el vacío que quedaba abierto desde que se descubrió `DB-ARGOCEAN` (ver más abajo): ya tiene DB y WL candidatos, aunque el WL aparece apagado en la config.
- **`roman.condorwork.com.ar`/`roman.condorenterprise.com.ar` → `172.18.5.40`, también deshabilitados** — mismo patrón que Argocean, a confirmar si es config vieja o si ROMAN realmente entra por otra ruta hoy.

**CEFAS — Self Service confirmado autónomo del motor clásico (25 ago 2026).** Acceso SSH+root real a `VM-DOCKER-Clientes`, `config.yml` de `ss_back_cefas` (`/usr/src/backend/config.yml`, patrón Dropwizard) leído directo del contenedor vía `docker exec`. El único datasource configurado es `jdbc:postgresql://postgres:5432/selfservice` — el `ss_pg_cefas` interno. Cierra capa 4b de `plan_relevamiento_alta_cefas.md`: las dos tecnologías conviven (ver hallazgo de arriba) pero **no comparten datos** — Self Service no toca el Oracle del motor clásico. El `docker-compose.yml`/`config.yml` completo tiene contraseñas en texto plano (Postgres y SMTP) — no versionar ese archivo tal cual, sacarlas primero si hace falta guardarlo en el repo.

**`WebLogic.191` es en realidad Oracle Forms & Reports 11g, no WebLogic JavaEE genérico (25 ago 2026).** Acceso SSH real (requiere forzar `-oHostKeyAlgorithms=+ssh-rsa`, el host solo ofrece host keys viejos que los clientes OpenSSH modernos rechazan por default). `ps aux` muestra el dominio `ClassicDomain` (`/app/oracle/mid/user_projects/domains/ClassicDomain`) con managed servers `WLS_FORMS`/`WLS_REPORTS` — Oracle Forms & Reports Middle Tier, no una app JavaEE con datasources de WebLogic. El banner de login del host lista "CLIENTES: OPEN, UIA, DCVIAJES" — desactualizado, no menciona a ninguno de los ~8 clientes reales que sirve (mismo patrón de documentación que no sigue el ritmo de la realidad ya visto en la columna `Notas` de `ExportList.csv`). Sin `sudo` funcional en este host no se pudo leer `tnsnames.ora`/`formsweb.cfg`, pero **`netstat -tn` sin privilegios especiales mostró las conexiones Oracle activas en vivo**: 2 `ESTABLISHED` a `192.1.1.32:1525` (`CLIENTES-DB`, la DB actual de CEFAS) y cero tráfico hacia `10.77.7.11` (`OPENDBPROD001`, destino de migración) — confirma que la migración de CEFAS todavía no cortó tráfico productivo. Cierra capa 4a/5 de `plan_relevamiento_alta_cefas.md`. Otras conexiones activas del mismo host, sin identificar todavía: `192.1.1.90` (la más frecuente), `192.1.1.238`.

**CEFAS — servidor NFS identificado, y es el mismo `WebLogic.191` (25 ago 2026).** `mount`/`/etc/fstab` en `VM-DOCKER-Clientes` (`192.1.1.38`) confirma un mount NFSv4: `192.1.1.191:/clientes/cefas/cdr2/condorlink` → `/home/open/cefaslink/archivos` — justo el `uploadPath` que usa `ss_back_cefas` (ver hallazgo de 4b, arriba). Cierra el blind spot de storage abierto desde el primer relevamiento de Docker: no hay un servidor NFS separado, `WebLogic.191` cumple doble función (Forms & Reports + storage NFS para Self Service). Coincide con el volumen `/dev/mapper/cdr2-cdr2_data1` ya visto en el banner de uso de disco de esa VM.

**CEFAS — destino de migración de la DB identificado (18 ago 2026).** `matrix_detail.ip_db_destino` de CEFAS (`10.77.7.11`) coincide exacto con la VM `OPENDBPROD001`, ya presente en el inventario como base de datos de **Heinlein**. Sería una instancia Oracle compartida entre dos clientes, mismo patrón que otras DBs multi-cliente del proyecto (ej. `DBClientes.190` para ABB/DCVIAJES). Falta confirmar el SID (`prodcefas` según la matriz) y si ya es la productiva — ver `plan_relevamiento_alta_cefas.md` capa 5.

**Credencial de pfSense — confirmada, no bloqueada (18 ago 2026).** Acceso real logrado al dashboard de `FWOPEN` (`192.1.1.11`) durante el trazado punta a punta de CEFAS. A diferencia de SSH/Portainer/NPM (siguen sin credencial), pfSense ya no es un bloqueador — ver `plan_relevamiento_alta_cefas.md`.

**`WebLogic.191` — connection refused en el puerto de consola (18 ago 2026).** A diferencia de `OPENWLPROD01`/GIAR (mismo truco de mirar la pantalla de login sin credencial, que sí respondió), `http://192.1.1.191:7001/console` no respondió nada — ni la pantalla de login. Sin diagnóstico todavía: puede ser el firewall (falta revisar la regla NAT en `FWOPEN`, ahora accesible), el proceso de WebLogic caído, o que se haya movido de IP durante la migración que la matriz menciona ("BD y WL migrados", sin decir a qué IP). No asumir que el servidor está muerto sin revisar la regla NAT primero.

**Otra confirmación de que la columna `Notas` mezcla contenido entre VMs (18 ago 2026).** `OPENWLPROD01` tiene, entre sus notas, el mismo fragmento "CEFAS... test weblogix simple licence. 12.2.1.19 y db 23ia" que ya habíamos marcado como sospechoso en `OPENWLCLI01` (ítem 4 de "Todavía abierto", más abajo) y en `PiedrasWL01` — ahora aparece en un **tercer** VM no relacionado. Refuerza que estos tres fragmentos de nota son contaminación entre celdas, no datos propios de cada VM — no leer "CEFAS" en la nota de `OPENWLPROD01` como evidencia de que el WL de CEFAS vive ahí.

**"Piedras" — confirmado como sitio real (18 ago 2026).** Dos vías independientes el mismo día: una sesión de TeamViewer activa dentro de un host Windows en `192.168.100.165/24` (gateway `192.168.100.254`) — exactamente el subnet del dashboard pfSense "Open - Piedras" (`192.168.100.1`) que había quedado como "no responde" en el relevamiento manual de firewalls — y un export de vCenter propio del sitio, `ExportList-Piedras-Full.csv`, que confirma un tercer host ESXi (`192.168.100.4`, distinto tanto del cluster principal `192.1.1.x` como del segundo host `192.1.3.252`) con 15 VMs propias. El host Windows resultó ser una de esas 15 VMs (`Win10-Piedras`, IP y hostname `DESKTOP-RK7PGB6` coinciden exacto con el export). Detalle completo en `infra/inventory.json` → `meta.piedras_site` / `vms[].site == "Piedras"`, y tabla resumen en `infra/topology.md` §3.

Hallazgos derivados de las 15 VMs de Piedras, todas apagadas salvo `Win10-Piedras` y `DC2`:

- **`DC2` — segundo controlador de dominio.** El sitio principal ya tenía `DC1`; que Piedras tenga su propio `DC2` sugiere que es un sitio de Active Directory replicado, no un entorno aislado — dato a favor de tratarlo como parte del mismo dominio administrativo, no como infraestructura completamente separada.
- **`OpenPiedrasFw01` — candidato fuerte a ser el pfSense que "no responde".** FreeBSD, 2 NICs, nombre explícito. Está *apagada* en el export — probable explicación de por qué el dashboard `192.168.100.1` nunca respondió durante el relevamiento de firewalls. Sigue sin confirmarse por acceso directo (para eso habría que encenderla).
- **`OPENDB_31` — Piedras respalda hacia el datacenter principal, no localmente.** Su nota de backup Veeam apunta al servidor `openbk` / repositorio `openbk.open.solutions` — que es literalmente la VM `OPENBK` del sitio principal (`192.1.1.14` / `10.77.254.114`), no un Veeam local de Piedras. Confirma conectividad activa entre ambos sitios vía `10.77.254.114`.
- **Ruteo Piedras → cluster principal, confirmado en vivo (18 ago 2026).** Desde dentro de la sesión de TeamViewer en `Win10-Piedras` (`192.168.100.165`), `http://192.1.1.38:81/` (el panel de Nginx Proxy Manager de `VM-DOCKER-Clientes`, en el cluster principal `192.1.1.x`) respondió directamente y mostró la pantalla de login de NPM. A diferencia del hallazgo de `OPENDB_31` (inferido de una nota de backup), esto es una prueba de conectividad real, hecha a mano, y confirma que Piedras alcanza por red al segmento `192.1.1.x` — no solo al rango de backups `10.77.254.x`. Implica que el ítem 1 de Tier 1 (mapear las 4 instancias de NPM — ver `plan_relevamiento_alta_cefas.md`) puede arrancarse directamente desde una sesión de TeamViewer a Piedras, sin necesitar un salto intermedio a una VM del cluster principal. Login a NPM todavía pendiente — falta la credencial.
- **`PiedrasDB01` — nota cruzada sin explicar.** Su campo `Notas` contiene solo una IP, `192.1.3.121`, que pertenece al rango del *segundo* host del sitio principal (`192.1.3.252`), no al rango de Piedras (`192.168.100.x`). Podría ser una referencia a una migración o replicación entre sitios — sin confirmar.
- **`COBRA` — posible cliente sin documentar.** Windows Server 2012, DNS `COBRA.open.solutions`, no coincide con ninguno de los 15 clientes conocidos. Mismo patrón por el que se encontró Argocean — no asumir que es un cliente nuevo sin verificar.
- **`PiedrasWL01` es explícitamente de prueba** — su propia nota dice "test weblogix simple licence. 12.2.1.19 y db 23ia", así que no cuenta como WebLogic productivo del sitio.

**`ExportList-Datacenter-Full.csv` — mismo snapshot, sin novedad.** Llegó junto con el export de Piedras como un re-export "más completo" del sitio principal, pero al comparar registro por registro contra `ExportList.csv` (129 VMs) el estado, host e IP de cada VM son idénticos — es el mismo snapshot con columnas extra (Clúster, UUID, DNS, estado de VMware Tools). El campo `Clúster` viene vacío para las 129 VMs, así que no resuelve la pregunta abierta sobre si `192.1.3.252` es un cluster/sitio separado. No aportó VMs ni hallazgos nuevos — se documenta acá para que nadie vuelva a re-parsearlo esperando encontrar algo distinto.

**`OPENPORTAL01` = "portalDM" — resuelto.** La nota de la VM en el export enriquecido dice "portal prod DM" con IP `10.77.10.5 (ex 192.1.3.22)` — coincide exactamente con la entrada de Relevamiento para `portalDM` que quedaba sin confirmar (ver "Todavía abierto" más abajo, antes ítem 2). Es el portal de producción de **Maipú** (mismo prefijo "DM" que `DMWL01`/`DMFW01`, y su job de backup Veeam es `2026_DiagMaipu`).

**`OPENDOCKER03` — resuelto (ya estaba explicado, ahora sin ítem abierto pendiente).** La IP que Relevamiento le atribuye (`192.1.1.113`) no aparecía en el ExportList.csv original porque la VM estaba apagada al momento del export y vCenter solo reporta IP de invitado con VMware Tools corriendo. El relevamiento de Docker la confirma de forma independiente en esa misma IP (Ubuntu 22.04, Portainer). La nota del export enriquecido confirma la IP una tercera vez.

**`WEBSERVER` — rol confirmado.** La nota dice: corre Docker, con contenedores para `help.open.com.ar` (probablemente un wiki/portal de ayuda — el nombre del contenedor es `documize`), `nagios` (monitoreo) y `ppc-prod`. No es un frontend nginx (esa conjetura ya se había descartado) ni un servidor web genérico — es un host de utilidades internas.

**`OPENWLCLI01` — aclarado.** La nota dice "(se clono de la 10.77.7.201) dejar apagada" — es un clon intencionalmente apagado de `OPENWLPROD01` (WebLogic de producción de EBY/GIAR), no un servidor productivo separado. Coherente con que figura "Apagado" y 0 días de actividad en el export.

**`OpenRepo` — aclarado.** La nota dice "en teoria no se usa mas x migracion a git" — repositorio de código legado, reemplazado por un sistema de control de versiones git. VM apagada, sin uso activo esperado.

**nginx — resuelto.** `Relevamiento_general_infraestructura_Docker_actualizado.docx` confirma que Nginx Proxy Manager corre como contenedor en 4 de los 9 hosts docker: `DOCKER-DEB`, `OPENDOCKER04`, `VM-DOCKER-Clientes`, y `VM-DOCKER-Clientes (1)`. No es una categoría de host dedicado — es un servicio containerizado. La conjetura anterior `web_frontend_nginx_candidate` (`OPENPORTAL01`, `OPENPORTALCLI02`, `WEBSERVER`) estaba equivocada; el rol real de esas VMs sigue sin confirmar.

**pfsense — confirmado para 7 de 9 hosts.** `Analisis Azure.docx` (ver `cloud-infra/source/`) ya había confirmado `OPENVPNFW01` (VPN Site-to-Site de Azure, peer "pfSense" en `200.55.243.92`). `source-files/Relevamiento (sin claves) - Pfsense.csv` — un relevamiento manual con acceso real al dashboard de cada firewall — confirma 6 más: `CliProFw01`, `DMFW01`, `FW`, `FWOPEN`, `OPENFWCLI001`, `OPENFWCLI10`. Cada uno tiene su IP de dashboard, credencial de acceso (usuario `smartsouth`, sin contraseña en el archivo) y una etiqueta de destino — por ejemplo `DMFW01` aparece etiquetado "Open - Maipu", consistente con ser el firewall dedicado de ese cliente. Solo quedan sin confirmar `OPENFWCLI02` y `VM_FW`. Detalle completo en `inventory.json` → `vms[].confirmation`.

**Corregido: la exposición a Internet NO es mínima — el "hallazgo positivo" anterior era incorrecto (19 ago 2026).** El relevamiento manual de firewalls había anotado "No hay puertos TCP expuestos... el único servicio escuchando desde Internet es OpenVPN en el puerto 2190 (UDP)". La captura real de la tabla Firewall → NAT → Port Forward de `FWOPEN` (`192.1.1.11`) contradice esto directamente: **al menos 19 reglas de NAT activas en WAN1**, la mayoría TCP, ver detalle completo en `inventory.json` → `vms[FWOPEN].nat_rules`. Entre lo más sensible:

- **`vcenter` — puerto WAN `667` → `192.1.1.29:443`.** La interfaz de administración de vCenter (la herramienta que gestiona los 12 hosts ESXi del cluster principal) está expuesta a Internet. Es la exposición más seria de la lista.
- **`firewall` — puerto WAN `666` → `192.1.1.11:443`.** El propio `FWOPEN` expone su panel de administración HTTPS hacia afuera (aparece duplicada en la captura).
- **Múltiples apps de cliente con NAT directo, sin pasar por NPM:** `192.1.1.191` (`WebLogic.191`) recibe tres puertos WAN distintos hacia el mismo destino interno `:80` — `8089` ("Mafisa Publicacion"), `8088` ("NAT DVAL"), `8191` ("UIA Weblogic") — cada cliente con su propio puerto externo en vez de un dominio.
- **Correo (Exchange):** `192.1.1.10:25/587/993` — un servidor de mail que no está en ningún export de vCenter (ver blind spot nuevo, abajo).
- **Confirma las dos NAT reales de los NPM que ya veníamos mapeando:** `200.55.243.94:80/443` → `192.1.1.37` (`DOCKER-DEB`) y `WAN1:80/443` → `192.1.1.38` (`VM-DOCKER-Clientes`) — **esta última es la ruta real de entrada de CEFAS**, cierra la capa 3 de `plan_relevamiento_alta_cefas.md`.

No se revisó ningún otro firewall todavía — no se puede asumir que esto es exclusivo de `FWOPEN` ni que los demás (`FW`, `CliProFw01`, etc.) están mejor. Vale la pena repetir este chequeo en los otros pfSense confirmados antes de dar por cerrado el tema.

**Rex Argentina — primera infraestructura identificada (19 ago 2026).** La misma tabla de NAT de `FWOPEN` tiene dos reglas "SS Rex Front" (puerto WAN `4431` → `192.1.1.57:80`) y "SS Rex Back" (`4433` → `192.1.1.57:4433`). `192.1.1.57` es `OPENDOCKER.57`, que el relevamiento de Docker ya listaba con un servicio `SelfRex` sin más contexto — ahora se confirma qué es: el mismo patrón "Self Service" que CEFAS (`ss_front_cefas`/`ss_back_cefas`), pero para Rex. Es la primera infraestructura real que se le puede asociar a Rex Argentina desde que se lo descubrió — avanza el ítem 2 de Tier 1 en `PLAN.md`, aunque todavía sin identificar la DB.

**Dos clientes/apps nuevos, sin ningún rastro en ningún otro documento: "Mafisa" y "UIPBA".** `Mafisa Publicacion` (NAT directo a `WebLogic.191:80`) y `Cliente UIPBA` (`192.1.1.17:9001`, más `UIA DB 1521` en la misma IP puerto `1521` — un listener Oracle). **Ninguna de las dos IPs de UIPBA/UIA DB (`192.1.1.17`) ni la de mail (`192.1.1.10`) aparecen en ExportList.csv** — agregado como blind spot nuevo en `inventory.json`. Mismo patrón de "cliente sin documentar" que Argocean, pero esta vez ni siquiera vCenter los ve — podrían ser hosts físicos o de un cluster/vCenter distinto al relevado.

**`DB-ARGOCEAN` — resuelto.** Es la base de datos de un cliente llamado **Argocean**, según la hoja Discrepancias de la matriz ("DB 172.18.5.60 / SID MBA — No figura [en tabla funcional]"). Argocean no está en la lista original de 13 clientes en absoluto — apareció solo en las notas de discrepancias. La VM está actualmente apagada en ExportList.csv. WebLogic identificado el 19 ago 2026 — ver hallazgo de `VM-DOCKER-Clientes` más arriba.

**GIAR — versión de WebLogic resuelta (16 ago 2026).** Verificado por TeamViewer entrando a `http://10.77.7.201:7001/console/login/LoginForm.jsp` (`OPENWLPROD01`): la página de login muestra "Versión de WebLogic Server: **12.2.1.4.0**". Confirma el valor del inventario técnico (`12.2`); el valor funcional ("WL 11") era el desactualizado. No se pudo entrar a la consola en sí — la cuenta compartida (`soportesmart`) fue rechazada, así que solo se confirmó la versión desde la pantalla de login, no el resto del dominio. **Gotcha a tener en cuenta:** `10.77.7.201` (GIAR) y `10.77.10.101` (Maipú) se confunden fácil al leerlas rápido — casi se registra este dato contra el cliente equivocado.

## Nuevo: los "13 clientes" son más bien 15, con bajas

La matriz (`Matriz_servicios_por_cliente_Hosting_V2.xlsx`) agrega dos clientes que no estaban en el CSV de Relevamiento:

- **Rex Argentina** (código `279`) — "incorporado recientemente al hosting", entorno PROD, usa Condor Work, DB referenciada solo como "REX Producción" (todavía sin resolver a una VM específica — necesita una búsqueda por TeamViewer, ~15 usuarios / 4.600 legajos según las notas de la matriz).
- **Argocean** — ver arriba, por ahora solo con DB, sin WebLogic identificado.

Más importante todavía, aunque con una fuente más floja de lo que parece a primera vista: **una nota suelta en la matriz dice que ABB S.A. y Arris de Argentina S.A. (GIAR) ya son clientes dados de baja**, con sus bases de datos retenidas solo temporalmente. Esa nota está pegada debajo de la tabla principal del Excel (no es un campo formal), y dice textual "Estos dos clientes estan de baja pero por el momento se mantiene sus bases". El problema: el campo formal `Estado / migración` de esos mismos dos clientes, en la misma planilla, dice **"Mantenimiento solamente"** — no "de baja". No son necesariamente contradictorios (podría estar en mantenimiento mientras se lo da de baja), pero es una nota informal contradiciendo, o al menos matizando, un campo estructurado — no algo para dar por hecho. Confirmar con el equipo saliente antes de tratar a cualquiera de los dos como cliente de baja prioridad. (Ambos siguen apareciendo en `used_by_clients` en inventory.json de todas formas, ya que sus VMs todavía existen y están encendidas.)

## Nuevo: la matriz documenta sus propias discrepancias sin resolver

La hoja "Discrepancias" de la matriz es el propio equipo de Open detectando conflictos entre una fuente "funcional" (de una persona llamada Fran) y el inventario "técnico" — vale la pena llevarla adelante tal cual en vez de re-derivarla:

| Cliente | Tema | Dice la fuente funcional | Dice el inventario técnico |
|---|---|---|---|
| ROMAN (CSM) | Versión de WebLogic | WL 11 | 10 |
| JOBS | Versión de WebLogic | WL 12 | 11 |
| ABB | DB actual | `192.1.1.31` / SID ABB | `192.1.1.31` y `192.1.1.190` (dos IPs) |
| CEFAS | SID | CEFASPDB | CEFAS |
| BOCA | SID | BOCAPDB | BOCA |
| EBY | Servidor WebLogic | destino `10.77.7.201` | actual, compartido: `192.1.2.54` |
| Enerflex | Charset | `WE8ISO8859P15` | sin verificar — marcado como posible error de tipeo |

El documento explícitamente **no** eligió un valor por sobre el otro cuando las fuentes no coincidían — la misma política que debería seguir este proyecto. Ver `clients[].matrix_detail` en inventory.json para el detalle completo por cliente (SID, versión/edición/tamaño/charset de DB, destino de migración, qué productos Condor usa cada cliente).

## Todavía abierto

### 1. El alcance de Relevamiento (solo WL+DB) implica que ausencia ≠ cliente nuevo

(Ver análisis previo, sigue vigente.) Solo 2 de los 13 clientes originales muestran una coincidencia de nombre para infraestructura adicional sin documentar: **GIAR** tiene una instancia extra `WL-GIAR` además de su `OPENWLPROD01`/`DB-GIAR` mapeados, y **ROMAN** tiene un `WL-ROMAN` y un `DB-ROMAN-HISTORICO` extra además de su `WL-CLIENTES`/`DB-ROMAN` mapeados. Otras ~31 VMs con patrón WL/DB no llevan ningún código de cliente en el nombre y todavía necesitan una pasada por TeamViewer para clasificarlas como: otro componente de un cliente conocido, una copia de dev/QA/test, o un cliente genuinamente separado (como resultó ser Argocean).

### 2. Segundo host ESXi (`192.1.3.252`) — probablemente un sitio separado

Todavía sin confirmar si es una segunda ubicación física o una máquina standalone; aloja la infraestructura de GIAR y ROMAN que no está en el rango `192.1.1.x`.

### 3. Nombres de posibles clientes sin trackear, vistos en las Notas del export enriquecido (18 ago 2026)

Ninguno de estos está en la lista de 15 clientes conocidos (13 de Relevamiento + Rex Argentina + Argocean). Ninguno está confirmado — son nombres sueltos en anotaciones manuales o en listas de esquemas/tablespaces de Oracle, que pueden ser clientes reales, entornos legados, o nombres internos sin relación con un cliente. Priorizar por fuerza de la señal:

- **Señal más fuerte — lista explícita de clientes.** La nota de `VPNCLIENTESRDP` (VM de acceso RDP, IP `192.1.1.136`) dice: "RDP de acceso a clientes no compatibles localmente — Allianz, Monroe, Rovella, SMQ, NABSA". Es una anotación humana enumerando clientes por nombre, no un nombre de esquema — la fuente más confiable de esta pasada. Ninguno de los cinco tiene VM mapeada todavía.
- **Señal más débil — nombres de esquema/tablespace de Oracle**, que podrían ser clientes o podrían ser artefactos internos/legados: `Electroban` y `Pietroboni` (notas de `CLIENTES-DB`/`CLIENTES-DB2`), `PVINST`/`VINST` (aparece igual en `CLIENTES-DB` y en `Database-24`, lo que sube algo la confianza de que sea el mismo nombre real), `CITEDEF` (nota de `Database .23` — coincide con el nombre de un organismo real, Centro de Investigaciones Científicas y Técnicas para las Fuerzas Armadas), `aqualum`/`abbhist`/`abbtubio` (nota de `DBClientes-12C.31` — los dos últimos parecen sub-esquemas de ABB, no clientes nuevos), y `SIGO`/`NQN`/`DBRHGOB` (notas de `OPENDBPR001` y `Database12c .18`).

Antes de tratar cualquiera de estos como cliente nuevo, confirmar por TeamViewer o preguntándole al equipo saliente — mezclar nombre de esquema con nombre de cliente fue justo el tipo de error que este proyecto viene evitando a propósito.

### 4. Posible tercera IP para EBY, pero la nota que la menciona es sospechosa de estar contaminada

El cruce de IPs (`cross_reference_ips.py`) contra los nuevos exports encontró `10.77.8.201` dentro de la nota de `OPENWLCLI01` (el clon apagado de `OPENWLPROD01`/`10.77.7.201`): "EBY PRODUCCION / 10.77.8.201 / (se clono de la 10.77.7.201) dejar apagada / EBYPROD / CEFAS / GIARG / test weblogix simple licence. / 12.2.1.19 y db 23ia". A primera vista es una tercera IP candidata para la ruta de EBY (Tier 1, ítem 3 de `PLAN.md`), pero la misma nota mezcla textualmente varios clientes sueltos (EBY, CEFAS, GIARG) y termina con **la frase idéntica, palabra por palabra**, a la nota propia de `PiedrasWL01` ("test weblogix simple licence. 12.2.1.19 y db 23ia") — una VM completamente distinta, en el sitio Piedras. Esa coincidencia exacta de texto entre dos VMs no relacionadas es la señal más fuerte hasta ahora de que la columna `Notas` del export tiene contenido pegado/desplazado entre celdas, no una anotación fiable celda-por-celda. Tratar `10.77.8.201` como un dato a verificar, no a asumir, al resolver el ítem 3 de Tier 1 — y tener la misma cautela con cualquier otra nota de `OPENWLCLI01` de acá en más.

También apareció `10.10.10.1` en la nota de `FWOPEN` (junto a sus otras IPs conocidas) — sin indicio de qué es; baja prioridad.

## Pendiente para la reunión con el equipo saliente

- **Credencial de acceso a `CLIENTES-DB` (`192.1.1.32`)** — bloqueado desde el 25 ago 2026: ni SSH ni consola VMware/Veeam aceptan acceso, tampoco la credencial que sí funciona en `WebLogic.191`/`VM-DOCKER-Clientes`. Es lo único que falta para cerrar capa 5 de `plan_relevamiento_alta_cefas.md` (confirmar el SID real, `CEFAS` vs `CEFASPDB`) — mismo patrón de bloqueo que SSH/Portainer/NPM al inicio del proyecto, ya resueltos con acceso directo.

## Próxima pasada sugerida por TeamViewer (en orden de prioridad)

1. Confirmar el estado real de ABB y GIAR (¿de baja, o solo "mantenimiento"? la fuente se contradice, ver arriba) antes de bajarles prioridad a sus entornos.
2. Resolver la identidad de la VM de base de datos de Rex Argentina — es un cliente PROD actual sin VM mapeada todavía.
3. Confirmar o descartar pfsense en las 2 VMs `firewall_candidate_pfsense` que quedan (`OPENFWCLI02`, `VM_FW`) — las otras 7 ya están confirmadas.
4. Recorrer las 12 VMs `infra_generic_unclear` (`OPENINFRxx`) — sin ninguna señal en el nombre.
5. Para las ~31 VMs con patrón WL/DB todavía sin mapear, clasificar cada una como producción-de-un-cliente-sin-documentar vs. dev/QA/test/histórico vs. componente de un cliente conocido.
6. Confirmar el rol real de `OPENPORTAL01`, `OPENPORTALCLI02`, `WEBSERVER` ahora que se sabe que la conjetura de nginx para ellos era incorrecta.
7. Confirmar si Allianz, Monroe, Rovella, SMQ y NABSA (nombrados en la nota de `VPNCLIENTESRDP`) son clientes activos sin VM mapeada, o clientes ya dados de baja que solo quedaron en una nota vieja.
8. Si hay oportunidad de entrar a vCenter directamente (no solo al export CSV), anotar la fecha de creación ("Created"/`Config.CreateDate`) de `DASADBPROD01`, `OPENDBPROD006` y `OL8LABWL01` — son los tres candidatos a incorporación/migración más reciente (ver hallazgo arriba), y una fecha real cerraría la pregunta en vez de inferirla por uptime.
9. **Sitio Piedras, ahora que está confirmado:** identificar `COBRA` (¿cliente sin documentar o nombre interno?), entender la nota cruzada `192.1.3.121` en `PiedrasDB01`, y — si hace falta confirmar `OpenPiedrasFw01` como el pfSense de Piedras — encenderla y entrar al dashboard.
