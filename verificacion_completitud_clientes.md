# Verificación de completitud del relevamiento — por cliente

**Fecha:** 14 sep 2026 (sesión de ESYOP, ver `plan_relevamiento_alta_esyop.md`). **ESYOP: de ~46% a ~95%, 6/7 capas cerradas en una sola sesión.** Elegido por estar debajo del 50% de completitud, con el camino más allanado del grupo (capa 3 ya cerrada, capa 6 a ~0.7 de la sesión de ABB). El resultado superó lo esperado: `WebLogic.19` (`192.1.1.19`), terreno nunca tocado, resultó tener Forms & Reports 11.1.1 real (`ClassicDomain`) con **una sesión de usuario real en curso en el momento exacto de la consulta** — `GET /forms/frmservlet?config=esyop` desde un puesto de trabajo del cliente (`ifhost=PC-GUIDO`/`192.168.2.84`), 4.439 hits de `lservlet` en el día (no bots), `formsweb.cfg` con sección `[esyop]` dedicada (`pageTitle=Condor Work ES&OP`). Capa 6 subió a 1.0 sin `sqlplus`: un `netstat -tn` corrido mientras la sesión seguía viva capturó `192.1.1.19:25918 → 192.1.1.31:1521 ESTABLISHED` — la DB ya conocida, con conexión real observada en el instante, en simultáneo con la sesión de capa 4. Capa 7 cerrada como "no aplica" (único NFS en `/etc/fstab` comentado, sin montar); capas 1/2 también "no aplica" (acceso directo por NAT de `FWOPEN`, sin dominio ni NPM). Único cabo suelto: capa 5 (Jasper) — la matriz lo marca `Sí` pero sin referencia en `formsweb.cfg`, contenedor sin identificar, baja prioridad.

**Fecha:** 13-14 sep 2026 (sesión de Argocean, ver `plan_relevamiento_alta_argocean.md`; sincronizado desde `infra/findings.md`, no estaba volcado en este doc todavía). **Argocean: de ~29% a ~47%.** Capa 6 cerrada por la evidencia más dura posible, sin necesitar sesión: `DB-ARGOCEAN` figura **`Apagada`** en `ExportList.csv` (vCenter, la fuente más confiable del proyecto) — no puede haber instancia Oracle corriendo en una VM apagada. Capa 3 cerrada por capturas de pantalla en vivo del dashboard de `FW` (`192.1.3.1`): las 5 reglas de Argocean/ROMAN están habilitadas, con un hallazgo de exposición real — el alias `Argocean_IP_Publicas` es **letra muerta en la práctica**, porque una regla previa sin restricción de origen, para el mismo destino (`WAN:8080` → `172.18.5.40:80`), ya deja pasar cualquier IP antes de que se evalúe el alias. **Capa 4 queda bloqueada, no resuelta:** cuatro vías de acceso independientes a `WL-CLIENTES` (`172.18.5.40`, confirmado `Powered On` con IP en vivo en vCenter) fallaron por timeout — SSH directo desde la estación Windows, SSH vía NAT pública del `FW` (`200.55.243.116:215`), consola web de vCenter, y SSH administrativo al propio `FW`. Queda un único camino sin probar correctamente: el `Diagnostics > Command Prompt` embebido en el dashboard de pfSense (los intentos previos de `ping`/`ssh` se corrieron por error en la PowerShell de Windows, no en esa consola del propio firewall).

**Fecha:** 12/13 sep 2026 (cierre de capa 6 de BOCA — trazado completo 7/7 —, cierre de capa 1 de ROMAN, y **cierre de capa 6 de EBY — trazado completo 7/7**; base 6 sep, sesión de BOCA + Rex Argentina + ROMAN). **EBY cerrado del todo:** `sqlplus` a `Database .90`/`CDRADM` (`root@192.1.1.90`) confirmó que esa DB compartida **no** es de EBY — de los 8 inquilinos de `WebLogic.191`, solo `SIGO` tiene schema propio ahí (2.6 GB, uso activo el mismo día). La ruta paralela `yacyreta` queda descartada de DB propia; la ruta productiva real (`eby-prod`) ya estaba cerrada por `tnsnames.ora` oficial. EBY de ~90% a **100%** — sexto cliente con las 7 capas verificadas en vivo (tras CEFAS, JOBS, ABB, BOCA... en rigor el tercero con trazado 100% junto a CEFAS/JOBS, sumando ABB y BOCA como "verificado hasta donde hay tráfico"). Bono: resuelve el blind spot de **SIGO** (uno de los 4 clientes de `.191` sin VM mapeada), que gana su primera DB confirmada en vivo del proyecto. **ROMAN — capa 1 cerrada, sin tráfico real:** acceso a `DOCKER-DEB` (NPM en contenedor `ssl-app-1`) — `proxy-host-93_access.log`/`proxy-host-94_access.log` (`romanprod`/`romanqa`) muestran **0 requests `lservlet` en 5.097 requests totales**, 100% escaneo de vulnerabilidades (bots). Junto con el `access.log` de `WLS_FORMS` (6-sep, cero `config=csm`), son **dos fuentes independientes confirmando cero uso real**. Además, la credencial `root` que cerró BOCA (`192.1.1.32`) se probó contra `10.77.7.30` (DB de ROMAN) y **fue rechazada** — capa 6 sigue en 0.9, no 1.0. ROMAN de ~65% a **~70%**. **BOCA cerrado del todo:** `sqlplus` a `CLIENTES-DB` (`192.1.1.32`, `root`) reveló que la instancia `ORACLE_SID=BOCA` es una CDB multitenant — los datos reales viven en la PDB `BOCAPDB` (schema `CONDOR`, ~22.5 GB, charset `WE8MSWIN1252` confirmado), con último DML **el mismo día de la consulta** (uso productivo activo, la mejor evidencia de capa 6 lograda en el proyecto hasta ahora). Resuelve la fila "BOCA / SID" de Discrepancias: `BOCA` y `BOCAPDB` eran las dos correctas a la vez (CDB vs PDB), mismo patrón que Rex/GIAR. BOCA de ~90% a **100%**.

**Fecha (base):** 6 sep 2026 (sesión de BOCA + verificación en vivo de Rex Argentina + trazado formal de ROMAN iniciado; base 2 sep, cierre de EBY). **ROMAN: trazado formal iniciado** — SSH+sudo a `OPENWLPROD01` cerró capa 4 a nivel config (`formsweb.cfg` con secciones `[csm]*` reales → "CSM PRODUCCION", `@PRODCSM`), capa 6 a 0.9 (`PRODCSM`/`QACSM` → `OPENDBPROD03`/`10.77.7.30:1525`; `romanstest`/`OPENDBDES011` descartados de la ruta prod), capa 7 «no aplica», y la versión WL (`12.2.1.4.0`, cierra la fila de Discrepancias). Pero **cero tráfico Forms de ROMAN en 5 días de `access.log`** — configurado como producción, posiblemente dormido; ROMAN de ~48% a ~65%. **Rex Argentina: primera verificación en vivo del proyecto** — `v$session` en `OPENDBPROD001`/`CDBOPEN03` (PDB `PRODREX01`) confirma usuarios nominales reales conectados desde `OL8LABWL01`, cerrando capas 4 y 6 a 1.0 (de ~40% a ~68%). De paso, se confirmó que `PRODGIAR`/`QAREX01` existen como PDB real pero sin tráfico visto en el momento de la consulta, y que `PRODCEFAS`/`QAGIAR` (esperados por el mismo `tnsnames.ora`) directamente no existen como PDB. **BOCA pasó a trazado formal: 6 de 7 capas cerradas** — se accedió `192.1.2.54` (`WL12C-Desarrollo`) por primera vez, corre el Forms productivo de BOCA (`WLS_FORMS`, confirmado por access log) pese al nombre "Desarrollo"; capas 1-5 y 7 cerradas, solo falta capa 6 (`sqlplus` a `CLIENTES-DB`, espera al lunes por la nota "no domingos"). `192.1.2.54` no tiene consola ni sudo → el rédito planeado de cerrar **ABB** de paso no salió ahí, pero **ABB se cerró después por `sqlplus` directo con `root@192.1.1.31`**: 7/7 capas, DB productiva = `192.1.1.31` (instancia `ABB`, schema `CONDOR`), `192.1.1.190` descartada (solo `abbhist`/`abbtubio` históricas). ABB de ~33% a **~90%**. Hallazgo colateral fuerte: **ABB sin una sola sesión Forms ni un DML desde el 1‑jul‑2026** — apagado de hecho (apoya la nota de baja de la matriz; decisión de negocio pendiente en `QUESTIONS.md`). Bonos: capa 6 de **ESYOP** verificada en vivo (co‑inquilina de `.31`, en uso activo — DML del 4 sep) y de **DCVIAJES** parcial (`.190`, viva y en uso). Base previa: EBY con **6 de 7 capas cerradas** (1-3 y 7 ya estaban; 4 y 6 se cerraron para la ruta `eby-prod` vía `formsweb.cfg` + `tnsnames.ora` reales de `OPENWLPROD01`; 5 cerrada como «no aplica» por desk-check), corrección del cierre de JOBS/Enerflex, hallazgos del re-dump de NPM de `DOCKER-DEB` sobre ROMAN/GIAR/MAIPU/HEINLEIN/Rex, y el mismo `tnsnames.ora` de `OPENWLPROD01` destrabando alias de DB para GIAR, ROMAN, Rex Argentina y Heinlein de un saque. De paso: credencial `root` conseguida para `192.1.1.90` (`Database .90` = `CDRADM`, Oracle 11g, DB compartida entre varios tenants de `WebLogic.191` — no exclusiva de EBY); `VINST`/`VINST2025`/`T2022` descartados como clientes (el último es `CONDORERP`, un ERP interno) — el universo sigue siendo 15 clientes.

## Método (igual que la primera vuelta, ahora con la regla explícita)

Se mide cada uno de los 15 clientes contra el mismo modelo de 7 capas que usan `plan_relevamiento_alta_cefas.md` / `plan_relevamiento_alta_jobs.md` / `plan_relevamiento_alta_eby.md` / `plan_relevamiento_alta_boca.md`: dominio de entrada → NPM → firewall/NAT → app motor clásico → app Docker/reportes → base de datos → almacenamiento. Solo CEFAS, JOBS, EBY y BOCA tienen ese trazado formal; el resto es una extrapolación con la misma vara.

Puntaje por capa:

| Puntaje | Significa |
|---|---|
| **1.0** | Verificado en vivo — sesión SSH/consola/`sqlplus`, o evidencia de red directa (`netstat` con tráfico real) |
| **0.9** | Confirmado por configuración oficial del sistema (`formsweb.cfg`/`tnsnames.ora` leídos con `sudo` en el propio WL) pero sin la sesión/`sqlplus` final |
| **0.6** | Evidencia de red fuerte pero sin verificación directa (ej. `netstat` confirma destino, `sqlplus` bloqueado por credencial) |
| **0.5** | Identificado por nombre/IP/dominio (CSV, matriz, o entrada de NPM) pero sin sesión que lo confirme |
| **0.3** | Señal débil, parcial o en disputa (dos fuentes no coinciden, dominio deshabilitado, IP sin VM) |
| **0** | Sin dato |

## Headline

**Global ponderado: ~71%** (progresión: ~43% → ~49% → ~51% → ~54% → ~56% → ~58% → ~63% → ~64% → ~65% → ~66% → ~67% → ~69% → ~71%). Último movimiento (14 sep): **ESYOP** pasó de ~46% a **~95%** — trazado dedicado, primero desde la sesión de bono de ABB. `WebLogic.19` (terreno nunca tocado) resultó tener una sesión de usuario real en curso en el momento exacto de la consulta (`config=esyop`, puesto `PC-GUIDO`), y un `netstat -tn` corrido mientras esa sesión seguía viva capturó la conexión real a la DB (`192.1.1.31:1521 ESTABLISHED`) — capas 4 y 6 cerradas con la evidencia más directa posible, sin necesitar repetir nada en día hábil. Capas 1/2/7 "no aplica" confirmado. Sexto cliente con 6+/7 capas en una sola sesión (tras CEFAS, JOBS, BOCA, EBY, Rex Argentina). Ver `plan_relevamiento_alta_esyop.md`. Antes (14 sep): **Rex Argentina** pasó de ~68% a **100%** — SSH a `OPENDOCKER.57` reveló los nombres reales del módulo Self Service (`rex_frontend`/`rex_backend`/`rex_postgres`, corriendo hace 2 años), cerrando capas 3 (puertos reales = NAT ya conocido de `FWOPEN`), 5 (contenedores confirmados activos) y 7 (storage real de recibos, `/data/selfrex/archivos`, confirmado — y confirmado **roto**, `Stale file handle`, hallazgo operativo activo). Séptimo cliente con las 7 capas verificadas en vivo. Ver `plan_relevamiento_alta_rex.md`. Antes (13-14 sep): **Argocean** pasó de ~29% a **~47%** — capa 6 cerrada por vCenter (DB apagada, evidencia gratis, sin sesión) y capa 3 cerrada por consola en vivo de `FW` (con un hallazgo de exposición real: el control de acceso por alias de IP es letra muerta). Capa 4 sigue bloqueada — cuatro vías de acceso fallaron, queda un único camino sin probar (consola `Diagnostics` embebida del propio pfSense). Antes (12 sep): **EBY** cerró su capa 6 y pasó de ~90% a **100%** — `sqlplus` directo a `Database .90`/`CDRADM` (`root@192.1.1.90`) descartó esa DB compartida como propiedad de EBY (solo `SIGO`, de los 8 inquilinos de `WebLogic.191`, tiene schema real ahí, en uso activo el mismo día). La ruta productiva `eby-prod` ya estaba cerrada por `tnsnames.ora`; con la ruta paralela `yacyreta` descartada de DB propia, EBY queda con las 7 capas resueltas (config oficial + evidencia directa `sqlplus`, sin ambigüedad pendiente). Bono: `SIGO` gana su primera DB confirmada en vivo del proyecto. Antes (12/13 sep): **ROMAN** cerró su capa 1 en `DOCKER-DEB` (`proxy-host-93`/`94`, NPM en contenedor `ssl-app-1`) — **0 requests `lservlet` en 5.097 totales**, coincide con el `access.log` de `WLS_FORMS` ya visto el 6-sep: dos fuentes independientes confirman que ROMAN no tiene un solo usuario real, pese a estar configurado como producción de punta a punta. La credencial `root` que cerró BOCA se probó contra `10.77.7.30` (su DB) y **fue rechazada** — capa 6 sigue en 0.9. ROMAN de ~65% a **~70%**. Antes (12 sep): **BOCA** cerró su capa 6 y pasó de ~90% a **100%** — quinto cliente (tras CEFAS, JOBS, EBY... en rigor el segundo con trazado 100% completo junto a CEFAS/JOBS) con las 7 capas verificadas en vivo. `root@192.1.1.32`, `ORACLE_SID=BOCA` → `v$database` mostró `CDB=YES`: la CDB está casi vacía, los datos reales viven en la PDB `BOCAPDB` (schema `CONDOR`, ~22.5 GB, charset `WE8MSWIN1252` confirmado). Último DML de `CONDOR`: el mismo día de la consulta — uso productivo activo sin ambigüedad, mejor evidencia de capa 6 del proyecto. Resuelve la fila "BOCA / SID" de Discrepancias: las dos fuentes tenían razón (CDB vs PDB), no era una disputa real — mismo patrón multitenant que Rex/GIAR (`CDBOPEN03`). Antes (6 sep): **ABB** de ~33% a **~90%** (trazado completo 7/7 — `root@192.1.1.31` confirma la DB productiva por `sqlplus`/`dba_tab_modifications`; `192.1.1.190` descartada), más los bonos **ESYOP** (~29→~46%) y **DCVIAJES** (~36→~46%), que comparten box de DB con ABB y ganaron capa 6 en vivo. ABB no llega a 100 porque está **apagado de hecho desde el 1‑jul‑2026** — todo lo verificable está verificado, sin tráfico vivo que observar. Antes en la misma tanda: **ROMAN** pasó de ~48% a **~65%** — trazado formal iniciado en `OPENWLPROD01`: capa 4 cerrada a nivel config (secciones `[csm]*` reales en `formsweb.cfg`, "CSM PRODUCCION", `@PRODCSM`), capa 6 a 0.9 (`PRODCSM`/`QACSM` → `OPENDBPROD03`/`10.77.7.30:1525`, `romanstest`/`OPENDBDES011` descartados de la ruta productiva), capa 7 «no aplica», versión WL resuelta (`12.2.1.4.0`, cierra la fila de Discrepancias). No llega más arriba porque **no hay tráfico Forms de ROMAN en 5 días de `access.log`** — está configurado como producción pero puede estar dormido. El salto anterior fue **BOCA**, que pasó de ~43% (extrapolación) a **~90%** (trazado formal, 6/7 capas) en una sola sesión: `192.1.2.54` accedido por primera vez, Forms productivo confirmado ahí por access log, capas Docker y storage cerradas como «no aplica». Después: **Rex Argentina** pasó de ~40% a **~68%** — primera verificación en vivo del proyecto para este cliente: `v$session` en `OPENDBPROD001`/`CDBOPEN03` (PDB `PRODREX01`) muestra usuarios nominales reales (`MAIETA`, `CDELISE`, `MCORREA`, `JBRICEÑO`) conectados vía `frmweb`/`java` desde `OL8LABWL01` — cierra capas 4 y 6 a 1.0, no solo alias de config. Antes: EBY a **6 de 7 capas** (capa 4 por `formsweb.cfg`, capa 6 por `tnsnames.ora` oficial), corrección de scoring en JOBS/Enerflex, y el `tnsnames.ora` de `OPENWLPROD01` destrabando alias de DB para ROMAN, Rex, GIAR y Heinlein sin sesión en cada box.

## Por cliente

| Cliente | % anterior | % ahora | Qué cambió |
|---|---|---|---|
| **CEFAS** | 100 | **100** | Sin cambios — trazado completo, verificado en vivo. |
| **JOBS** | ~90 | **100** *(corrección)* | Ya estaba cerrado (capas 4-7 confirmadas por PID/`netstat`/`tnsnames.ora` el 1 sep) antes de que arrancara esta sesión — la medición anterior lo calificó de "relevando" por error. |
| **ENERFLEX** | ~57 | **~73** *(corrección)* | Comparte `WL12C-PROD`/`CLIENTES-DB2` con JOBS — el mismo cierre de JOBS ya lo verificaba en vivo (`WLS_FORMS1` sirve `enerflex.condorwork.com.ar`, `ORDS-Enerflex` mapeado por PID a `.51`/`.32`/`.24`). Falta el charset propio (fila abierta en Discrepancias) y su capa 5, por eso no llega a 100. |
| **EBY** | ~90 | **100** | **Trazado completo, 7/7 (12 sep 2026).** Capas 1-3, 5 y 7 ya estaban cerradas (rutas de entrada, NPM, firewall, sin storage/Docker dedicado). Capas 4 y 6 cerradas para la ruta productiva `eby-prod` desde el 2 sep (`formsweb.cfg`/`tnsnames.ora` de `OPENWLPROD01`, alias `EBYPROD`/`EBYQA`/`EBYAUDIT` → `10.77.7.15`). **Último cabo cerrado hoy:** `sqlplus` directo a `Database .90`/`CDRADM` (`root@192.1.1.90`) confirmó que esa DB compartida de la ruta paralela `yacyreta` **no es de EBY** — de los 8 inquilinos de `WebLogic.191`, solo `SIGO` tiene schema propio ahí (2.6 GB, DML del mismo día). Sin ambigüedad pendiente: la única DB real de EBY es `OPENDBPROD005`. |
| **GIAR** | ~58 | **~75** | (12/13 sep 2026 — ver `plan_relevamiento_alta_giar.md`). **Capa 3 cerrada:** NAT completo de `FW` confirma el backend real del legado. **Capa 4/6 legado cerradas por ausencia directa:** `WL-GIAR`+`DB-GIAR` `Powered Off` en vCenter — más fuerte que ABB (que seguía encendida sin sesiones). **Capa 4 nuevo cerrada a nivel config:** `formsweb.cfg` real de `OPENWLPROD01` tiene secciones dedicadas `[giargprod]`/`[activaciongiarg]` (`pageTitle=Giarg PRODUCCION`, `userid=@PRODGIAR`) — deployment deliberado, mismo patrón que EBY/ROMAN. **Capa 6 nuevo, señal a favor pero sin sesión en vivo:** `PRODGIAR` tiene schema `CONDOR` (7.5 GB) con procedimientos de negocio recompilados hoy/ayer, más un schema nuevo `MOTOROLA` (posible nombre legado de GIAR/Arris) sin tocar desde mayo. `v$session` filtrado por `con_id` dio cero sesiones — pero la consulta fue en sábado, mismo caveat que ROMAN/DCVIAJES. **Conclusión revisada: GIAR probablemente migró de verdad, no está "de baja"** — falta solo confirmar sesión real en día hábil para llegar a 1.0. |
| **ROMAN** | ~65 | **~70** | (6 sep + 12/13 sep — ver `plan_relevamiento_alta_roman.md`). **Capa 1 cerrada (12/13 sep):** `proxy-host-93`/`94` en `DOCKER-DEB` — 0 `lservlet` en 5.097 requests, todo escaneo de bots. **Capa 4 cerrada a nivel config:** `formsweb.cfg` de `WLS_FORMS` con secciones reales `[csm]`/`[csmerp]`/`[csmFSAL]`/`[csmERPFSAL]` → `pageTitle=CSM PRODUCCION`, `userid=@PRODCSM`; QA `[qacsm*]` → `@QACSM`. Sin managed server propio (comparte `WLS_FORMS` con EBY/GIAR). **Versión WL = `12.2.1.4.0`** — cierra la fila de Discrepancias (era 10 vs 11). **Capa 6 a 0.9, sigue así:** `PRODCSM`/`QACSM` → `OPENDBPROD03` (`10.77.7.30:1525`), charset `WE8ISO8859P1`; `romanstest`/`OPENDBDES011` descartados de la ruta productiva; `ssh root@10.77.7.30` **probado y rechazado (12 sep)** — la credencial que cerró BOCA no sirve en `10.77.7.x`. **Capa 7 «no aplica»** (`/u02/clientes/csm` local, sin NFS). **Conclusión del trazado:** ROMAN está configurado como producción de punta a punta (capas 1/4/6/7) pero **confirmado sin un solo usuario real** — dos logs independientes coinciden. Faltan capas 3 y 5 (bajo costo) y la credencial de DB para el cierre 1.0 de capa 6. |
| **MAIPU** | ~14 | **~20** | Primeros dominios conocidos: `qadmportal.condor.solutions` → `DASADBPROD01` (deshabilitado) y una redirección hacia `dmportal.condor.solutions` (no resuelto todavía). Sigue "solo infraestructura". |
| **HEINLEIN** | ~14 | **~23** | Primer dominio conocido: `heinleintest.condor.solutions` → `OL8LABWL01`, y el `tnsnames.ora` de `OPENWLPROD01` confirma el alias `HEINLEIN_TEST` → `OPENDBPROD001` — pero todo lo conocido de HEINLEIN es ambiente de test, nada de producción. |
| **Rex Argentina (279)** | ~68 | **100** | **Trazado completo, 7/7 (14 sep 2026) — ver `plan_relevamiento_alta_rex.md`.** Capas 4/6 (motor clásico) ya cerradas el 6 sep. **Hoy se cerraron 3/5/7 vía SSH a `OPENDOCKER.57`:** `docker ps -a` reveló los nombres reales del módulo Self Service (`rex_frontend`, `rex_backend`, `rex_postgres`, corriendo hace 2 años) — **capa 3** cierra porque sus puertos reales (`80`/`4433`) coinciden exacto con las reglas NAT ya conocidas de `FWOPEN`; **capa 5** cierra por contenedores identificados y activos en vivo; **capa 6** gana un segundo módulo (`rex_postgres`, DB propia del Self Service, separada de la Oracle `PRODREX01` del motor clásico); **capa 7** cierra confirmando que `rex_backend`/`rex_frontend` montan `/data/selfrex/archivos` para los recibos — y que ese mount está roto ahora mismo (`Stale file handle`), hallazgo operativo real. Séptimo cliente con las 7 capas verificadas en vivo. |
| **BOCA** | ~43 | **100** | **Trazado completo, 7/7 (12 sep 2026).** Capas 1-3 cerradas el 6 sep (`cabj.condorwork.com.ar` → `192.1.2.54:9001` Forms prod vivo por access log; NPM `DOCKER-DEB`; NAT genérico + exposición `:9998`). **Capa 4:** `WLS_FORMS` en `192.1.2.54` sirve BOCA en prod (`base_domain`, F&R `12.2.1.4.0`). **Capas 5 y 7:** no aplica. **Capa 6 cerrada el 12 sep:** `root@192.1.1.32`, `ORACLE_SID=BOCA` → CDB multitenant casi vacía; datos reales en la PDB **`BOCAPDB`**, schema `CONDOR` (~22.5 GB, coincide con los 18G de la matriz), charset `WE8MSWIN1252` confirmado. Último DML de `CONDOR`: **el mismo día de la consulta** — uso productivo activo en vivo, sin ambigüedad. Resuelve Discrepancias "BOCA/SID": las dos fuentes (`BOCA` técnico, `BOCAPDB` funcional) tenían razón, a distinto nivel. |
| DVAL | ~43 | 43 | Sin cambios esta sesión. |
| **ABB** | ~33 | **~90** | Trazado completo 7/7 (6 sep, `plan_relevamiento_alta_abb.md`). Capas 1-3 sólidas (dominio `abb.condorwork.com.ar` id 28 → `192.1.2.54:9001`; NPM `DOCKER-DEB`; NAT genérico + exposición `:9998` compartida con BOCA). **Capa 4** por herencia de BOCA (`WLS_FORMS`, `base_domain` F&R `12.2.1.4.0`, config `ABB` responde 200). **Capa 5 y 7 «no aplica»** (sin contenedor `abb`; sin `/clientes/abb`; matriz sin Jasper/Condor Link). **Capa 6 a 1.0:** `root@192.1.1.31` → instancia `ABB` (non-CDB, `READ WRITE`), schema de app `CONDOR` (~6.9 GB), charset `WE8ISO8859P1` (= matriz); `dba_tab_modifications` da último DML de `CONDOR` = 01/07/2026 17:24, que calza exacto con la última sesión Forms del access log. **`192.1.1.190` descartada** (sin instancia `ABB`, solo `abbhist`/`abbtubio` apagadas). Discrepancia "ABB / DB actual" cerrada a favor de la fuente funcional. No llega a 100 porque el cliente está **apagado de hecho desde el 1‑jul‑2026** (cero sesiones/DML desde) — todo lo verificable está verificado, pero no hay tráfico vivo que observar. |
| **DCVIAJES** | ~36 | **~46** | Bono del trazado de ABB (`192.1.1.190` era el otro candidato de su DB). `root@192.1.1.190`: DB `DCVIAJES` viva (`ora_pmon_DCVIAJES`, la única instancia levantada del box), `READ WRITE`, `NOARCHIVELOG`, charset `WE8ISO8859P1` (= matriz), schema `CONDOR` con último DML 04/09/2026 → **en uso activo**. **Capa 6 a ~0.6** — DB confirmada viva y en uso, pero sin `v$session` desde `WebLogic.191` capturada (sábado) y AWR vacío. |
| **ESYOP** | ~46 | **~95** | **Trazado dedicado, 6/7 capas (14 sep 2026) — ver `plan_relevamiento_alta_esyop.md`.** Terreno nuevo (`WebLogic.19`, `192.1.1.19`, nunca tocado) resultó tener una sesión de usuario real en curso en el momento exacto de la consulta: `config=esyop` desde `ifhost=PC-GUIDO`/`192.168.2.84`, 4.439 hits de `lservlet` en el día, `formsweb.cfg` con sección `[esyop]` dedicada (`pageTitle=Condor Work ES&OP`). **Capa 6 a 1.0** sin `sqlplus` — `netstat -tn` capturó `192.1.1.19:25918 → 192.1.1.31:1521 ESTABLISHED` en simultáneo con la sesión Forms activa. Capa 7 "no aplica" (NFS comentado, sin montar); capas 1/2 "no aplica" (acceso directo por NAT de `FWOPEN`, sin dominio). Solo falta capa 5 (Jasper — matriz dice `Sí`, sin referencia en `formsweb.cfg`, contenedor sin identificar). |
| **Argocean** | ~29 | **~47** | (13-14 sep 2026, ver `plan_relevamiento_alta_argocean.md`). **Capa 6 cerrada, gratis:** `DB-ARGOCEAN` figura `Apagada` en `ExportList.csv` — la fuente más confiable del proyecto, sin necesitar TeamViewer. **Capa 3 cerrada:** captura de pantalla en vivo del dashboard de `FW` confirma las 5 reglas de Argocean/ROMAN habilitadas — pero con hallazgo de exposición: el alias `Argocean_IP_Publicas` es letra muerta, una regla previa sin restricción ya deja pasar cualquier IP para el mismo destino. **Capa 4 bloqueada:** `WL-CLIENTES` (`172.18.5.40`) figura `Powered On` con IP en vivo, pero 4 vías de acceso independientes (SSH directo, SSH vía NAT del `FW`, consola vCenter, SSH al propio `FW`) fallaron por timeout. Queda un único camino sin probar: `Diagnostics > Command Prompt` embebido en pfSense. Ruta de dominio (capa 1) deshabilitada desde antes. |

## Por capa (los 15 clientes)

| Capa | Antes | Ahora | Por qué se movió |
|---|---|---|---|
| 1 · Dominio de entrada | ~70% | **~82%** | EBY (10 rutas), ROMAN, GIAR, MAIPU, HEINLEIN y Rex ganaron dominios nuevos del re-dump de `DOCKER-DEB`; BOCA cerrada (`cabj.condorwork.com.ar` confirmado prod vivo por access log); **ROMAN cerrada (12/13 sep)** — `proxy-host-93`/`94` de `DOCKER-DEB`: 0 tráfico real, resolución negativa pero definitiva (dos logs independientes coinciden). |
| 2 · Nginx Proxy Manager | ~35% | **~44%** | Mismo re-dump — EBY, ROMAN y BOCA identificados con confianza. Sigue faltando transcribir `OPENDOCKER04` y la réplica `VM-DOCKER-Clientes (1)`. |
| 3 · Firewall / NAT | ~55% | **~62%** | EBY y BOCA confirmaron el patrón genérico (sin regla dedicada). BOCA sumó un hallazgo de exposición: `FWOPEN` regla `test` → WAN `9998` → `192.1.2.54:9001`. **Argocean cerrada (13-14 sep)** — consola en vivo de `FW`, con un segundo hallazgo de exposición: el alias de IPs permitidas es letra muerta en la práctica. **Rex Argentina cerrada (14 sep)** — puertos reales de los contenedores (`80`/`4433`) coinciden exacto con las reglas NAT ya conocidas de `FWOPEN`, cierra el lazo firewall↔servicio real. |
| 4 · App (motor clásico) | ~55% | **~80%** | JOBS/Enerflex re-contados como verificados; EBY capa 4 cerrada para `eby-prod`; **BOCA capa 4 cerrada** (`192.1.2.54` accedido, `WLS_FORMS` sirve prod, dominio `base_domain` F&R 12.2.1); **ROMAN capa 4 cerrada a nivel config** (`formsweb.cfg` de `OPENWLPROD01` con secciones `[csm]*` reales, `12.2.1.4.0`); **Rex Argentina capa 4 a 1.0** (`OL8LABWL01` confirmado en vivo por sesiones `frmweb` reales en `PRODREX01`). |
| 5 · App Docker / reportes | ~20% | **~40%** | EBY y BOCA cerradas como «no aplica» — BOCA por `docker ps -a` negativo en `OPENDOCKER01` (`cabjjasper` ruta muerta). Resolución positiva de capa, no señal indirecta. ROMAN sin trazar todavía. **Rex Argentina cerrada (14 sep)** — `docker ps -a` en `OPENDOCKER.57` confirma `rex_frontend`/`rex_backend`/`rex_postgres` activos hace 2 años, resolución positiva real. |
| 6 · Base de datos | ~50% | **~85%** | JOBS/Enerflex verificados por PID; EBY a 1.0 en las dos rutas (`eby-prod` por `tnsnames.ora` oficial, `yacyreta` descartada por `sqlplus` directo a `Database .90`/`CDRADM` — solo `SIGO` tiene schema ahí); **ROMAN a 0.9** — `tnsnames.ora` leído en vivo (`PRODCSM`/`QACSM` → `OPENDBPROD03`/`10.77.7.30:1525`), `romanstest`/`OPENDBDES011` descartados de la ruta prod, charset confirmado; falta `v$session`. GIAR con alias sin sesión. **Rex Argentina a 1.0** — `v$session` en `PRODREX01` con usuarios nominales reales. **ABB a 1.0** — `root@192.1.1.31`, instancia `ABB`/schema `CONDOR`/charset confirmados, último DML calza con el access log; `192.1.1.190` descartada. **BOCA a 1.0 (12 sep)** — `root@192.1.1.32`, CDB `BOCA` casi vacía, datos reales en la PDB `BOCAPDB`/schema `CONDOR` (~22.5 GB), charset `WE8MSWIN1252` confirmado, DML del mismo día — uso activo sin ambigüedad. **DCVIAJES a ~0.6** — bono del trazado de ABB: instancia viva, charset y schema `CONDOR` confirmados en vivo, DML del 4 sep (en uso); falta la sesión desde su WL. Credencial `root` para `192.1.1.90` sumó `sqlplus` directo (`CDRADM`). **ESYOP a 1.0 (14 sep)** — trazado dedicado, `netstat -tn` en `WebLogic.19` capturó la conexión real hacia `192.1.1.31:1521` en simultáneo con una sesión Forms activa. **Argocean a 1.0 (13-14 sep)** — `DB-ARGOCEAN` apagada, confirmado por `ExportList.csv` (vCenter), sin necesidad de sesión. |
| 7 · Almacenamiento | ~10% | **~31%** | ROMAN se suma a EBY/BOCA/CEFAS — todos confirmando que **no aplica** (sin mount NFS dedicado; `/u02/clientes/csm` local). *Nota: la "capa 7" de JOBS no es storage sino "ambientes no productivos" — no comparables, ver los planes fuente.* **Rex Argentina cerrada (14 sep) — sí aplica, y está rota:** `docker inspect` confirma que `rex_backend`/`rex_frontend` montan `/data/selfrex/archivos` para los recibos; `df -h` da `Stale file handle` sobre ese mismo mount. Primer caso de capa 7 confirmada como storage real, no "no aplica" — y confirmada con un problema activo. |

## Caveats (se mantienen los de la primera vuelta)

- CEFAS, JOBS, BOCA y EBY tienen las 7 capas cerradas y verificadas en vivo (EBY desde el 12 sep, con la ruta `yacyreta` descartada de DB propia por `sqlplus` directo); ROMAN tiene trazado formal iniciado (capas 4/6/7 a nivel config, 1-3 y 5 pendientes) — el resto es extrapolación con la misma vara, no medición directa.
- ROMAN quedó a ~70% con capas 1/4/6/7 cerradas (1 y 7 en vivo/definitivas, 4 y 6 *a nivel config*) pero **confirmado sin un solo usuario real**: `access.log` de `WLS_FORMS` (0 `config=csm`) y `access.log` del NPM de `DOCKER-DEB` (0 `lservlet` en 5.097 requests) coinciden. Está configurado como producción de punta a punta pero dormido. La credencial de DB (`root@10.77.7.30`) se probó y fue rechazada (12 sep) — capa 6 sigue sin sesión `sqlplus`. Si se cuenta solo lo verificado con acceso directo a la DB, ROMAN baja a ~55%.
- La diferencia de **confianza** ya no aplica a EBY del mismo modo: su capa 6 sigue cerrada por `tnsnames.ora` oficial para la ruta `eby-prod` (config, no `sqlplus` a `OPENDBPROD005` — sigue sin credencial ahí), pero la ruta paralela `yacyreta` ya no es ambigüedad sin resolver — `sqlplus` directo a `Database .90` la descartó con evidencia directa. El bloqueo de credencial de DB dejó de ser total: `root` funciona en `192.1.1.90` y `192.1.1.31`/`192.1.1.32` (segmento `192.1.1.x`), pero sigue rechazado en `10.77.7.x` (`.15`, `.30`, `.151`) — ver `QUESTIONS.md`.
- Los clientes "planos" restantes (DVAL, DCVIAJES) no tuvieron sesión nueva esta ronda — su score sigue siendo la extrapolación original (DCVIAJES con el bono de capa 6 de la sesión de ABB), no una remedición. **ESYOP** salió de esa lista con trazado dedicado (14 sep 2026, 6/7 capas). **ABB** pasó a trazado completo (7/7). **Argocean** también salió de la lista (13-14 sep): capas 3 y 6 con evidencia directa (consola en vivo + vCenter), pero **capa 4 queda como bloqueo de conectividad real, no como falta de sesión** — cuatro vías de acceso distintas fallaron contra un host que vCenter marca encendido. Si el último camino (consola de pfSense) tampoco responde, es evidencia dura de un host aislado de red pese a figurar prendido.
- ROMAN, GIAR, HEINLEIN y Rex ganaron *evidencia de dominio y de alias de DB en un `tnsnames.ora` oficial*, no verificación en vivo — es config declarada en el WL, un escalón más fuerte que "dominio conocido", pero todavía nadie confirmó procesos/tráfico/sesiones de esos clientes en los boxes. No tratar el alias TNS como "capa 6 resuelta" sin más: es el mismo tipo de salto que ya corrigió el caso EBY para la ruta `yacyreta` (config ≠ sesión verificada). MAIPU sigue solo con evidencia de dominio (deshabilitado).

## Próxima verificación sugerida

Por orden de impacto, siguiendo la misma lógica de "elegir el cliente que más destraba":

1. ~~**Cerrar capa 4/6 de EBY**~~ **Hecho (2 sep 2026, cerrado del todo 12 sep 2026)** — `formsweb.cfg` + `tnsnames.ora` de `OPENWLPROD01` cerraron ambas para la ruta `eby-prod` (EBY a ~90%); `sqlplus` a `Database .90`/`CDRADM` descartó la ruta paralela `yacyreta` de DB propia (solo `SIGO` tiene schema ahí). **EBY a 100%, trazado completo 7/7.**
2. ~~**Sesión en `192.1.2.54` (`WL12C-Desarrollo`)**~~ **Hecho (6 sep 2026)** — accedido por SSH. Corre el Forms prod de BOCA (`WLS_FORMS`); sin consola ni sudo. Cerró BOCA a ~90% (falta solo su capa 6). **No** cerró ABB — ese box no expone datasources sin sudo.
3. ~~**Capa 6 de BOCA**~~ **Hecho (12 sep 2026)** — `root@192.1.1.32`, `ORACLE_SID=BOCA` → `v$database` reveló `CDB=YES`: la CDB `BOCA` está casi vacía, los datos reales viven en la PDB `BOCAPDB` (schema `CONDOR`, ~22.5 GB, charset `WE8MSWIN1252` confirmado). Último DML el mismo día — uso activo sin ambigüedad. **BOCA a 100%, trazado completo 7/7.**
4. ~~**ABB (Tier 1 #4) directo a las DBs**~~ **Hecho (6 sep 2026)** — `root@192.1.1.31` → instancia `ABB` / schema `CONDOR`; último DML `01/07/2026 17:24` calza exacto con la última sesión Forms del access log. `192.1.1.190` descartada (sin instancia `ABB`, solo `abbhist`/`abbtubio` apagadas). ABB a ~90%, trazado completo. **ABB apagado de hecho desde el 1‑jul‑2026** — sin sesión ni DML desde (dato para `QUESTIONS.md`). Bonos: ESYOP (~46%) y DCVIAJES (~46%) ganaron capa 6 en vivo. Ver `plan_relevamiento_alta_abb.md`.
5. ~~**Access log del NPM de `DOCKER-DEB` para ROMAN**~~ **Hecho (12/13 sep 2026)** — `proxy-host-93`/`94` (NPM en contenedor `ssl-app-1`): 0 `lservlet` en 5.097 requests, 100% bots. Capa 1 cerrada, confirma que ROMAN está dormido. **Queda solo:**
   - **`v$session` en `OPENDBPROD03`** (`10.77.7.30:1525`, servicio `PRODCSM`) — bloqueado por credencial de DB; `ssh root@10.77.7.30` **probado y rechazado (12 sep 2026)**, el mismo `root` que cerró BOCA no sirve en `10.77.7.x` (tampoco en `.151`/`.15`). Pendiente conseguir credencial específica para ese segmento — ver `QUESTIONS.md`.
6. ~~**Confirmar si `serzarex` es Rex Argentina y cuál de sus dos alias de DB está en uso**~~ **Hecho (6 sep 2026)** — `v$session` en `PRODREX01` confirma `OL8LABWL01` + `OPENDBPROD001` como el stack real, con usuarios nominales reales. Rex Argentina a ~68%. ~~Ítem 2 de Tier 1 prácticamente cerrado (falta storage/NAT dedicado)~~ **cerrado del todo (14 sep 2026)** — ver `plan_relevamiento_alta_rex.md`: NAT confirmado por coincidencia de puertos reales de contenedor, storage confirmado (y roto). Rex Argentina a **100%**.
7. **Identificar `192.1.3.34` (`PDBREXPROD`)** — VM sin rastro en `ExportList.csv`, segmento aislado `192.1.3.x`, blind spot nuevo abierto por este mismo `tnsnames.ora`.
8. **Clasificar `OPENDBDES011` (`10.77.7.151`, "Roman test" en vCenter)** — descartado de la ruta *productiva* de ROMAN el 6 sep, pero sigue sin trazar qué corre realmente ahí (¿la DB de test `romanstest` de la matriz? ¿otra cosa? — `VINST2025` del `tnsnames.ora` de `OPENWLPROD01` también resuelve a esta IP).
9. **Argocean — probar el `Diagnostics > Command Prompt` de `FW` (`192.1.3.1`)** para `172.18.5.40` (`WL-CLIENTES`) — el único camino de acceso que queda sin probar correctamente. `ping` primero; si responde, el mismo `ssh` ya armado (`soportesmart`, host keys viejas). Cierra capa 4 en cualquiera de los dos sentidos.
10. ~~**Re-correr `v$session` en GIAR/ESYOP/DCVIAJES en día hábil**~~ **ESYOP cerrado (14 sep 2026)** — trazado dedicado con evidencia en vivo (sesión Forms real + `netstat` capturando la conexión), sin necesitar repetir nada en día hábil. Quedan **GIAR** (ya resuelto por otra vía, ver `plan_relevamiento_alta_giar.md`) y **DCVIAJES** (sin trazado dedicado todavía, capa 6 sigue en ~0.6 por consulta de fin de semana).
11. **Identificar el contenedor Jasper de ESYOP** (capa 5 de `plan_relevamiento_alta_esyop.md`) — matriz marca `Jasper: Sí`, sin referencia en `formsweb.cfg`. Baja prioridad: revisar los hosts Docker con "múltiples instancias de JasperReports" ya listados en `relevamiento_docker.txt`.
